---
milestone: v2.2
milestone_name: Security Hardening
audited: 2026-04-13
status: passed
---

# v2.2 Milestone Audit — Security Hardening

## Scope

4 high-severity findings from the 2026-04-11 full system audit, grouped into 2 phases:

- **Phase 39 — Path Validation**: SEC2-01, SEC2-02
- **Phase 40 — Execution & Parser Hardening**: SEC2-03, SEC2-04

Shipped via PR #47 (merged 2026-04-13 into main). CI: all 8 checks green (security, governance, GitGuardian, tests across ubuntu 20/22/24, macos 22, windows 22).

## Process Deviation

The v2.2 milestone bypassed the standard discuss-phase → plan-phase → execute-phase ceremony. No `PLAN.md`, `SUMMARY.md`, or `VERIFICATION.md` artifacts exist in `.planning/phases/phase-39/` or `phase-40/`. Implementation proceeded directly from `REQUIREMENTS.md` to code + tests + PR. This was appropriate given the narrow scope (4 well-specified, independent security fixes with clear acceptance criteria and source line references), but means this audit is verifying against the requirements document rather than phase verification files.

## Requirements Coverage

### SEC2-01: @file: Protocol Path Containment — PASS

**Acceptance criteria verification:**
- ✅ Allowlist enforced at `get-shit-done/bin/gsd-tools.cjs:303-314`
- ✅ `inProject` check: `resolvedFile.startsWith(path.resolve(cwd) + path.sep)`
- ✅ `inTmpGsd` check: `tmpdir` + `gsd-` prefix + `.json` suffix
- ✅ Traversal rejected with explicit `Error('@file: path not in allowlist: ...')`
- ✅ Tests present: `tests/security-hardening-v2.2.test.cjs` SEC2-01 describe block (3 tests)

### SEC2-02: Command Path Validation — PASS

**Acceptance criteria verification:**
- ✅ `requireSafePath` applied in `cmdSummaryExtract` at `commands.cjs:409`
- ✅ `requireSafePath` applied in `cmdTodoComplete` at `commands.cjs:722`
- ✅ Reuses existing `security.cjs` helper (no new utility)
- ✅ Tests present: SEC2-02 describe blocks for both commands (7 tests total)

### SEC2-03: execSync Replacement — PASS

**Acceptance criteria verification:**
- ✅ No `execSync` references remain in `init.cjs` (grep confirms 0 matches)
- ✅ No `child_process` import remains in `init.cjs`
- ✅ Replaced with `execGit` and `safeExec` from `core.cjs`
- ✅ No behavioral regression: full suite 2411/2411 pass
- ✅ Dedicated regression test: SEC2-03 describe block asserts absence of both imports

### SEC2-04: Frontmatter Parser Hardening — PASS

**Acceptance criteria verification:**
- ✅ Regex parser replaced with indexOf scanner at `frontmatter.cjs:24-50` (O(n), zero backtracking)
- ✅ `escapeRegex(blockName)` applied at `frontmatter.cjs:223` (injection prevented)
- ✅ `MAX_INPUT_SIZE = 1024 * 1024` guard at `frontmatter.cjs:10`, enforced in `extractFrontmatter` (line 17) and `parseMustHavesBlock` (line 207)
- ✅ ReDoS timing test in SEC2-04 block asserts pathological input completes in < 1000ms
- ✅ No new dependencies added
- ✅ Tests present: 11 tests in SEC2-04 describe block covering oversize, CRLF, multi-block, splice, injection

## Integration Check

**Cross-phase wiring:**
- SEC2-01 (gsd-tools.cjs) and SEC2-02 (commands.cjs) both depend on path resolution from `security.cjs` — consistent `requireSafePath` usage pattern. No conflict.
- SEC2-03 (init.cjs) moved from `child_process` → `core.cjs` wrappers. `core.cjs` already exports `execGit`/`safeExec` used elsewhere — pattern alignment across codebase.
- SEC2-04 (frontmatter.cjs) `escapeRegex` consumed from `core.cjs` — already used by other modules. No duplicate implementation.

**No cross-phase integration gaps identified.** Each fix is independent and touches a distinct module. The shared dependency (`security.cjs`, `core.cjs`) is pre-existing infrastructure.

## End-to-End Verification

- **Full suite**: 2411 pass / 0 fail / 459 suites / 6.9s
- **Security-specific suite**: 22 pass / 0 fail (tests/security-hardening-v2.2.test.cjs)
- **CI on PR #47**: all 8 jobs green across platforms (ubuntu 20/22/24, macos 22, windows 22, plus security/governance/GitGuardian)

## Tech Debt / Deferred Items

- **Process deviation**: No formal phase SUMMARY.md/VERIFICATION.md artifacts. Acceptable for narrow-scope security milestones with explicit source-line requirements. Consider formalizing a "direct-to-implementation" path for similar targeted hardening work in future process docs.
- **Phase directory cleanup**: `phase-39/README.md` and empty `phase-40/` exist without accompanying artifacts. These should be cleaned up during `/gsd:complete-milestone` archive step.

## Verdict

**PASS** — All 4 requirements met against acceptance criteria, full suite green, CI green, shipped. Ready for `/gsd:complete-milestone 2.2`.
