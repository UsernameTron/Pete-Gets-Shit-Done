---
milestone: v2.9
audited: 2026-07-15
status: tech_debt
---

# v2.9 Autonomous Workflows Completion — Milestone Audit

**Scope:** FIN-01, FIN-02 (Phase 58) + SHIP-01..04 (Phase 59). 3-source cross-reference: REQUIREMENTS.md ↔ ROADMAP.md ↔ tree on `main`.
**Requirements score:** 6/6 met. **Verdict:** `tech_debt` — every requirement satisfied by the tree; deferred items below are worth carrying forward, none block close-out.

## Requirements

| REQ | Verdict | Evidence |
|-----|---------|----------|
| FIN-01 — Gate 5.5 availability check + graceful `[skip]` when `repo-doc-architect` unresolvable | **met** | `commands/gsd/finalize.md:132-153` (availability check precedes spawn contract; verbatim `[skip]` notice names the closeout-deferred-drift caveat + `/gsd:sync-docs` fallback; "Never error, never attempt a spawn that cannot resolve"); locked by `tests/finalize.test.cjs:36-77` (10/10 pass) |
| FIN-02 — finalize exercised e2e on a close-out; every push behind a consent gate, zero ungated remote ops | **met** (operator-ratified sandbox vehicle) | `.planning/phases/58-finalize-hardening/VERIFICATION.md` — two full 8-gate runs, live consent at Gates 1/7, `[auto-push]` receipts under `--yes-push`, Gate 2 hard-stop vs injected build failure, origin audit shows only post-consent refs. Criterion 2 (agent-available spawn) verified structurally only — see tech-debt #4 |
| SHIP-01 — `ship-milestone.md` composes the finalizer critical path behind exactly 2 gates | **met** | `get-shit-done/workflows/ship-milestone.md` — chain health→audit-agents→sync-docs→coverage+drift→audit-milestone→GATE 1→ship/ci-watch→GATE 2→complete-milestone; conditional audit-verdict gate + completion-authorization gate; `tests/ship-milestone.test.cjs` chain-order + `AskUserQuestion` count===2 assertions pass |
| SHIP-02 — routed via `/gsd:do` as `workflow:ship-milestone`; shelved status lifted (re-pointed: registry membership + description quality) | **met** | `node get-shit-done/bin/gsd-tools.cjs do-registry` emits `"name": "workflow:ship-milestone"`; frontmatter description disambiguates vs `complete-milestone` and `ship-and-merge`; blind spot-check 3/3 (`GSD-AUTONOMOUS-WORKFLOWS.md:141`); shelf lifted — W5 heading `:135` and Ranked-build row `:262` both read "BUILT (Phase 59, 2026-07-15)", no live SHELVED flag (blockquote is historical progression ending "The shelf is lifted") |
| SHIP-03 — `complete-milestone`'s 3 internal prompts stay live human; GATE 2 authorizes *starting* only | **met** | `ship-milestone.md:216-234` — "passes no pre-answers and no auto-flags"; names archive-phases/branch-handling/tag-push as live; `tests/ship-milestone.test.cjs:100-107` asserts the invocation carries no `--yes/--auto/-y` flag |
| SHIP-04 — structural tests: registry includes it, referenced `/gsd:` commands resolve, ≤2 gates (re-pointed: routing table→do-registry) | **met** | `tests/ship-milestone.test.cjs` (12/12) + `tests/do-routing.test.cjs` (15/15) — `WORKFLOW_FLOWS` line 37 includes `ship-milestone`; every `Skill(skill="gsd:X")` ref resolves to a real command file; `AskUserQuestion` count === 2 |

Combined test run `node --test tests/ship-milestone.test.cjs tests/do-routing.test.cjs tests/finalize.test.cjs`: **37 pass / 0 fail** (12 + 15 + 10).

## Phase Criteria Results

**Phase 58 — Finalize Hardening & Re-verification**
1. Unavailable path completes Gate 5.5 without failed spawn, logs skip, continues to Gate 6 — **met** (`finalize.md:134-140`).
2. Available path still spawns `repo-doc-architect`, no regression — **structural only** (spawn contract byte-identical, locked by tests; first live spawn = this v2.9 close-out; see tech-debt #4).
3. E2E on a real close-out, every push gated, zero ungated ops — **met** via operator-ratified sandbox (VERIFICATION.md).
4. `GSD-AUTONOMOUS-WORKFLOWS.md` no longer lists Gate 5.5 fragility or e2e re-verification as open preconditions — **met** (`:143` "No open unshelve preconditions remain").

**Phase 59 — Ship-Milestone Workflow**
1. Workflow exists, composes chain behind exactly 2 gates — **met**.
2. Close-out intent routes to `workflow:ship-milestone`, preferred over `complete-milestone`; shelf lifted everywhere — **met**.
3. Through GATE 2, `complete-milestone`'s 3 prompts stay live — **met**.
4. Structural suite asserts registry membership + ref resolution + ≤2 gates — **met**.

**Phase 57.1 — Bitter Lesson Surgery (off-milestone insert, no REQ-IDs)** — shipped PR #51; `do.md` routing table removed, registry-based routing in place (`do-routing.test.cjs` "contains no routing table" + "references the do-registry" pass). Context only; outside the 6-REQ audit scope.

## Tech-Debt List (tracked, not gaps)

1. **HOOK-01** — version hook registrations (settings template + installer contract test) so a fresh clone gets the runtime safety net. **Explicitly deferred** in `REQUIREMENTS.md:27`; hygiene, not a workflow. Not a v2.9 gap.
2. **Ecosystem audit FLAG** (`.planning/ecosystem/ECOSYSTEM-REPORT.md`, verdict FLAG not BLOCK) — 8 install-drift items + 1 tool note (`test-runner` declared `isolation: worktree` without the Write tool). **test-runner: genuinely RESOLVED (2026-07-15)** — Write moved out of `disallowedTools` (its own description requires writing new test suites; stale counts dropped). **8 "install-drift" items: FALSE POSITIVE, reclassified (2026-07-15 close-out re-entry)** — verified line-by-line that every difference is solely the GSD installer's deliberate `~/` → `$HOME/` path normalization; zero content drift, and `$HOME/` is the *correct* installed form (expands reliably in non-interactive shells). The earlier `cp` resync was a misdiagnosis: it briefly forced byte-equality by installing the un-normalized `~/` source, but the installer re-normalizes on the next install/session, so it neither holds nor should. No file action needed — the real follow-up is auditor item 5 below.
3. **discuss-phase / smart-discuss description overlap** — routing-signal overlap flagged and **DEFERRED** in BITTER_LESSON_LOG. Watch for misroutes; not affecting the audited flows. **RESOLVED (2026-07-15, post-GATE-1 fix pass):** discuss-phase description rewritten to defer recommended-defaults intents to workflow:smart-discuss; blind re-judge of eval id 8 now picks smart-discuss decisively (was AMBIGUOUS).
4. **FIN-02 criterion 2 — agent-available spawn verified structurally only** — the live spawn path is contract-locked (`tests/finalize.test.cjs`) but was not yet exercised live at Phase 58 sandbox time. **Now resolvable:** `~/.claude/agents/repo-doc-architect.md` is present in this install, so Gate 5.5 spawns it live during this very close-out — the first real exercise of the agent-available path. Documented limitation in VERIFICATION.md, operator-accepted; closed by this run.
5. **Ecosystem auditor false-positive on path normalization** (new, 2026-07-15) — `gsd-ecosystem-auditor` byte-compared repo `agents/*.md` against installed copies without accounting for the installer's `~/` → `$HOME/` rewrite, so it reported phantom "install-drift" items. **RESOLVED (2026-07-15, commit `37a6ea6`):** Step 7 now normalizes the `~/` ↔ `$HOME/` transform on both sides before diffing and downgrades path-form-only differences to an informational note; only content differences that survive normalization are flagged. Verified: normalized comparison reports 0 real drift across the roster (the lone post-fix "diff" is the auditor file's own committed edit, which reconciles on next install).

## Verdict Rationale

All 6 requirements are satisfied by the actual tree, not just by SUMMARY/traceability claims: the finalize Gate 5.5 skip path and consent protocol are present in the command file and locked by 10 passing tests; the ship-milestone workflow exists with exactly 2 `AskUserQuestion` gates, the mandated chain order, verbatim gate prompts, and a no-pre-answer contract, all verified by 12 passing structural tests; the workflow is registry-routable (`do-registry` emits `workflow:ship-milestone`) and the shelf is lifted in every status surface. Status is `tech_debt` rather than `passed` for exactly ONE reason now: **HOOK-01**, which was **explicitly deferred to a future milestone** in `REQUIREMENTS.md` from the outset and was never v2.9 scope. Every other item is closed: test-runner tool fixed, discuss/smart-discuss overlap fixed (blind re-judge confirms), the 8 "install-drift" items reclassified as benign path-normalization AND the auditor root-caused so it no longer false-positives (item 5, commit `37a6ea6`), and FIN-02 criterion 2's live spawn is exercised by this close-out. No requirement gap; nothing blocks completion. Completing v2.9 with HOOK-01 carried to the next milestone is the correct, intended path — this is what a `tech_debt` verdict with a single deferred-by-design item means.

_Audited: 2026-07-15 · Verifier: Claude (gsd-verifier scope:integration, audit-milestone flow)_
