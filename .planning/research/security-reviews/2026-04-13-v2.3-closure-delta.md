---
title: v2.3 Security Hooks Review — Closure Delta
milestone: v2.3
reviewed: 2026-04-07
closure_verified: 2026-04-15
source_review: ./2026-04-07-hooks-review.md
---

# v2.3 Closure Delta — Hooks Security Review

Maps each Critical Finding from the 2026-04-07 review to its closure status
after Phase 41 (Hook Ports) and Phase 42 (Security Guardian) shipped.

| # | Finding | Status | Evidence |
|---|---|---|---|
| 1 | Hook Version Tracking broken | Deferred to v2.4 | Logic present in `hooks/gsd-check-update.js` lines 40-85 template literal (reads `// gsd-hook-version: X` header). No hook source file carries that header yet. Low severity — no security impact, only a stale-hook nag. Carried as v2.4 backlog item. |
| 2 | Shell injection in gsd-check-update.js | Closed by design | `spawn(process.execPath, ['-e', template])` uses argv array (no shell). Template interpolates only cacheFile / projectVersionFile / globalVersionFile, each wrapped in `JSON.stringify()`. Inner `execSync` uses hardcoded literal `'npm view get-shit-done-cc version'`. Zero `shell:true` anywhere. No untrusted input path. |
| 3 | Inlined Pattern Duplication | Closed | `hooks/gsd-prompt-guard.js` INJECTION_PATTERNS is the single source of 18 regex literals (13 original + 5 new). Tests (`tests/prompt-injection-scan.test.cjs`, `tests/security.test.cjs`) reference patterns by design as verification, not as duplication. Closure commit: `1b64c1b feat(41-01): enhance prompt guard to 18 patterns with fail-closed blocking`. |
| 4 | Advisory-Only Guards Weak | Closed for security-critical hooks | `hooks/gsd-config-protection.js:90` and `hooks/gsd-prompt-guard.js:99` both `process.exit(2)` with `permissionDecision: 'deny'`. `gsd-context-monitor` and `gsd-workflow-guard` remain advisory by design — they are informational, not security. Closure commits: `4754c58 feat(41-01): port config protection hook`, `1b64c1b feat(41-01): enhance prompt guard to 18 patterns`. |
| 5 | Incomplete Subagent Detection | Closed | `hooks/gsd-workflow-guard.js:35` checks `data.tool_input?.is_subagent OR data.session_type === 'task'`. `hooks/gsd-context-monitor.js:65` exits silently in subagent context. `.claude/settings.json:10` registers SubagentStop. |

## Outcome

4 of 5 critical findings closed in v2.3. 1 deferred to v2.4 as a low-severity
trivial fix (add version headers to 7 hook source files). Security review
retired; any future hook security work should reference this delta.

## Phase 41/42 commits referenced

- `3d28633` docs(41) phase plan
- `1b64c1b` feat(41-01) prompt guard 18 patterns + fail-closed
- `4754c58` feat(41-01) config protection hook
- `ff8412c` feat(41-02) cost tracker hook
- `3acb687` feat(41-02) wire hooks into pipeline + installer
- `2fa9073` feat(42-01) gsd-security-guardian + threat model
- `192ff9e` fix(42-01) remove absolute path from agent
- `208c26e` verify(42) Phase 42 verified — SEC3-01, SEC3-02 satisfied
