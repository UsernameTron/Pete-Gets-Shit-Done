---
phase: 28-stale-reference-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - commands/gsd/research-phase.md
  - commands/gsd/crew.md
  - commands/gsd/plan-phase.md
  - governance/templates/global/CLAUDE.md
autonomous: true
requirements: [HYG-01, HYG-03]

must_haves:
  truths:
    - "grep for gsd-plan-checker in command files and governance/templates/global/CLAUDE.md returns zero hits"
    - "grep for gsd-integration-checker in command files and governance/templates/global/CLAUDE.md returns zero hits"
    - "grep for gsd-nyquist-auditor in command files and governance/templates/global/CLAUDE.md returns zero hits"
    - "grep for gsd-phase-researcher in command files returns zero hits (excluding provenance in agent definitions)"
    - "grep for gsd-project-researcher in command files returns zero hits (excluding provenance in agent definitions)"
    - "governance/templates/global/CLAUDE.md Phase 3 Quality Gates references gsd-verifier with scope parameter"
  artifacts:
    - path: "commands/gsd/research-phase.md"
      provides: "Research phase command with current agent names"
      contains: "gsd-research-orchestrator"
    - path: "commands/gsd/crew.md"
      provides: "Crew roster with current agent names"
      contains: "gsd-verifier"
    - path: "commands/gsd/plan-phase.md"
      provides: "Plan phase command with current agent name"
      contains: "gsd-verifier (scope: plan)"
    - path: "governance/templates/global/CLAUDE.md"
      provides: "Governance CLAUDE.md template with current agent names"
      contains: "gsd-verifier"
  key_links:
    - from: "commands/gsd/research-phase.md"
      to: "agents/gsd-research-orchestrator.md"
      via: "resolve-model call and spawn instruction"
      pattern: "resolve-model gsd-research-orchestrator"
    - from: "commands/gsd/plan-phase.md"
      to: "agents/gsd-verifier.md"
      via: "verification step reference"
      pattern: "gsd-verifier.*scope.*plan"
---

<objective>
Replace stale consolidated agent names in all command files and both CLAUDE.md files (project root + governance template).

Purpose: Command files are executable prompts that agents follow. Stale names cause agents to reference non-existent agents, breaking spawn chains and confusing quality gates. The two CLAUDE.md files are read at every session start — stale names there mislead every session.

Output: 5 files updated with current agent names. Zero stale agent references in commands/ or CLAUDE.md.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update command files — research-phase, plan-phase, crew</name>
  <files>commands/gsd/research-phase.md, commands/gsd/plan-phase.md, commands/gsd/crew.md</files>
  <read_first>commands/gsd/research-phase.md, commands/gsd/plan-phase.md, commands/gsd/crew.md</read_first>
  <action>
**Per HYG-01: Replace stale agent names with current equivalents.**

Agent name mapping:
- gsd-plan-checker -> gsd-verifier (scope: plan)
- gsd-integration-checker -> gsd-verifier (scope: integration)
- gsd-nyquist-auditor -> gsd-verifier (scope: nyquist)
- gsd-phase-researcher -> gsd-research-orchestrator (scope: phase)
- gsd-project-researcher -> gsd-research-orchestrator (scope: project)

**commands/gsd/research-phase.md** — 4 changes:
1. Line 12: "Spawns gsd-phase-researcher agent" -> "Spawns gsd-research-orchestrator agent (scope: phase)"
2. Line 50: `resolve-model gsd-phase-researcher` -> `resolve-model gsd-research-orchestrator`
3. Line 80: "Spawn gsd-phase-researcher Agent" -> "Spawn gsd-research-orchestrator Agent (scope: phase)"
4. Line 192: "gsd-phase-researcher spawned" -> "gsd-research-orchestrator spawned"
NOTE: Lines 28-29 (available_agent_types) and line 145 (Task() call) already use "gsd-research-orchestrator" — do NOT change those.

**commands/gsd/plan-phase.md** — 1 change:
1. Line 21: "verify with gsd-plan-checker" -> "verify with gsd-verifier (scope: plan)"

**commands/gsd/crew.md** — 11 changes across ASCII roster, spawning map, detail example, and recommend example:
- ASCII roster (lines 76, 80-81, 91-92): Replace each stale agent entry with its current equivalent. Collapse the 3 checker entries (gsd-plan-checker, gsd-integration-checker, gsd-nyquist-auditor) into a single "gsd-verifier" entry with note "(scope: plan | integration | nyquist)". Collapse the 2 researcher entries (gsd-phase-researcher, gsd-project-researcher) into a single "gsd-research-orchestrator" entry with note "(scope: phase | project)".
- Spawning map (lines 139-140): Replace "gsd-phase-researcher (provides RESEARCH.md input)" with "gsd-research-orchestrator (provides RESEARCH.md input)" and "gsd-plan-checker (validates output)" with "gsd-verifier scope: plan (validates output)"
- Detail example (line ~139-140 area): Update any stale names in the planner dependency chain
- Recommend example (lines 241, 243): Replace "gsd-phase-researcher" with "gsd-research-orchestrator" and "gsd-plan-checker" with "gsd-verifier (scope: plan)"

Preserve ASCII alignment and formatting. Match existing indentation patterns.
  </action>
  <verify>
    <automated>cd /Users/cpconnor/projects/Pete-Gets-Shit-Done && grep -n "gsd-plan-checker\|gsd-integration-checker\|gsd-nyquist-auditor\|gsd-phase-researcher\|gsd-project-researcher" commands/gsd/research-phase.md commands/gsd/plan-phase.md commands/gsd/crew.md; echo "EXIT:$?"</automated>
  </verify>
  <done>grep returns zero hits (EXIT:1) across all three command files. All references use gsd-verifier or gsd-research-orchestrator with scope parameter.</done>
</task>

<task type="auto">
  <name>Task 2: Update governance template CLAUDE.md — Phase 3 Quality Gates (HYG-03)</name>
  <files>governance/templates/global/CLAUDE.md</files>
  <read_first>governance/templates/global/CLAUDE.md</read_first>
  <action>
**Per HYG-03: Fix Phase 3 Quality Gates agent references in governance/templates/global/CLAUDE.md.**

NOTE: The project root CLAUDE.md does NOT contain a Phase 3 Quality Gates section with stale agent names — only the governance template does. Do NOT modify the project root CLAUDE.md for this task.

The governance template has stale refs at its Phase 3 Quality Gates section (lines ~141-147):
```
    - **gsd-plan-checker** — Validates plan completeness before execution
    - **gsd-verifier** — Validates implementation against spec
    - **gsd-integration-checker** — Cross-component integration validation
```

Replace with:
```
    - **gsd-verifier** (scope: plan) — Validates plan completeness before execution
    - **gsd-verifier** (scope: integration) — Cross-component integration validation
    - **gsd-verifier** (scope: nyquist) — Fills validation gaps in completed phases
```

This collapses the 3 separate agent bullet points into the single gsd-verifier with scope parameters, removing the redundant "gsd-verifier — Validates implementation against spec" line since that is now covered by scope: integration. The 3 scope variations are the complete replacement for the old 3 separate agents plus the generic verifier line.
  </action>
  <verify>
    <automated>cd /Users/cpconnor/projects/Pete-Gets-Shit-Done && grep -n "gsd-plan-checker\|gsd-integration-checker\|gsd-nyquist-auditor" governance/templates/global/CLAUDE.md; echo "EXIT:$?"</automated>
  </verify>
  <done>grep returns zero hits (EXIT:1) in governance/templates/global/CLAUDE.md. Phase 3 Quality Gates section lists gsd-verifier with scope parameters only.</done>
</task>

</tasks>

<verification>
Full sweep of all Plan 01 files for any remaining stale references:
```bash
grep -rn "gsd-plan-checker\|gsd-integration-checker\|gsd-nyquist-auditor\|gsd-phase-researcher\|gsd-project-researcher" \
  commands/gsd/research-phase.md commands/gsd/plan-phase.md commands/gsd/crew.md \
  governance/templates/global/CLAUDE.md
```
Must return zero hits.

All existing tests must still pass:
```bash
npm test
```
</verification>

<success_criteria>
- Zero stale agent references in commands/gsd/*.md
- Zero stale agent references in governance/templates/global/CLAUDE.md
- Phase 3 Quality Gates section in governance template references gsd-verifier with scope parameters
- All tests pass (no functional code changed, but verify nothing broke)
</success_criteria>

<output>
After completion, create `.planning/phases/28-stale-reference-cleanup/28-01-SUMMARY.md`
</output>
