---
phase: 36-system-component-audit
plan: 02
type: execute
wave: 2
depends_on: ["36-01"]
files_modified:
  - .planning/phases/36-system-component-audit/36-AUDIT-REPORT.md
autonomous: true
requirements: [AUDIT-04, AUDIT-05]

must_haves:
  truths:
    - "All GSD commands (workflow .md files) are reachable -- each has frontmatter with name/description"
    - "No orphaned workflow files exist (files with no routing path)"
    - "All configured hooks have correct matchers for their intended events"
    - "Hook commands reference valid files that exist on disk"
  artifacts:
    - path: ".planning/phases/36-system-component-audit/36-AUDIT-REPORT.md"
      provides: "Command routing and hook audit sections appended"
      contains: "AUDIT-04"
  key_links:
    - from: "~/.claude/get-shit-done/workflows/*.md"
      to: "36-AUDIT-REPORT.md"
      via: "Frontmatter validation and reachability check"
      pattern: "AUDIT-04"
    - from: "~/.claude/settings.json"
      to: "36-AUDIT-REPORT.md"
      via: "Hook configuration extraction and validation"
      pattern: "AUDIT-05"
---

<objective>
Audit all GSD commands for routing reachability and all hooks for correct configuration and firing.

Purpose: Verify AUDIT-04 (command routing) and AUDIT-05 (hook functionality) to complete the system component audit
Output: AUDIT-04 and AUDIT-05 sections appended to 36-AUDIT-REPORT.md
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-system-component-audit/36-AUDIT-REPORT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit GSD command routing -- verify all workflows are reachable</name>
  <files>.planning/phases/36-system-component-audit/36-AUDIT-REPORT.md</files>
  <read_first>
    - .planning/phases/36-system-component-audit/36-AUDIT-REPORT.md
    - /Users/cpconnor/projects/Pete-Gets-Shit-Done/commands/gsd/help.md
    - /Users/cpconnor/projects/Pete-Gets-Shit-Done/commands/gsd/do.md
  </read_first>
  <action>
Audit all 61 GSD command files for reachability. The source of truth is the project repo at `/Users/cpconnor/projects/Pete-Gets-Shit-Done/commands/gsd/` (61 .md files). Also compare against the installed copy at `~/.claude/get-shit-done/workflows/` (56 files) + `~/.claude/get-shit-done/commands/gsd/` (1 file) to identify any source-vs-installed discrepancies.

For each command file in `/Users/cpconnor/projects/Pete-Gets-Shit-Done/commands/gsd/*.md`:

**Check 1 — File has valid frontmatter or clear command name:**
- Read the first 20 lines of each file
- Check if it starts with `---` frontmatter containing a name/description, OR has a clear `# /gsd:command-name` heading
- Record the command name mapped to each file

**Check 2 — Routing reachability:**
A command is "reachable" if at least ONE of these is true:
  a. It appears in the help.md file as a listed command
  b. It is referenced by name in another workflow/command (e.g., do.md routes to it)
  c. It has a `/gsd:` prefix in its heading or frontmatter making it directly invokable
  d. It is an internal workflow invoked by the execution engine (e.g., execute-plan.md invoked by execute-phase.md)

For each file, record: filename, command name, reachable (yes/no), routing path (how it's reached).

**Check 3 — Orphan detection:**
Any command file that is NOT reachable by any of the 4 paths above is an "orphan."

**Check 4 — Source vs Installed comparison:**
Compare the 61 source files (`commands/gsd/*.md`) against installed files (`~/.claude/get-shit-done/workflows/*.md` + `~/.claude/get-shit-done/commands/gsd/*.md`). Report any source commands not present in the installed location (deployment gap).

Append the AUDIT-04 section to the existing 36-AUDIT-REPORT.md (replace the `<!-- AUDIT-04` placeholder):

```markdown
## AUDIT-04: GSD Command Routing Audit

### Summary

**Total source command files:** [count] (from commands/gsd/)
**Reachable commands:** [count]
**Orphaned commands:** [count]
**Source-only (not installed):** [count]

### Command Inventory

| # | File | Command | Reachable | Routing Path | Installed |
|---|------|---------|-----------|--------------|-----------|
| 1 | autonomous.md | /gsd:autonomous | Yes/No | [how reached] | Yes/No |
[...all 61 files...]

### Orphaned Commands (if any)

[List any files with no routing path]

### Source vs Installed Discrepancies (if any)

[List commands in source but not installed, or vice versa]

### AUDIT-04 Verdict

**Result:** PASS/FAIL
**Summary:** [X]/61 commands reachable. [details]
```
  </action>
  <verify>
    <automated>grep -c "AUDIT-04" .planning/phases/36-system-component-audit/36-AUDIT-REPORT.md && grep "AUDIT-04 Verdict" .planning/phases/36-system-component-audit/36-AUDIT-REPORT.md</automated>
  </verify>
  <acceptance_criteria>
    - 36-AUDIT-REPORT.md contains "## AUDIT-04: GSD Command Routing Audit"
    - Contains "### Command Inventory" table with rows for all workflow files
    - Contains "### AUDIT-04 Verdict" with PASS or FAIL
    - The total command count is 61 (matching source commands/gsd/ file count)
    - Each row has a Reachable column with Yes or No and an Installed column
    - Orphaned commands section lists any unreachable files (or states "None")
    - Source vs Installed discrepancies section documents any deployment gaps
  </acceptance_criteria>
  <done>All GSD workflow files audited for routing reachability with results in 36-AUDIT-REPORT.md</done>
</task>

<task type="auto">
  <name>Task 2: Audit hooks -- verify matchers, event targets, and referenced files</name>
  <files>.planning/phases/36-system-component-audit/36-AUDIT-REPORT.md</files>
  <read_first>
    - .planning/phases/36-system-component-audit/36-AUDIT-REPORT.md
    - ~/.claude/settings.json
    - /Users/cpconnor/projects/Pete-Gets-Shit-Done/.claude/settings.json
  </read_first>
  <action>
Audit all hooks configured in both settings files:
- `~/.claude/settings.json` (user-level hooks)
- `/Users/cpconnor/projects/Pete-Gets-Shit-Done/.claude/settings.json` (project-level hooks)

For each hook entry, validate:

**Check 1 — Event name is valid:**
Valid Claude Code hook events: SessionStart, PreToolUse, PostToolUse, PermissionRequest, UserPromptSubmit, Stop, SubagentStop, SessionEnd, Notification, PreCompact, ConfigChange.
Record PASS if event name matches, FAIL if not a valid event.

**Check 2 — Matcher is correct for intent:**
- For PreToolUse/PostToolUse hooks: matcher should reference valid tool names (Bash, Write, Edit, Read, Glob, Grep, WebFetch, mcp__*)
- Verify the matcher regex/pattern is syntactically valid
- Record the matcher and whether it covers the intended tool(s)

**Check 3 — Command references valid files:**
- If the hook command references a file path (e.g., `node "/Users/cpconnor/.claude/hooks/gsd-prompt-guard.js"`), verify the file EXISTS on disk
- Run `ls -la [path]` for each referenced file
- Record PASS if file exists, FAIL if file is missing

**Check 4 — No stale agent references in hook commands:**
- Search hook command strings for references to absorbed agents: gsd-plan-checker, gsd-integration-checker, gsd-nyquist-checker, gsd-crew-assessor
- Record PASS if none found, FAIL with specifics

The hooks to audit from ~/.claude/settings.json (15 hooks across 5 events):
1. SessionStart[0]: Project state scanner (inline script)
2. SessionStart[1]: gsd-check-update.js (file: ~/.claude/hooks/gsd-check-update.js)
3. PreToolUse[0] (Bash): Branch safety -- blocks commits on main/master (inline script)
4. PreToolUse[1] (Bash): Staged files -- blocks private/generated files (inline script)
5. PreToolUse[2] (Bash): Required docs check (inline script)
6. PreToolUse[3] (Bash): Secrets scan (inline script)
7. PreToolUse[4] (Bash): Nested repo check (inline script)
8. PreToolUse[5] (Bash): Pre-push dirty check (inline script)
9. PreToolUse[6] (Write|Edit): gsd-prompt-guard.js (file: ~/.claude/hooks/gsd-prompt-guard.js)
10. PreToolUse[7] (mcp__.*): MCP tool logger (inline script)
11. PostToolUse[0] (Write|Edit): File type advisor (inline script)
12. PostToolUse[1] (Bash|Edit|Write|MultiEdit|Agent|Task): gsd-context-monitor.js (file: ~/.claude/hooks/gsd-context-monitor.js)
13. Stop[0]: Uncommitted files check (inline script)
14. Stop[1]: gsd-lessons-check.sh (file: ~/.claude/hooks/gsd-lessons-check.sh)
15. PreCompact[0]: Task state preservation (inline script)

From project .claude/settings.json (1 hook):
16. Stop[0]: lesson-capture-gate.cjs (file: .claude/hooks/lesson-capture-gate.cjs)

Also note the statusLine entry in user settings referencing ~/.claude/hooks/gsd-statusline.js -- verify this file exists too.

Append AUDIT-05 section to 36-AUDIT-REPORT.md:

```markdown
## AUDIT-05: Hook Configuration Audit

### Summary

**Total hooks:** [count]
**Valid configuration:** [count]
**File references valid:** [count]
**Stale agent refs:** [count]

### Hook Inventory

| # | Event | Matcher | Purpose | Valid Event | Valid Matcher | Files Exist | No Stale Refs | Overall |
|---|-------|---------|---------|-------------|---------------|-------------|---------------|---------|
| 1 | SessionStart | (all) | Project scanner | PASS/FAIL | PASS/FAIL | PASS/FAIL | PASS/FAIL | PASS/FAIL |
[...all hooks...]

### File Reference Validation

| Hook | Referenced File | Exists |
|------|----------------|--------|
| PreToolUse[6] | ~/.claude/hooks/gsd-prompt-guard.js | Yes/No |
| PostToolUse[1] | ~/.claude/hooks/gsd-context-monitor.js | Yes/No |
| SessionStart[1] | ~/.claude/hooks/gsd-check-update.js | Yes/No |
| Stop[1] | ~/.claude/hooks/gsd-lessons-check.sh | Yes/No |
| Stop (project) | .claude/hooks/lesson-capture-gate.cjs | Yes/No |

### AUDIT-05 Verdict

**Result:** PASS/FAIL
**Summary:** [details]
```

Also add a final "## Phase 36 Overall Summary" section with a combined verdict table for AUDIT-03, AUDIT-04, AUDIT-05.
  </action>
  <verify>
    <automated>grep -c "AUDIT-05" .planning/phases/36-system-component-audit/36-AUDIT-REPORT.md && grep "Phase 36 Overall Summary" .planning/phases/36-system-component-audit/36-AUDIT-REPORT.md && grep "AUDIT-05 Verdict" .planning/phases/36-system-component-audit/36-AUDIT-REPORT.md</automated>
  </verify>
  <acceptance_criteria>
    - 36-AUDIT-REPORT.md contains "## AUDIT-05: Hook Configuration Audit"
    - Contains hook inventory table with rows for all 16 hooks
    - Contains "### File Reference Validation" table checking at least 5 referenced files
    - Contains "### AUDIT-05 Verdict" with PASS or FAIL
    - Contains "## Phase 36 Overall Summary" with combined AUDIT-03/04/05 table
    - File reference checks include: gsd-prompt-guard.js, gsd-context-monitor.js, gsd-check-update.js, gsd-lessons-check.sh, lesson-capture-gate.cjs, gsd-statusline.js
    - Each hook row has PASS or FAIL for all 4 checks
  </acceptance_criteria>
  <done>All hooks audited for configuration validity with combined Phase 36 summary in 36-AUDIT-REPORT.md</done>
</task>

</tasks>

<verification>
- 36-AUDIT-REPORT.md contains all three AUDIT sections (03, 04, 05)
- Every section has a Verdict with PASS or FAIL
- Phase 36 Overall Summary provides combined results
- Command count matches actual file count
- Hook count matches actual configuration entries
</verification>

<success_criteria>
- AUDIT-04: All workflow files inventoried with reachability status
- AUDIT-05: All hooks validated for event, matcher, file refs, and stale refs
- Phase 36 Overall Summary combines all three audit verdicts
</success_criteria>

<output>
After completion, create `.planning/phases/36-system-component-audit/36-02-SUMMARY.md`
</output>
