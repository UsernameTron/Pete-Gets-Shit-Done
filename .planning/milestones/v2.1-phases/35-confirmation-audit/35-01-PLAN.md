---
phase: 35-confirmation-audit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/35-confirmation-audit/35-AUDIT-REPORT.md
autonomous: true
requirements:
  - AUDIT-01
  - AUDIT-02

must_haves:
  truths:
    - "Every agent .md file in ~/.claude/agents/ and .claude/agents/ has a tier label that matches its actual tools: field"
    - "gsd-validator-hub is referenced by at least one workflow file and the agent definition exists"
    - "An audit report documents pass/fail evidence for both INT-01 and INT-02"
  artifacts:
    - path: ".planning/phases/35-confirmation-audit/35-AUDIT-REPORT.md"
      provides: "Pass/fail audit report for AUDIT-01 and AUDIT-02"
      contains: "AUDIT-01"
  key_links:
    - from: "~/.claude/agents/*.md"
      to: ".planning/phases/35-confirmation-audit/35-AUDIT-REPORT.md"
      via: "Tier label vs tools field comparison"
      pattern: "tier.*tools"
    - from: "~/.claude/get-shit-done/workflows/*.md"
      to: "~/.claude/agents/gsd-validator-hub.md"
      via: "Workflow routing reference"
      pattern: "gsd-validator-hub"
---

<objective>
Audit and verify that two v1.4 integration fixes (INT-01 and INT-02) are fully resolved with no regressions.

Purpose: Confirm prior debt closure actually landed — tier labels match tool grants across all 15 GSD agents, and gsd-validator-hub is reachable through workflow routing.
Output: Audit report at `.planning/phases/35-confirmation-audit/35-AUDIT-REPORT.md` with pass/fail evidence for each check.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit agent tier labels vs tool grants (AUDIT-01)</name>
  <files>.planning/phases/35-confirmation-audit/35-AUDIT-REPORT.md</files>
  <read_first>
    - All .md files in ~/.claude/agents/ (29 files — read frontmatter of each)
    - All .md files in .claude/agents/ (3 files — read frontmatter of each)
  </read_first>
  <action>
    Read every agent .md file in both ~/.claude/agents/ and .claude/agents/. For each agent:
    1. Extract the `tools:` field from YAML frontmatter (or note if missing)
    2. Extract any tier label (look for "Tier N" in frontmatter, comments, or body text)
    3. Classify the actual tier based on tool grants:
       - Tier 1 (Read-only): Only Read, Grep, Glob, WebFetch, WebSearch
       - Tier 2 (Read+Write): Adds Write, Edit, Bash (limited)
       - Tier 3 (Full access): Full Bash, all tools, or no restrictions
    4. Compare declared tier label to actual tier from tools
    5. Flag any mismatches

    The 15 "source agents" referenced in AUDIT-01 are the GSD built-in agents (gsd-* prefix in ~/.claude/agents/).
    Also audit the 3 project agents in .claude/agents/ and any other non-GSD agents found.

    Per AUDIT-01: verify v1.4 DEBT-01 resolved INT-01. Every agent must have a tier label that matches its actual tool grants.

    Write the first section of 35-AUDIT-REPORT.md with a table showing:
    | Agent | Location | Declared Tier | Actual Tier (from tools) | Match? |

    Mark each row PASS or FAIL. Include a summary verdict for AUDIT-01.
  </action>
  <verify>
    <automated>grep -c "PASS\|FAIL" .planning/phases/35-confirmation-audit/35-AUDIT-REPORT.md</automated>
  </verify>
  <acceptance_criteria>Every agent .md file is listed in the report table with tier comparison. No agent is missing from the audit. AUDIT-01 section has a clear PASS/FAIL verdict.</acceptance_criteria>
  <done>All agents audited with tier-vs-tools comparison, results documented in audit report with per-agent PASS/FAIL and overall AUDIT-01 verdict.</done>
</task>

<task type="auto">
  <name>Task 2: Audit gsd-validator-hub workflow routing (AUDIT-02)</name>
  <files>.planning/phases/35-confirmation-audit/35-AUDIT-REPORT.md</files>
  <read_first>
    - ~/.claude/agents/gsd-validator-hub.md
    - All .md files in ~/.claude/get-shit-done/workflows/ (search for "validator-hub" references)
  </read_first>
  <action>
    Per AUDIT-02: verify v1.4 DEBT-04 resolved INT-02 — gsd-validator-hub is reachable through at least one workflow routing path.

    1. Confirm gsd-validator-hub.md exists in ~/.claude/agents/ and has valid frontmatter
    2. Search all workflow files in ~/.claude/get-shit-done/workflows/ for references to "gsd-validator-hub" or "validator-hub"
    3. For each workflow that references it, document the routing path:
       - Which workflow file
       - What triggers the route to gsd-validator-hub
       - What the validator-hub does in that workflow context
    4. Also search skill files in ~/.claude/get-shit-done/skills/ for references (secondary routing)

    Append the AUDIT-02 section to 35-AUDIT-REPORT.md with:
    - Agent existence check (exists / not found)
    - Routing paths found (list each workflow + trigger)
    - Overall AUDIT-02 verdict: PASS if at least one routing path exists, FAIL otherwise

    End the report with an overall Phase 35 summary:
    - AUDIT-01: PASS/FAIL
    - AUDIT-02: PASS/FAIL
    - Overall: PASS (both pass) or FAIL (any fail, with remediation notes)
  </action>
  <verify>
    <automated>grep -c "AUDIT-02" .planning/phases/35-confirmation-audit/35-AUDIT-REPORT.md && grep "Overall.*PASS\|Overall.*FAIL" .planning/phases/35-confirmation-audit/35-AUDIT-REPORT.md</automated>
  </verify>
  <acceptance_criteria>AUDIT-02 section documents whether gsd-validator-hub exists and lists all workflow routing paths to it. Overall phase summary present with clear verdict.</acceptance_criteria>
  <done>gsd-validator-hub routing paths documented, AUDIT-02 verdict rendered, overall phase 35 audit report complete with both AUDIT-01 and AUDIT-02 results.</done>
</task>

</tasks>

<verification>
- 35-AUDIT-REPORT.md exists and contains both AUDIT-01 and AUDIT-02 sections
- Every GSD agent (15 gsd-* agents) appears in the AUDIT-01 table
- At least one workflow routing path to gsd-validator-hub is documented
- Overall verdict is clearly stated
</verification>

<success_criteria>
1. Audit report covers all 15+ agents with tier-vs-tools comparison (AUDIT-01)
2. gsd-validator-hub routing documented with at least one path (AUDIT-02)
3. Each audit item has explicit PASS/FAIL with evidence
4. Overall phase verdict rendered
</success_criteria>

<output>
After completion, create `.planning/phases/35-confirmation-audit/35-01-SUMMARY.md`
</output>
