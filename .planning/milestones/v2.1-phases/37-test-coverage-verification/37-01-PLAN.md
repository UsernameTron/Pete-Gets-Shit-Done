---
phase: 37-test-coverage-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/build-hooks.test.cjs
  - tests/state.test.cjs
  - tests/uat.test.cjs
  - tests/phase.test.cjs
  - tests/workstream.test.cjs
  - tests/profile-pipeline.test.cjs
autonomous: true
requirements:
  - AUDIT-06

must_haves:
  truths:
    - "build-hooks.js line coverage >= 80%"
    - "state.cjs line coverage >= 80%"
    - "uat.cjs line coverage >= 80%"
    - "phase.cjs line coverage >= 80%"
    - "workstream.cjs line coverage >= 80%"
    - "profile-pipeline.cjs line coverage >= 80%"
    - "All existing tests still pass (zero regressions)"
  artifacts:
    - path: "tests/build-hooks.test.cjs"
      provides: "Additional coverage for build-hooks.js"
    - path: "tests/state.test.cjs"
      provides: "Additional coverage for state.cjs"
    - path: "tests/uat.test.cjs"
      provides: "Additional coverage for uat.cjs"
    - path: "tests/phase.test.cjs"
      provides: "Additional coverage for phase.cjs"
    - path: "tests/workstream.test.cjs"
      provides: "Additional coverage for workstream.cjs"
    - path: "tests/profile-pipeline.test.cjs"
      provides: "Additional coverage for profile-pipeline.cjs"
  key_links:
    - from: "tests/*.test.cjs"
      to: "get-shit-done/bin/lib/*.cjs"
      via: "require() with direct function calls"
      pattern: "require\\('.*lib/"
---

<objective>
Close coverage gaps in six GSD library modules to push each above 80% line coverage and lift overall coverage toward the 90% target.

Purpose: Per D-02, these six modules are quick wins (5-25 lines of new test code each) that collectively raise overall coverage significantly before tackling the large install.js file.
Output: Extended test files for build-hooks.js, state.cjs, uat.cjs, phase.cjs, workstream.cjs, and profile-pipeline.cjs -- all passing, all modules at 80%+ line coverage.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-test-coverage-verification/37-CONTEXT.md
@.planning/codebase/TESTING.md
@.c8rc.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Coverage gaps for build-hooks.js, state.cjs, and uat.cjs</name>
  <files>tests/build-hooks.test.cjs, tests/state.test.cjs, tests/uat.test.cjs</files>
  <read_first>
    - scripts/build-hooks.js (82 lines -- read entire file to identify uncovered branches)
    - tests/build-hooks.test.cjs (existing tests to extend)
    - get-shit-done/bin/lib/state.cjs (1046 lines -- focus on uncovered functions)
    - tests/state.test.cjs (existing tests to extend)
    - get-shit-done/bin/lib/uat.cjs (282 lines -- focus on uncovered functions)
    - tests/uat.test.cjs (existing tests to extend)
    - .planning/codebase/TESTING.md (test patterns and conventions)
  </read_first>
  <action>
    Run `npm run test:coverage 2>&1 | grep -E "build-hooks|state\.cjs|uat\.cjs"` to get the exact current line coverage and identify which lines are uncovered for each module.

    Per D-02, these are the three smallest gaps:
    - build-hooks.js: 79.26% -> 80%+ (need ~5 lines of coverage)
    - state.cjs: 89.48% -> 90%+ (need ~5 lines of coverage)
    - uat.cjs: 88.65% -> 90%+ (need ~10 lines of coverage)

    For each module:
    1. Run coverage to identify exact uncovered line numbers
    2. Read the source file at those line numbers to understand what code paths are untested
    3. Add tests to the existing test file that exercise those code paths
    4. Use `node:test` + `node:assert` (zero external deps per project convention)
    5. Use temp directory isolation via `tests/helpers.cjs` where filesystem operations are needed
    6. Follow the describe/test nesting pattern established in the existing test file

    Do NOT modify any source files -- only test files. Per D-03, target line/statement coverage only (no formal branch threshold). Per D-01 approach, focus on testable utility functions, skip pure I/O glue.
  </action>
  <verify>
    <automated>npm run test:coverage 2>&1 | grep -E "build-hooks|state\.cjs|uat\.cjs"</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test:coverage` output shows build-hooks.js >= 80.00% statements
    - `npm run test:coverage` output shows state.cjs >= 80.00% statements
    - `npm run test:coverage` output shows uat.cjs >= 80.00% statements
    - `npm test` exits with code 0 (all tests pass, zero failures)
    - tests/build-hooks.test.cjs contains at least one new `test(` or `it(` block beyond current count
    - tests/state.test.cjs contains at least one new `test(` or `it(` block beyond current count
    - tests/uat.test.cjs contains at least one new `test(` or `it(` block beyond current count
  </acceptance_criteria>
  <done>build-hooks.js at 80%+, state.cjs at 80%+, uat.cjs at 80%+ line coverage. All existing tests still pass.</done>
</task>

<task type="auto">
  <name>Task 2: Coverage gaps for phase.cjs, workstream.cjs, and profile-pipeline.cjs</name>
  <files>tests/phase.test.cjs, tests/workstream.test.cjs, tests/profile-pipeline.test.cjs</files>
  <read_first>
    - get-shit-done/bin/lib/phase.cjs (887 lines -- focus on uncovered functions)
    - tests/phase.test.cjs (existing tests to extend)
    - get-shit-done/bin/lib/workstream.cjs (491 lines -- focus on uncovered functions)
    - tests/workstream.test.cjs (existing tests to extend)
    - get-shit-done/bin/lib/profile-pipeline.cjs (539 lines -- focus on uncovered functions)
    - tests/profile-pipeline.test.cjs (existing tests to extend)
    - .planning/codebase/TESTING.md (test patterns and conventions)
  </read_first>
  <action>
    Run `npm run test:coverage 2>&1 | grep -E "phase\.cjs|workstream\.cjs|profile-pipeline"` to get the exact current line coverage for each module.

    Per D-02:
    - phase.cjs: 87.93% -> 90%+ (need ~15 lines of coverage)
    - workstream.cjs: 80.24% -> 85%+ (need ~20 lines of coverage)
    - profile-pipeline.cjs: 83.11% -> 88%+ (need ~25 lines of coverage)

    For each module:
    1. Run coverage to identify exact uncovered line numbers
    2. Read the source file at those line numbers
    3. Add tests to the existing test file exercising those code paths
    4. Use `node:test` + `node:assert` per project convention
    5. Use temp directory isolation via `tests/helpers.cjs` where filesystem ops needed
    6. Follow existing describe/test nesting pattern in each file

    Do NOT modify source files -- only test files. Target line/statement coverage per D-03. Focus on testable logic functions, skip interactive I/O.
  </action>
  <verify>
    <automated>npm run test:coverage 2>&1 | grep -E "phase\.cjs|workstream\.cjs|profile-pipeline"</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test:coverage` output shows phase.cjs >= 80.00% statements
    - `npm run test:coverage` output shows workstream.cjs >= 80.00% statements
    - `npm run test:coverage` output shows profile-pipeline.cjs >= 80.00% statements
    - `npm test` exits with code 0 (all tests pass, zero failures)
    - tests/phase.test.cjs contains at least one new `test(` or `it(` block beyond current count
    - tests/workstream.test.cjs contains at least one new `test(` or `it(` block beyond current count
    - tests/profile-pipeline.test.cjs contains at least one new `test(` or `it(` block beyond current count
  </acceptance_criteria>
  <done>phase.cjs at 80%+, workstream.cjs at 85%+, profile-pipeline.cjs at 88%+ line coverage. All existing tests still pass.</done>
</task>

</tasks>

<verification>
Run `npm run test:coverage` and verify:
1. All six modules above their target thresholds
2. Zero test failures across the full suite
3. Overall coverage has increased from 86.73% baseline
</verification>

<success_criteria>
- All six library modules at or above 80% line coverage
- Zero test failures (all 2178+ tests pass)
- Overall coverage increased from 86.73% baseline
- Only test files modified (no source changes)
</success_criteria>

<output>
After completion, create `.planning/phases/37-test-coverage-verification/37-01-SUMMARY.md`
</output>
