---
phase: 56-doc-drift-detector
milestone: v2.8
task: null
total_tasks: 3 plans (8 tasks across 3 waves)
status: ready_to_execute
last_updated: 2026-05-08T13:14:21.041Z
branch: chore/milestone-v2.8-init
---

<current_state>
Phase 56 (Doc Drift Detector) is fully planned, cross-AI peer-reviewed, hardened, and execution-ready. All upstream artifacts exist: CONTEXT (decisions D-01..D-18 locked), RESEARCH (concrete regex shapes for 9 metrics across 3 living docs + Nyquist Dim 8 Validation Architecture), VALIDATION (per-task verification map), three PLAN.md files (1291 + 1125 + 515 lines, 5 hardening edits applied), and REVIEWS.md (Gemini LOW + Codex MEDIUM consensus with 5-edit Recommended Replanning section that has been fully applied).

The plan checker passed on first iteration both before and after the cross-AI review revisions. There are no blockers, no human actions pending, and no uncommitted source code changes.
</current_state>

<completed_work>
This session executed `/gsd:plan-phase 56` followed by `/gsd:review --phase 56 --all` followed by `/gsd:plan-phase 56 --reviews`:

- ✓ **Research** — gsd-research-orchestrator wrote 56-RESEARCH.md (755 lines): concrete regex shapes per metric per file, TAP summary measurement validated against Node 22 (`# tests 2723`, `# suites 545` confirmed live), coverage-final.json aggregated to live values 91.34% line / 83.22% branch / 97.47% function, 14 known drift records enumerated for Wave 3 ground truth, Validation Architecture for Nyquist Dim 8, 8 pitfalls documented.
- ✓ **Validation** — Wrote 56-VALIDATION.md mirroring 55-VALIDATION.md structure: 9-row per-task verification map, 12 Wave 0 requirements, manual-only verifications, sign-off checklist.
- ✓ **Plan (initial)** — gsd-planner wrote 56-01/02/03-PLAN.md (2,830 lines initial). Wave structure: 56-01 (TDD core, no deps), 56-02 (measurement + integration, deps=[01]), 56-03 (real-repo + living-docs, deps=[02]). All 5 DOCDRIFT-01..05 covered. Roadmap "0/TBD → 0/3" updated. Committed as 2df2f51.
- ✓ **Verify (initial)** — gsd-verifier (plan scope) returned VERIFICATION PASSED on first iteration across 10 dimensions (frontmatter integrity, anti-shallow execution rules, all 18 D-decisions honored, Validation Architecture compliance, pitfall coverage, Wave 3 acceptance, out-of-scope discipline, cross-plan data contracts, Nyquist Dim 8, CLAUDE.md compliance).
- ✓ **Cross-AI review** — `/gsd:review --phase 56 --all` invoked Gemini (parallel) + Codex (parallel) + skipped Claude for runtime independence. Initial Codex invocation hung on stdin; retried with `< /dev/null` redirect (codex exec was reading from background-shell's open stdin despite prompt-as-arg). Gemini verdict: LOW risk, 2 LOW concerns. Codex verdict: MEDIUM risk, 3 MEDIUM + 2 LOW concerns. 56-REVIEWS.md written with consensus summary identifying 5-edit Recommended Replanning path.
- ✓ **Plan (revised)** — gsd-planner (mode=reviews) applied all 5 surgical hardening edits via Edit tool on existing PLAN.md files (additive, not replan-from-scratch). Net delta: +101 lines, +4 test cases (2 in 56-01, 2 in 56-02). Architecture, wave deps, and DOCDRIFT coverage preserved. Committed as 35df38b.
- ✓ **Verify (revised)** — gsd-verifier (plan scope, revision verification) returned VERIFICATION PASSED with each of the 5 edits confirmed at specific line numbers (56-02:598 for `maxBuffer`, 56-02:763-769 for `isRepoRoot`/`missingDocPolicy`, 56-01:1036-1043 for line-by-line constraint JSDoc, 56-01:313-322+818 for METRICS empty-claims guard, 56-03:24+291-297 for Wave 3 truth + WORDING-COLLISION AVOIDANCE).

Commits this session (3 tracked, on branch `chore/milestone-v2.8-init`):
- `2df2f51` docs(phase-56): plan complete — 3 plans authored across 3 TDD waves
- `35df38b` docs(phase-56): apply cross-AI review hardening edits to plans
- (handoff commit pending after this file is written)

The actual plan/research/review .md files live in gitignored `.planning/phases/56-doc-drift-detector/` per project convention. Only STATE.md, ROADMAP.md, REQUIREMENTS.md, and HANDOFF.json are tracked from `.planning/`.
</completed_work>

<remaining_work>
Phase 56 execution and ship:

1. **Wave 1 — Plan 56-01 (TDD core)**: 3 tasks. Build `tests/fixtures/doc-drift/{clean,drift,edge}/` tree, write RED unit tests in `tests/check-doc-drift.test.cjs` for the 8 pure functions and the METRICS registry, then implement `scripts/check-doc-drift.cjs` GREEN with exports + `require.main === module` guard (exits 2 if invoked directly — main lands in Wave 2). +37 test cases including the 2 new hardening tests.

2. **Wave 2 — Plan 56-02 (measurement + integration)**: 3 tasks. Add `measureCoverageFromJson` (read coverage-final.json, aggregate s/b/f), `measureTestCounts` (spawn `node --test --test-reporter=tap` with `maxBuffer: 16 * 1024 * 1024`, parse summary, handle empty-stdout catch path), filesystem `measureAgents/Commands/Skills/Hooks` functions. Wire `main(argv)` with `isRepoRoot` helper deriving `missingDocPolicy`, the 4 CLI flags (`--json`, `--root`, `--coverage-stale-secs`, `--help`), exit codes 0/1/2, and the `{status, checked, files, metrics, drift}` JSON envelope. Add `"scripts/check-doc-drift.cjs"` to `.c8rc.json` include array. +59 test cases including the 2 new hardening tests.

3. **Wave 3 — Plan 56-03 (real-repo + living-docs)**: 2 tasks. Run detector against repo root expecting exit 1 with the 14 known drift records (command_count 65→66, hook_count_execution 7→6, test_count and coverage staleness). Surgically edit CLAUDE.md, README.md, docs/DEVOPS-HANDOFF.md to reflect live values. Add detector reference to all 3 living docs using neutral phrasing per WORDING-COLLISION AVOIDANCE subsection. Re-run detector to confirm exit 0 with no spurious drift. Run `npm test` and `npm run test:coverage` to confirm full suite green and thresholds held (≥91% line / ≥83% branch overall, ≥80% per-module on the new script).

4. **Verify**: `/gsd:verify-work` for conversational UAT against the phase acceptance criteria.

5. **Ship**: `/gsd:ship` for PR creation, code review, and merge prep.

After Phase 56 ships, only Phase 57 (Backfill + CI Integration) remains to close out v2.8. Phase 57 depends on Phases 55 and 56 both being complete; 55 already shipped, so 56 is the last gating dependency.
</remaining_work>

<decisions_made>
- **Path A (apply hardening edits) over Path B (execute-as-is)**: Codex MEDIUM concerns are operational hardening, not correctness blockers — but ~20min replan cost is far less than re-debugging in CI later. Five surgical edits added without touching architecture.
- **Skip Claude CLI in cross-AI review**: Running inside Claude Code; reviewing with same runtime collapses the value of independence. Gemini + Codex provided sufficient adversarial diversity (different verdicts, different concerns surfaced).
- **Stay line-by-line in extractClaims** (vs Codex's whole-file scan suggestion): Architectural simplicity wins; sanity test added in Edit 3 confirms current docs are all single-line. JSDoc constraint + sanity test provide early-warning guardrail without re-architecting.
- **Derive missingDocPolicy from --root + isRepoRoot helper, NOT a new CLI flag**: Honors D-18 (4 CLI flags only). Path semantics alone (presence of package.json + .gitignore) are sufficient to classify fixture-mode vs production-mode.
- **Research-first path** (vs `--skip-research`): CONTEXT.md was decisions-locked but concrete regex shapes for the 9 metrics across 3 living docs were NOT enumerated. Research produced ground truth Wave 3 needs (the 14 drift records), Nyquist Dim 8 architecture, and confirmation that line-by-line is safe.
- **Parallel Gemini + Codex invocation** (vs workflow's sequential default): Different services, no shared rate limits. Saved ~5min, zero conflict risk. Codex's first run hung on stdin — fixed with `< /dev/null` redirect; document the pattern for future review invocations.
</decisions_made>

<blockers>
None. All upstream artifacts validated. No human actions pending. No tests broken on main. Branch `chore/milestone-v2.8-init` is clean.
</blockers>

<context>
The big mental model: Phase 56 ships a CI-enforced numeric drift detector that mirrors Phase 55 (link validator) shape exactly — same architecture, same TDD waves, same fixture tree pattern, same `--json` envelope, same exit codes 0/1/2. Phase 55 proves the pattern works. Phase 56 reuses it for a different concern (numeric claims vs link refs).

The 14 known drift records found during research are not a problem — they're the proof. Wave 3 expects them, displays them, fixes them surgically, and verifies the detector exits 0 after the fix. This is by design; if the detector found ZERO drift, the docs would already be perfect and we'd have nothing to validate.

The hardening edits address operational concerns rather than correctness. Codex worried about TAP buffer truncation on large suites (`maxBuffer`), false-negative drift if a doc claim wraps lines (sanity test), and silent fixture-convenience masking real issues (`missingDocPolicy`). All three are now explicit in the plans with grep-verifiable acceptance criteria.

The Gemini-vs-Codex risk-verdict divergence (LOW vs MEDIUM) is informative on its own: Gemini reviews for plan completeness; Codex reviews for production hardness. Both are valid lenses. Pete chose Path A because the hardening cost is small and the long-term debugging cost of operational issues is large.

After Phase 56 ships, Phase 57 wires both validators into `.github/workflows/test.yml` as blocking steps. After Phase 57 ships, v2.8 (Documentation Integrity) milestone closes.
</context>

<next_action>
After `/clear` to start with a fresh context window:

```
/gsd:execute-phase 56
```

The execution engine will:
1. Spawn gsd-executor on Plan 56-01 (Wave 1, no deps)
2. After 56-01 completes, spawn gsd-executor on Plan 56-02 (Wave 2, deps=[01])
3. After 56-02 completes, spawn gsd-executor on Plan 56-03 (Wave 3, deps=[02])
4. Each wave commits atomically; on completion, return to orchestrator

If anything goes sideways during execution: STOP, do not push through. The plans are detailed enough that an executor failure indicates a real issue, not a missing instruction. Re-plan if needed via `/gsd:plan-phase 56 --gaps`.

Expected runtime: Wave 1 ~10-15min (fixture authoring + RED test writing + GREEN implementation), Wave 2 ~15-20min (more I/O code + integration tests + spawnSync test harness), Wave 3 ~10min (mostly surgical doc edits + sanity runs). Total ~45-60min for execution; add ~10min for verify-work and ~5min for ship.
</next_action>
