---
phase: 57-backfill-and-ci-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/ROADMAP.md
  - .planning/milestones/v1.0-ROADMAP.md
  - .planning/milestones/v1.1-ROADMAP.md
  - .planning/milestones/v1.2-ROADMAP.md
  - .planning/milestones/v1.3-ROADMAP.md
  - .planning/milestones/v1.4-ROADMAP.md
  - .planning/milestones/v1.5-ROADMAP.md
  - .planning/milestones/v1.6-ROADMAP.md
  - .planning/milestones/v1.7-ROADMAP.md
  - .planning/milestones/v1.8-ROADMAP.md
  - .planning/milestones/v1.9-ROADMAP.md
  - .planning/milestones/v2.0-ROADMAP.md
  - .planning/milestones/v2.1-ROADMAP.md
  - .planning/milestones/v2.3-ROADMAP.md
  - .planning/milestones/v2.4-ROADMAP.md
  - .planning/milestones/v2.6-ROADMAP.md
  - .planning/milestones/v2.7-ROADMAP.md
  - .planning/milestones/archived-phases/02-coverage-audit/02-VERIFICATION.md
  - docs/README-technical.md
  - docs/README.md
  - README.md
  - docs/AGENTS.md
  - docs/ARCHITECTURE.md
  - docs/CONFIGURATION.md
autonomous: true
requirements:
  - DOCREF-01
  - DOCREF-02
must_haves:
  truths:
    - "node scripts/validate-doc-links.cjs --json | node -e \"let d=''; process.stdin.on('data',c=>d+=c); process.stdin.on('end',()=>{const o=JSON.parse(d); const exempt = ['tests/fixtures/doc-links/','.claude/skills/SKILL.md','.claude/skills/dream-memory-consolidation/SKILL.md','.planning/REQUIREMENTS.md','.planning/milestones/v2.7-phases/54-automated-uat-runner/54-03-PLAN.md']; const remaining = o.broken.filter(r => !exempt.some(e => r.file===e || r.file.startsWith(e))); if (remaining.length !== 0) { console.error('Remaining broken refs after backfill:', JSON.stringify(remaining,null,2)); process.exit(1); }})\" exits 0 (every broken ref outside the planned exempt list is repaired)"
    - "node scripts/validate-doc-links.cjs --json | node -e \"let d=''; process.stdin.on('data',c=>d+=c); process.stdin.on('end',()=>{const o=JSON.parse(d); const evidence = o.broken.filter(r => r.ref.includes('full-audit-2026-04-11.md') || r.ref.includes('codebase/STRUCTURE.md')); if (evidence.length !== 0) { console.error('DOCREF-01/02 evidence failed: refs to relocated docs still flagged'); process.exit(1); }})\" exits 0 (DOCREF-01/02 closure with validator output as evidence per D-03)"
    - "git grep -nE '\\(milestones/v[0-9]' .planning/milestones/v*-ROADMAP.md returns no matches (sed sweep complete)"
    - "git grep -nE '\\]:\\s+milestones/v[0-9]' .planning/milestones/v*-ROADMAP.md returns no matches (reference-style sweep complete)"
    - "npm test exits 0"
    - "test -f .planning/phases/57-backfill-and-ci-integration/57-01-SUMMARY.md exits 0"
  artifacts:
    - path: ".planning/milestones/v2.7-ROADMAP.md"
      provides: "Sample of sed-swept archived ROADMAP — links use sibling-relative paths (e.g., (v2.7-phases/...) not (milestones/v2.7-phases/...))"
    - path: "docs/README-technical.md"
      provides: "Real broken refs in technical README repaired or removed (lines 18, 32, 269, 608, 742, 975 per CONTEXT D-04)"
    - path: "docs/README.md"
      provides: "i18n placeholder links removed or repaired (line 5 per CONTEXT D-04)"
    - path: "README.md"
      provides: "Anchor refs at line 27 (#how-it-works, #user-guide) repaired or removed"
    - path: ".planning/phases/57-backfill-and-ci-integration/57-01-SUMMARY.md"
      provides: "Backfill evidence: validator before/after JSON output, list of exact repairs by file, DOCREF-01/02 closure evidence"
  key_links:
    - from: ".planning/milestones/v2.7-ROADMAP.md inline link"
      to: ".planning/milestones/v2.7-phases/<phase>/...md"
      via: "sibling-relative path after sed sweep removes 'milestones/' prefix"
      pattern: "\\(v[0-9]+\\.[0-9]+-phases/"
    - from: "57-01-SUMMARY.md DOCREF closure section"
      to: "validator JSON output"
      via: "embedded JSON snippet showing zero broken refs to full-audit-2026-04-11.md or .planning/codebase/STRUCTURE.md"
      pattern: "\"broken\":\\s*\\[\\]"
---

<objective>
Drive `validate-doc-links.cjs` from 109 broken refs to a clean state against the live repo (excluding only the five paths that will be exempted via `--exclude` in Wave 2). Repair the 83 archived-roadmap cross-refs via mechanical sed-sweep (D-02), the ~10 real broken file refs case-by-case (D-04), and the 5 remaining anchor issues case-by-case (D-05). Close DOCREF-01 and DOCREF-02 by recording validator output as evidence — both named docs already exist on disk and the validator already shows zero broken refs to them, so the closure is a documentation step, not a relocation step (D-03).

Purpose: Lock in the precondition for Wave 3's strict-blocking CI gate. The validator must observe a clean repo on first invocation in CI; otherwise the day-1 strict-blocking gate trips on the next PR. This wave does the cleanup so Wave 3 can ship `continue-on-error: false` from the start (D-16).

Output: A repo where `node scripts/validate-doc-links.cjs --json` returns broken refs ONLY from inside the five planned exempt paths (Wave 2 will add `--exclude` to filter those). Every other broken ref is repaired or removed. The 57-01-SUMMARY.md captures before/after counts, per-file repair list, and DOCREF-01/02 closure evidence.

Phase boundary: this plan does NOT add the `--exclude` flag (Wave 2) and does NOT edit `.github/workflows/test.yml` (Wave 3).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/57-backfill-and-ci-integration/57-CONTEXT.md
@.planning/phases/55-internal-link-validator/55-03-PLAN.md
@.planning/phases/56-doc-drift-detector/56-03-PLAN.md
@scripts/validate-doc-links.cjs
@docs/README-technical.md
@docs/README.md
@README.md
@docs/AGENTS.md
@docs/ARCHITECTURE.md
@docs/CONFIGURATION.md

<groundtruth>
Validator output as of 2026-05-08 (109 total broken refs):

By group (matches CONTEXT D-01..D-05 strategy):
- 83 archived-roadmap cross-refs across `.planning/milestones/v*-ROADMAP.md` — mechanical sed sweep (D-02)
- 16 real file/anchor broken refs needing case-by-case repair (D-04, D-05)
- 6 inside `tests/fixtures/doc-links/` — exempt via Wave 2 `--exclude` (NOT touched here)
- 2 inside `.claude/skills/` — exempt via Wave 2 `--exclude` (NOT touched here)
- 1 inside `.planning/REQUIREMENTS.md` line 14 — exempt via Wave 2 `--exclude` (see scope_note)
- 1 inside `.planning/milestones/v2.7-phases/54-automated-uat-runner/54-03-PLAN.md` line 278 — exempt via Wave 2 `--exclude` (see scope_note)

Real refs to repair (16 items, exact file:line:ref triples):
- `.planning/milestones/archived-phases/02-coverage-audit/02-VERIFICATION.md:43`  `coverage-baseline.md`  (file not found)
- `README.md:27`  `#how-it-works`  (anchor missing in target — same-file)
- `README.md:27`  `#user-guide`  (anchor missing in target — same-file)
- `docs/AGENTS.md:33`  `#gsd-research-orchestrator`  (anchor missing in target — same-file)
- `docs/AGENTS.md:39`  `#gsd-research-orchestrator`  (anchor missing in target — same-file)
- `docs/ARCHITECTURE.md:17`  `#cli-tools-layer`  (anchor missing — same-file)
- `docs/CONFIGURATION.md:84`  `#adaptive-workflow`  (anchor missing — same-file)
- `docs/README-technical.md:18`  `assets/terminal.svg`  (file not found)
- `docs/README-technical.md:32`  `docs/USER-GUIDE.md`  (file not found)
- `docs/README-technical.md:269`  `docs/workflow-discuss-mode.md`  (file not found)
- `docs/README-technical.md:608`  `docs/governance-customization.md`  (file not found)
- `docs/README-technical.md:742`  `docs/USER-GUIDE.md#configuration-reference`  (file not found)
- `docs/README-technical.md:975`  `LICENSE`  (file not found)
- `docs/README.md:5`  `pt-BR/README.md`  (file not found)
- `docs/README.md:5`  `ja-JP/README.md`  (file not found)
- `docs/README.md:5`  `zh-CN/README.md`  (file not found)
</groundtruth>

<scope_note>
CONTEXT.md D-08 said "wrap inline-backtick the regex example in REQUIREMENTS.md line 14" was the planner's preferred path. **Empirical finding:** the example IS already wrapped in single backticks (`[text](path/to/file.md)`), but `extractLinks()` does NOT skip inline-code spans — only fenced code blocks. The validator picks up the path through the backtick wrap. Same problem applies to `54-03-PLAN.md:278` (a regex literal inside a JS string inside a markdown bullet — backtick wrap of the whole bullet would span multiple visual elements, fragile).

**Decision:** Both files go on the Wave 2 `--exclude` list (planner judgment per D-08). This wave does NOT touch REQUIREMENTS.md or 54-03-PLAN.md to wrap them in code fences — that risks corrupting working documentation for marginal gain over the `--exclude` mechanism.

The Wave 2 `--exclude` list is therefore exactly five paths:
1. `tests/fixtures/doc-links/**`
2. `.claude/skills/SKILL.md`
3. `.claude/skills/dream-memory-consolidation/SKILL.md`
4. `.planning/REQUIREMENTS.md`
5. `.planning/milestones/v2.7-phases/54-automated-uat-runner/54-03-PLAN.md`
</scope_note>

</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 0: Reconcile ROADMAP Phase 57 Success Criterion #1 with the validator-clean closure strategy</name>
  <files>.planning/ROADMAP.md</files>

  <read_first>
- .planning/ROADMAP.md (current Phase 57 entry — confirm Success Criterion #1 still reads as the original "grep returns zero references" wording before applying the edit; if it already reads as the new wording from a prior revision pass, skip the Edit and just confirm)
- .planning/phases/57-backfill-and-ci-integration/57-CONTEXT.md (D-03 closure-via-clarification rationale; D-08 in-repo descriptive mentions are intentional)
  </read_first>

  <action>
The ROADMAP's original Phase 57 Success Criterion #1 demanded a literal grep returning zero references to the two named relocated docs across the entire repo. The actual closure strategy (CONTEXT D-03) keeps descriptive mentions in REQUIREMENTS.md (which AUTHORS the requirement), ROADMAP.md (which restates it), tasks/todo.md (which tracks it), and 2 template/workflow files inside `get-shit-done/` (which describe the validator's behavior). Those mentions are content ABOUT the targets, not broken hyperlinks TO them. The validator-clean evidence is the operationally meaningful gate.

Apply this Edit to `.planning/ROADMAP.md` Phase 57 Success Criterion list:

old_string:
```
  1. A grep across the entire repo finds zero references to `docs/health-reports/full-audit-2026-04-11.md` or `.planning/codebase/STRUCTURE.md` — all have been repaired or removed
```

new_string:
```
  1. The validator (`scripts/validate-doc-links.cjs`) reports zero broken link refs to `docs/health-reports/full-audit-2026-04-11.md` or `.planning/codebase/STRUCTURE.md`, AND every textual mention of these paths in the repo is an intentional description of the requirement (e.g., REQUIREMENTS.md DOCREF-01/02 description) rather than a broken hyperlink.
```

This is a single-line edit. Do NOT change other Success Criteria, do NOT renumber, do NOT touch the "Plans" list immediately below.

After the edit, verify with grep:

```bash
grep -F "validator (\`scripts/validate-doc-links.cjs\`) reports zero broken link refs" .planning/ROADMAP.md
# Expect: exactly 1 match
```

Commit:

```
docs(57-01): reconcile ROADMAP Phase 57 Success Criterion #1 with validator-clean closure

The original wording demanded a literal grep returning zero references to the
two relocated docs across the entire repo. The DOCREF-01/02 closure strategy
(CONTEXT D-03) is validator-clean evidence: the targets exist on disk; the
validator reports zero broken link refs to them; remaining textual mentions
are intentional descriptions of the requirement (REQUIREMENTS.md, ROADMAP.md,
tasks/todo.md, plus 2 template/workflow files). New wording captures both
parts: validator-clean AND remaining mentions are descriptive, not broken refs.
```
  </action>

  <verify>
    <automated>grep -F "validator (\`scripts/validate-doc-links.cjs\`) reports zero broken link refs" .planning/ROADMAP.md</automated>
  </verify>

  <acceptance_criteria>
- `grep -F "validator (\`scripts/validate-doc-links.cjs\`) reports zero broken link refs" .planning/ROADMAP.md` returns exactly 1 match
- `grep -F "A grep across the entire repo finds zero references to" .planning/ROADMAP.md` returns no match (old wording removed)
- `git diff --name-only HEAD~1 HEAD 2>/dev/null` lists exactly `.planning/ROADMAP.md` (and no other files in the Task 0 commit)
- `git log --oneline -1` subject begins with `docs(57-01):` and mentions reconcile / Success Criterion
- `npm test` exits 0 (ROADMAP edit cannot break tests)
  </acceptance_criteria>

  <done>ROADMAP.md Phase 57 Success Criterion #1 reads the validator-clean wording. Single-file commit on the branch covering only the ROADMAP edit.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 1: Capture pre-state evidence and run sed-sweep on archived ROADMAPs (DOCREF-01/02 closure baseline)</name>
  <files>.planning/milestones/v1.0-ROADMAP.md, .planning/milestones/v1.1-ROADMAP.md, .planning/milestones/v1.2-ROADMAP.md, .planning/milestones/v1.3-ROADMAP.md, .planning/milestones/v1.4-ROADMAP.md, .planning/milestones/v1.5-ROADMAP.md, .planning/milestones/v1.6-ROADMAP.md, .planning/milestones/v1.7-ROADMAP.md, .planning/milestones/v1.8-ROADMAP.md, .planning/milestones/v1.9-ROADMAP.md, .planning/milestones/v2.0-ROADMAP.md, .planning/milestones/v2.1-ROADMAP.md, .planning/milestones/v2.3-ROADMAP.md, .planning/milestones/v2.4-ROADMAP.md, .planning/milestones/v2.6-ROADMAP.md, .planning/milestones/v2.7-ROADMAP.md</files>

  <read_first>
- scripts/validate-doc-links.cjs (final state from Phase 55 — confirms what is being executed against)
- .planning/phases/57-backfill-and-ci-integration/57-CONTEXT.md (D-01..D-06 backfill strategy, D-02 sed command, D-03 DOCREF closure rationale)
- .planning/milestones/v2.7-ROADMAP.md (sample archived roadmap to inspect link patterns BEFORE running sed)
- .planning/milestones/v2.6-ROADMAP.md (another sample — confirm both inline `(milestones/v...)` and reference-style `]: milestones/v...` patterns appear)
- .planning/REQUIREMENTS.md (DOCREF-01, DOCREF-02 verbatim; confirms intent of "no broken refs to those targets")
  </read_first>

  <action>
**Step 1: Capture pre-state evidence.** Save the current validator output to a temp file using `mktemp` (sibling Pass 2 Codex MEDIUM pattern from 55-03-PLAN.md and 56-03-PLAN.md):

```bash
PRE_JSON="$(mktemp -t validate-doc-links-pre.XXXXXX)"
PRE_EXIT="${PRE_JSON}.exit"
node scripts/validate-doc-links.cjs --json > "$PRE_JSON" 2>&1
echo "$?" > "$PRE_EXIT"
echo "Pre-state JSON: $PRE_JSON"
echo "Pre-state exit: $(cat $PRE_EXIT)"

# Verify it parses cleanly and confirm count
node -e "const o=JSON.parse(require('fs').readFileSync('$PRE_JSON','utf8')); console.log('pre-state broken count:', o.broken.length); console.log('pre-state status:', o.status);"
```

Expected: `status: broken`, `broken count: 109` (or close — it may have shifted by ±2 if other docs changed since 2026-05-08).

**Step 2: Confirm DOCREF-01 / DOCREF-02 evidence.** Run a targeted query against the JSON to prove the named relocated docs are NOT in the broken list:

```bash
node -e "
const o = JSON.parse(require('fs').readFileSync('$PRE_JSON','utf8'));
const docref = o.broken.filter(r =>
  r.ref.includes('full-audit-2026-04-11.md') ||
  r.ref.includes('codebase/STRUCTURE.md')
);
console.log('DOCREF-01/02 broken refs:', docref.length);
if (docref.length !== 0) {
  console.error('UNEXPECTED — refs to relocated docs are still broken:');
  console.error(JSON.stringify(docref, null, 2));
  process.exit(1);
}
console.log('DOCREF-01/02 closure evidence: zero broken refs to relocated docs');
"
```

Save this output for the SUMMARY. If `docref.length` is greater than 0, STOP and report — the CONTEXT.md D-03 closure-via-clarification strategy assumed zero broken refs to those targets, and a non-zero count means the closure rationale needs revisiting.

**Step 3: Run the sed sweep on archived ROADMAPs.** This is the mechanical 83-record fix per D-02.

**Portability note (consensus review concern — Gemini LOW + Codex MEDIUM):** macOS/BSD sed (`sed -i ''`) and GNU sed (`sed -i`) have incompatible `-i` argument handling. Pete's machine is macOS, so the BSD form is the primary path. If this plan is ever executed on Linux/CI, swap `-i ''` for `-i` (no quoted empty arg). Choose the right form for the OS at execution time:

```bash
# Detect platform and pick the right sed -i form.
# macOS/BSD requires an empty backup-extension arg; GNU sed forbids it.
if [[ "$(uname)" == "Darwin" ]]; then
  SED_INPLACE=(sed -i '')
else
  SED_INPLACE=(sed -i)
fi

# Cover both inline (path) and reference-style ]: path link forms.
# The sweep removes the 'milestones/' prefix because archived ROADMAPs are
# inside `.planning/milestones/`, so paths starting with `milestones/` no
# longer resolve from there.
"${SED_INPLACE[@]}" 's|(milestones/v|(v|g; s|]: milestones/v|]: v|g' \
  .planning/milestones/v1.0-ROADMAP.md \
  .planning/milestones/v1.1-ROADMAP.md \
  .planning/milestones/v1.2-ROADMAP.md \
  .planning/milestones/v1.3-ROADMAP.md \
  .planning/milestones/v1.4-ROADMAP.md \
  .planning/milestones/v1.5-ROADMAP.md \
  .planning/milestones/v1.6-ROADMAP.md \
  .planning/milestones/v1.7-ROADMAP.md \
  .planning/milestones/v1.8-ROADMAP.md \
  .planning/milestones/v1.9-ROADMAP.md \
  .planning/milestones/v2.0-ROADMAP.md \
  .planning/milestones/v2.1-ROADMAP.md \
  .planning/milestones/v2.3-ROADMAP.md \
  .planning/milestones/v2.4-ROADMAP.md \
  .planning/milestones/v2.6-ROADMAP.md \
  .planning/milestones/v2.7-ROADMAP.md
```

If the executor is on a known macOS-only path (Pete's machine), the simpler explicit form is acceptable:

```bash
# macOS/BSD-only — DO NOT run on Linux without swapping to `sed -i`
sed -i '' 's|(milestones/v|(v|g; s|]: milestones/v|]: v|g' .planning/milestones/v*-ROADMAP.md
```

GNU/Linux equivalent (no quoted empty arg):

```bash
sed -i 's|(milestones/v|(v|g; s|]: milestones/v|]: v|g' .planning/milestones/v*-ROADMAP.md
```

(Note: `v2.2` and `v2.5` archives do not exist on disk — milestone archive list above is exhaustive per `find .planning/milestones -name 'v*-ROADMAP.md'`.)

**Step 4: Confirm sweep delta with git diff.**

```bash
# Confirm only path-prefix changes — no semantic content changes
git diff --stat .planning/milestones/v*-ROADMAP.md
git diff .planning/milestones/v2.7-ROADMAP.md | head -40   # spot-check one file's diff
```

Expected: every changed line is the path-prefix change `(milestones/v` → `(v` or `]: milestones/v` → `]: v`. Any other change is unintended — STOP and inspect.

**Step 5: Confirm sweep coverage with grep.** No archived ROADMAP should contain a `milestones/v` path prefix anymore:

```bash
git grep -nE '\(milestones/v[0-9]' .planning/milestones/v*-ROADMAP.md && echo "FAIL — inline refs remain" || echo "OK — inline refs swept"
git grep -nE '\]:\s+milestones/v[0-9]' .planning/milestones/v*-ROADMAP.md && echo "FAIL — reference-style refs remain" || echo "OK — reference-style refs swept"
```

Both grep commands must return nonzero (no matches found).

**Step 6: Re-run validator and confirm count drop.**

```bash
MID_JSON="$(mktemp -t validate-doc-links-mid.XXXXXX)"
node scripts/validate-doc-links.cjs --json > "$MID_JSON" 2>&1
node -e "
const pre = JSON.parse(require('fs').readFileSync('$PRE_JSON','utf8'));
const mid = JSON.parse(require('fs').readFileSync('$MID_JSON','utf8'));
const dropped = pre.broken.length - mid.broken.length;
console.log('pre:', pre.broken.length, 'mid:', mid.broken.length, 'dropped:', dropped);
if (dropped < 80) {
  console.error('Sed sweep dropped fewer than 80 broken refs (expected ~83).');
  process.exit(1);
}
"
```

Save `$MID_JSON` path for SUMMARY (it is consumed in Task 3's SUMMARY authoring).

**Step 7: Commit the sed-sweep result as its own commit.** Atomic — the diff is purely path-prefix changes across 16 files.

```
fix(57-01): mechanically repair archived ROADMAP cross-refs (sed sweep)

Sweeps `(milestones/v` -> `(v` and `]: milestones/v` -> `]: v` across all
.planning/milestones/v*-ROADMAP.md files. Archived ROADMAPs were checked
into .planning/milestones/ but their internal links still used the original
absolute path prefix; sibling-relative paths resolve correctly after sweep.

Drops ~83 broken refs from validator output. Real broken refs in
docs/README-technical.md, docs/README.md, README.md, docs/AGENTS.md,
docs/ARCHITECTURE.md, docs/CONFIGURATION.md, and the archived 02-coverage-audit
will be repaired in Task 2 of this plan.

Closes (DOCREF-01/02 baseline): zero broken refs to
docs/health-reports/full-audit-2026-04-11.md or .planning/codebase/STRUCTURE.md
in the validator's JSON output, confirming both relocated targets exist on disk
and Phase 57 closes their requirements via clarification rather than relocation
(per Phase 57 CONTEXT.md D-03).
```
  </action>

  <verify>
    <automated>git grep -nE '\(milestones/v[0-9]' .planning/milestones/v*-ROADMAP.md; INLINE=$?; git grep -nE '\]:\s+milestones/v[0-9]' .planning/milestones/v*-ROADMAP.md; REF=$?; if [ "$INLINE" -eq 0 ] || [ "$REF" -eq 0 ]; then echo "FAIL — milestones/v prefix remains"; exit 1; fi; echo "OK — sed sweep complete"</automated>
  </verify>

  <acceptance_criteria>
- `git grep -nE '\(milestones/v[0-9]' .planning/milestones/v*-ROADMAP.md` returns no matches (exits 1)
- `git grep -nE '\]:\s+milestones/v[0-9]' .planning/milestones/v*-ROADMAP.md` returns no matches (exits 1)
- `node scripts/validate-doc-links.cjs --json | node -e "let d=''; process.stdin.on('data',c=>d+=c); process.stdin.on('end',()=>{const o=JSON.parse(d); if (o.broken.length > 30) process.exit(1);})"` exits 0 (after the sweep, broken count must be at most 30 — 16 real refs + up to ~10 in exempt paths; expected count is around 26)
- `node scripts/validate-doc-links.cjs --json | node -e "let d=''; process.stdin.on('data',c=>d+=c); process.stdin.on('end',()=>{const o=JSON.parse(d); const docref = o.broken.filter(r => r.ref.includes('full-audit-2026-04-11.md') || r.ref.includes('codebase/STRUCTURE.md')); if (docref.length !== 0) process.exit(1);})"` exits 0 (DOCREF-01/02 closure: still zero broken refs to relocated docs after sweep)
- `git diff --name-only HEAD~1 HEAD 2>/dev/null` lists exactly the 16 archived ROADMAP files (and no other files in this commit)
- `git log --oneline -1` subject line begins with `fix(57-01):` and mentions `sed sweep`
- `npm test` exits 0 (no tests broken by the sweep — ROADMAPs are not test inputs)
  </acceptance_criteria>

  <done>Pre-state JSON captured to a unique mktemp file with separate `.exit` file. DOCREF-01/02 closure evidence captured (validator JSON shows zero broken refs to either named target). Sed sweep applied to all 16 archived `v*-ROADMAP.md` files removing the `milestones/` prefix from both inline and reference-style links. Validator broken count dropped from 109 to roughly 26 (the 16 real refs plus ~10 inside-exempt-path refs). Single commit on the branch covering only the sed-sweep diff.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Repair the 16 real broken refs case-by-case (file refs + anchor refs)</name>
  <files>docs/README-technical.md, docs/README.md, README.md, docs/AGENTS.md, docs/ARCHITECTURE.md, docs/CONFIGURATION.md, .planning/milestones/archived-phases/02-coverage-audit/02-VERIFICATION.md</files>

  <read_first>
- docs/README-technical.md (read lines 18, 32, 269, 608, 742, 975 to confirm exact context — line numbers may have shifted; use grep for the ref strings to locate)
- docs/README.md (read line 5 area to confirm i18n placeholder layout)
- README.md (read line 27 area to inspect the same-file anchors `#how-it-works` and `#user-guide`)
- docs/AGENTS.md (read lines 33 and 39 — both reference `#gsd-research-orchestrator`; confirm whether the heading exists or needs to be added)
- docs/ARCHITECTURE.md (read line 17 — `#cli-tools-layer` anchor)
- docs/CONFIGURATION.md (read line 84 — `#adaptive-workflow` anchor)
- .planning/milestones/archived-phases/02-coverage-audit/02-VERIFICATION.md (read line 43 — `coverage-baseline.md`)
- .planning/phases/57-backfill-and-ci-integration/57-CONTEXT.md (D-04 lists exact items, D-05 anchor strategy: retarget to existing heading where intent is clear; drop the `#anchor` portion when no close match exists)
  </read_first>

  <action>
**CONTEXT D-04/D-05 reconciliation:** CONTEXT.md estimated "~10 file-not-found and 9 anchor issues" based on earlier validator output. Current validator state (running `node scripts/validate-doc-links.cjs --json` after Phase 56 ship) shows:
- 10 file-not-found refs (6 in docs/README-technical.md + 3 i18n placeholders in docs/README.md + 1 archived ref in 02-VERIFICATION.md)
- 6 anchor refs (2 in docs/README.md + 2 in AGENTS.md duplicate + 1 in ARCHITECTURE.md + 1 in CONFIGURATION.md)
- 16 real refs total (the 3 missing anchors from CONTEXT D-05's count were inside `tests/fixtures/doc-links/` which Wave 2 exempts via `--exclude` rather than repairs)

The 16-item enumeration below is authoritative; CONTEXT D-04/D-05 numbers were estimates.

For each broken ref below, the executor performs ONE of: (a) repair the ref to point at the correct existing target, (b) remove the broken link entirely (preserving the visible link text as plain text), (c) drop only the `#anchor` portion if the target file exists but the anchor does not. Per D-05, dropping the anchor is preferred when no close-match heading exists; retargeting is preferred when intent is clear.

**MUST verify before each repair:** open the file, find the exact context, decide the right action, apply the smallest possible Edit. Do NOT rewrite paragraphs. Do NOT touch unrelated content.

### File-not-found refs (10 items)

**1. `docs/README-technical.md:18`  `assets/terminal.svg`**
   - Investigate: `ls docs/assets/ 2>/dev/null; ls assets/ 2>/dev/null` — locate any terminal SVG.
   - If no SVG exists anywhere: remove the image link entirely (replace `![alt](assets/terminal.svg)` with empty string or with the alt text as plain text).
   - If an SVG exists at a different path: repair the ref to that path.
   - Document the chosen action in the SUMMARY.

**2. `docs/README-technical.md:32`  `docs/USER-GUIDE.md`**
   - Investigate: `ls docs/USER-GUIDE.md 2>/dev/null; find docs -name '*USER*GUIDE*' -o -name '*user*guide*'`.
   - If a user-guide file exists under a different name: repair the ref.
   - If no user guide exists: remove the link, preserving the visible link text as plain text.

**3. `docs/README-technical.md:269`  `docs/workflow-discuss-mode.md`**
   - Investigate: `find docs -name '*discuss*'`.
   - If a discuss-mode doc exists: repair.
   - If not: remove the link, preserve text.

**4. `docs/README-technical.md:608`  `docs/governance-customization.md`**
   - Investigate: `find docs -name '*governance*'`.
   - If a governance doc exists: repair.
   - If not: remove the link, preserve text.

**5. `docs/README-technical.md:742`  `docs/USER-GUIDE.md#configuration-reference`**
   - Same target as item 2 — apply the same decision plus drop the `#configuration-reference` anchor.

**6. `docs/README-technical.md:975`  `LICENSE`**
   - Investigate: `ls LICENSE LICENSE.md LICENSE.txt 2>/dev/null`.
   - If `LICENSE` exists at repo root: confirm the relative path from `docs/README-technical.md` (probably `../LICENSE`) and repair.
   - If no LICENSE file: remove link, preserve text.

**7-9. `docs/README.md:5`  `pt-BR/README.md`, `ja-JP/README.md`, `zh-CN/README.md`** (three i18n placeholders)
   - Per D-04 and CONTEXT specifics: these are i18n placeholders pointing to translations that don't exist.
   - **Action:** remove the three i18n links from line 5. Preserve any line-5 prose that is not the broken links themselves.
   - The CONTEXT explicitly says "if translations are planned, they belong in a separate i18n phase, not Phase 57."

**10. `.planning/milestones/archived-phases/02-coverage-audit/02-VERIFICATION.md:43`  `coverage-baseline.md`**
   - This is an archived phase's internal ref. Investigate: `find .planning/milestones/archived-phases/02-coverage-audit -name '*baseline*'`.
   - If the baseline file exists in a sibling location: repair the ref.
   - If not (most likely — archived phases are read-only artifacts): remove the link, preserve the surrounding sentence's meaning. As an archived doc this carries no live consequence; the change is purely to satisfy the validator.

### Anchor refs (6 items)

**11-12. `README.md:27`  `#how-it-works`, `#user-guide`** (same-file anchors)
   - These are anchors into the same README.md. Read README.md headings and check whether `## How It Works` or `## User Guide` (or close variants) exist.
   - If a close-match heading exists (e.g., `## How GSD Works`): retarget the anchor to the matching slug (per `toGfmSlug`).
   - If no close match: drop the `#anchor` portion. The link becomes a same-file URL fragment that resolves to the document head, which is harmless and silent. Alternative: remove the link entirely. Pick whichever preserves the surrounding sentence's grammar.

**13-14. `docs/AGENTS.md:33` and `:39`  `#gsd-research-orchestrator`** (same-file anchor, occurs twice)
   - Read AGENTS.md headings for any `gsd-research-orchestrator` mention. If a heading like `### gsd-research-orchestrator` exists, the slug should already be `gsd-research-orchestrator` per `toGfmSlug`. Investigate why the validator is missing it.
   - If the heading exists but a typo prevents matching: fix the typo on the heading side (single character) — but only if the heading text itself is wrong; do NOT rewrite its case or punctuation otherwise.
   - If no such heading exists: drop the `#anchor` portion at lines 33 and 39, leaving a plain link to the file root.
   - These are TWO separate edits to the same file (different lines).

**15. `docs/ARCHITECTURE.md:17`  `#cli-tools-layer`**
   - Same approach: check for a `## CLI Tools Layer` or close variant. Retarget if found, drop anchor if not.

**16. `docs/CONFIGURATION.md:84`  `#adaptive-workflow`**
   - Same approach: check for `## Adaptive Workflow` or close variant. Retarget or drop.

### Verification protocol after each edit

After EACH edit, run this targeted re-check to confirm the specific ref is gone from validator output:

```bash
node scripts/validate-doc-links.cjs --json | node -e "
let d='';
process.stdin.on('data',c=>d+=c);
process.stdin.on('end',()=>{
  const o = JSON.parse(d);
  const target = process.argv[1];   // file:line:ref triple as 'file.md:LINE:ref'
  const [f,l,r] = target.split(':');
  const hits = o.broken.filter(b => b.file === f && b.line === parseInt(l,10) && b.ref === r);
  if (hits.length) { console.error('STILL BROKEN:', target); process.exit(1); }
  console.log('REPAIRED:', target);
});
" "$REF_TRIPLE"
```

(The `$REF_TRIPLE` placeholder is the file:line:ref text from the table above. Line numbers may shift after upstream edits — re-fetch by file+ref after each edit if needed.)

### Final consolidation

After all 16 repairs:

```bash
# Full re-run; broken count should now be 6 (tests/fixtures/doc-links) + 2 (.claude/skills) + 1 (REQUIREMENTS.md) + 1 (54-03-PLAN.md) = 10 — all inside the planned exempt list
node scripts/validate-doc-links.cjs --json | node -e "
let d='';
process.stdin.on('data',c=>d+=c);
process.stdin.on('end',()=>{
  const o = JSON.parse(d);
  const exempt = [
    'tests/fixtures/doc-links/',
    '.claude/skills/SKILL.md',
    '.claude/skills/dream-memory-consolidation/SKILL.md',
    '.planning/REQUIREMENTS.md',
    '.planning/milestones/v2.7-phases/54-automated-uat-runner/54-03-PLAN.md'
  ];
  const remaining = o.broken.filter(r => !exempt.some(e => r.file === e || r.file.startsWith(e)));
  if (remaining.length !== 0) {
    console.error('Remaining broken refs OUTSIDE exempt list:');
    console.error(JSON.stringify(remaining, null, 2));
    process.exit(1);
  }
  console.log('CLEAN OUTSIDE EXEMPT LIST. Broken in exempt:', o.broken.length);
});
"
```

Then run the suite to confirm nothing regressed:

```bash
npm test
```

Commit message:

```
fix(57-01): repair real broken refs across docs and READMEs

Repairs 16 real broken refs case-by-case per CONTEXT D-04 and D-05:
- 10 file-not-found refs in docs/README-technical.md, docs/README.md,
  archived 02-coverage-audit
- 6 anchor refs in README.md, docs/AGENTS.md, docs/ARCHITECTURE.md,
  docs/CONFIGURATION.md (retargeted to existing headings or anchor dropped)

After repair, all remaining broken refs are inside paths that Wave 2 will
exempt via `--exclude`: tests/fixtures/doc-links/, .claude/skills/SKILL.md,
.claude/skills/dream-memory-consolidation/SKILL.md, .planning/REQUIREMENTS.md,
.planning/milestones/v2.7-phases/54-automated-uat-runner/54-03-PLAN.md.

Closes: DOCREF-01 / DOCREF-02 acceptance criteria — comprehensive cross-ref
backfill complete; validator-clean repo against the planned exempt list.
```
  </action>

  <verify>
    <automated>node scripts/validate-doc-links.cjs --json | node -e "let d=''; process.stdin.on('data',c=>d+=c); process.stdin.on('end',()=>{const o=JSON.parse(d); const exempt = ['tests/fixtures/doc-links/','.claude/skills/SKILL.md','.claude/skills/dream-memory-consolidation/SKILL.md','.planning/REQUIREMENTS.md','.planning/milestones/v2.7-phases/54-automated-uat-runner/54-03-PLAN.md']; const remaining = o.broken.filter(r => !exempt.some(e => r.file===e || r.file.startsWith(e))); if (remaining.length !== 0) { console.error(JSON.stringify(remaining,null,2)); process.exit(1); }})" &amp;&amp; npm test</automated>
  </verify>

  <acceptance_criteria>
- `node scripts/validate-doc-links.cjs --json | node -e "let d=''; process.stdin.on('data',c=>d+=c); process.stdin.on('end',()=>{const o=JSON.parse(d); const exempt=['tests/fixtures/doc-links/','.claude/skills/SKILL.md','.claude/skills/dream-memory-consolidation/SKILL.md','.planning/REQUIREMENTS.md','.planning/milestones/v2.7-phases/54-automated-uat-runner/54-03-PLAN.md']; const remaining=o.broken.filter(r => !exempt.some(e => r.file===e || r.file.startsWith(e))); if (remaining.length !== 0) process.exit(1);})"` exits 0 (every broken ref outside the planned exempt list has been repaired)
- `node scripts/validate-doc-links.cjs --json | node -e "let d=''; process.stdin.on('data',c=>d+=c); process.stdin.on('end',()=>{const o=JSON.parse(d); console.log('total broken in exempt paths:', o.broken.length); if (o.broken.length > 12) process.exit(1);})"` exits 0 (broken refs only in exempt paths, count at most 12)
- `grep -F 'pt-BR/README.md' docs/README.md` returns no match (i18n placeholders removed)
- `grep -F 'ja-JP/README.md' docs/README.md` returns no match
- `grep -F 'zh-CN/README.md' docs/README.md` returns no match
- `grep -F 'docs/USER-GUIDE.md' docs/README-technical.md` returns no match (broken USER-GUIDE refs at lines 32 and 742 removed)
- `grep -F 'docs/workflow-discuss-mode.md' docs/README-technical.md` returns no match
- `grep -F 'docs/governance-customization.md' docs/README-technical.md` returns no match
- `grep -F 'assets/terminal.svg' docs/README-technical.md` returns no match
- `grep -F 'coverage-baseline.md' .planning/milestones/archived-phases/02-coverage-audit/02-VERIFICATION.md` returns no match
- `npm test` exits 0
- `git log --oneline -1` subject line begins with `fix(57-01):` and mentions `real broken refs`
- The Task 2 commit's diff (`git diff --stat HEAD~1 HEAD`) lists ONLY the 7 files in `<files>` (or a subset, if a particular doc had no real broken refs that needed the case-by-case treatment) — no other files
  </acceptance_criteria>

  <done>All 16 real broken refs repaired or removed via surgical edits. Validator output shows broken refs ONLY in the five planned exempt paths. Test suite green. Commit on the branch with a clean diff scope (7 files at most).</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Author 57-01-SUMMARY.md with backfill evidence and DOCREF-01/02 closure</name>
  <files>.planning/phases/57-backfill-and-ci-integration/57-01-SUMMARY.md</files>

  <read_first>
- .planning/phases/55-internal-link-validator/55-03-SUMMARY.md (sibling SUMMARY shape — read for section structure: frontmatter, "Validator Run Against Real Repo", "Suite + Coverage Evidence", "Acceptance Mapping")
- .planning/phases/56-doc-drift-detector/56-03-SUMMARY.md (sibling SUMMARY for drift fixes — read for "Doc Updates Applied" section shape, mirror it for "Backfill Applied")
- .planning/phases/57-backfill-and-ci-integration/57-CONTEXT.md (D-03 DOCREF-01/02 closure rationale, D-21 wave plan)
- .planning/REQUIREMENTS.md (DOCREF-01, DOCREF-02 verbatim — for the Acceptance Mapping table)
- The pre-state and post-state JSON captured during Tasks 1-2 (paths printed to console in Task 1)
  </read_first>

  <action>
Write `.planning/phases/57-backfill-and-ci-integration/57-01-SUMMARY.md` with this structure (replace `BEGIN_FM` / `END_FM` with the literal three-dash YAML frontmatter delimiter `---`):

```markdown
BEGIN_FM
phase: 57-backfill-and-ci-integration
plan: 01
status: complete
created: <today's date YYYY-MM-DD>
END_FM

# Phase 57 Plan 01 Summary — Backfill (Pre-Wave 2 / Pre-CI)

## Validator Pre-State (BEFORE backfill)

**Command:** `node scripts/validate-doc-links.cjs --json`
**Pre-backfill broken count:** <N> (e.g., 109)
**Status:** broken

### Pre-state breakdown by group (from JSON output)

| Group | Count | Strategy |
|-------|-------|----------|
| `.planning/milestones/v*-ROADMAP.md` archived cross-refs | <N> | Mechanical sed sweep (D-02) |
| Real broken refs (file-not-found in docs/README*, archived 02-coverage-audit) | <N> | Case-by-case repair (D-04) |
| Real broken anchor refs (README, AGENTS, ARCHITECTURE, CONFIGURATION) | <N> | Case-by-case repair (D-05) |
| `tests/fixtures/doc-links/**` | <N> | Wave 2 `--exclude` (intentional fixtures) |
| `.claude/skills/SKILL.md`, `.claude/skills/dream-memory-consolidation/SKILL.md` | <N> | Wave 2 `--exclude` (template examples) |
| `.planning/REQUIREMENTS.md` | 1 | Wave 2 `--exclude` (regex example in DOCLINK-01 description; backtick wrap insufficient because validator does not skip inline-code spans) |
| `.planning/milestones/v2.7-phases/54-automated-uat-runner/54-03-PLAN.md` | 1 | Wave 2 `--exclude` (regex literal inside JS string inside markdown bullet) |

**Total:** <N>

## DOCREF-01 / DOCREF-02 Closure (D-03)

DOCREF-01 (`docs/health-reports/full-audit-2026-04-11.md`) and DOCREF-02 (`.planning/codebase/STRUCTURE.md`) close via clarification with evidence, NOT relocation. Both targets exist on disk; the validator confirms zero broken refs to either.

**Evidence (from validator JSON output):**

```bash
$ node scripts/validate-doc-links.cjs --json | jq '.broken[] | select(.ref | contains("full-audit") or contains("STRUCTURE.md"))'
<empty output — zero broken refs to either target>
```

(Replace with actual command output captured during Task 1's pre-state evidence step.)

| Requirement | Target | Live state | Closure evidence |
|-------------|--------|------------|------------------|
| DOCREF-01 | `docs/health-reports/full-audit-2026-04-11.md` | EXISTS on disk | Validator JSON: zero broken refs containing `full-audit-2026-04-11.md` |
| DOCREF-02 | `.planning/codebase/STRUCTURE.md` | EXISTS on disk | Validator JSON: zero broken refs containing `codebase/STRUCTURE.md` |

The remaining textual mentions of these targets are in `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md`, `tasks/todo.md`, and template/workflow descriptions inside `get-shit-done/`. These are descriptions of the requirements themselves and are not link refs that resolve to the targets — they are content about the targets, not links to them. No further action required.

**ROADMAP reconciliation (Task 0):** The Phase 57 Success Criterion #1 in `.planning/ROADMAP.md` was rewritten in Task 0 of this plan to match the closure strategy. The original criterion demanded a literal grep returning zero references; the new criterion accepts validator-clean evidence PLUS the requirement that remaining textual mentions be intentional descriptions rather than broken hyperlinks. This SUMMARY references that edit so the closure rationale is end-to-end traceable.

## Backfill Applied

### Sed sweep (Task 1) — 16 archived ROADMAPs

Single sed invocation across `.planning/milestones/v*-ROADMAP.md` (16 files):

```bash
sed -i '' 's|(milestones/v|(v|g; s|]: milestones/v|]: v|g' .planning/milestones/v*-ROADMAP.md
```

Files swept: v1.0, v1.1, v1.2, v1.3, v1.4, v1.5, v1.6, v1.7, v1.8, v1.9, v2.0, v2.1, v2.3, v2.4, v2.6, v2.7 (no v2.2 or v2.5 archive on disk).

**Drop:** ~83 broken refs.

### Case-by-case repairs (Task 2) — 16 real refs

| File | Line | Original ref | Action | Reason |
|------|------|--------------|--------|--------|
| docs/README-technical.md | 18 | `assets/terminal.svg` | <action taken> | <reason> |
| docs/README-technical.md | 32 | `docs/USER-GUIDE.md` | <action> | <reason> |
| docs/README-technical.md | 269 | `docs/workflow-discuss-mode.md` | <action> | <reason> |
| docs/README-technical.md | 608 | `docs/governance-customization.md` | <action> | <reason> |
| docs/README-technical.md | 742 | `docs/USER-GUIDE.md#configuration-reference` | <action> | <reason> |
| docs/README-technical.md | 975 | `LICENSE` | <action> | <reason> |
| docs/README.md | 5 | `pt-BR/README.md` | Removed (i18n placeholder, no translation exists) | D-04 |
| docs/README.md | 5 | `ja-JP/README.md` | Removed (i18n placeholder, no translation exists) | D-04 |
| docs/README.md | 5 | `zh-CN/README.md` | Removed (i18n placeholder, no translation exists) | D-04 |
| README.md | 27 | `#how-it-works` | <action> | D-05 |
| README.md | 27 | `#user-guide` | <action> | D-05 |
| docs/AGENTS.md | 33 | `#gsd-research-orchestrator` | <action> | D-05 |
| docs/AGENTS.md | 39 | `#gsd-research-orchestrator` | <action> | D-05 |
| docs/ARCHITECTURE.md | 17 | `#cli-tools-layer` | <action> | D-05 |
| docs/CONFIGURATION.md | 84 | `#adaptive-workflow` | <action> | D-05 |
| .planning/milestones/archived-phases/02-coverage-audit/02-VERIFICATION.md | 43 | `coverage-baseline.md` | <action> | <reason> |

**Drop:** 16 broken refs.

## Validator Post-State (AFTER backfill)

**Command:** `node scripts/validate-doc-links.cjs --json`
**Post-backfill broken count:** <N> (e.g., 10)
**Status:** broken (BUT all remaining are inside the Wave 2 exempt list)

### Post-state breakdown by file

| File | Count | Will be exempted in Wave 2? |
|------|-------|------------------------------|
| `tests/fixtures/doc-links/` | <N> | YES (`--exclude 'tests/fixtures/doc-links/**'`) |
| `.claude/skills/SKILL.md` | <N> | YES (`--exclude '.claude/skills/SKILL.md'`) |
| `.claude/skills/dream-memory-consolidation/SKILL.md` | <N> | YES (`--exclude '.claude/skills/dream-memory-consolidation/SKILL.md'`) |
| `.planning/REQUIREMENTS.md` | 1 | YES (`--exclude '.planning/REQUIREMENTS.md'`) |
| `.planning/milestones/v2.7-phases/54-automated-uat-runner/54-03-PLAN.md` | 1 | YES (`--exclude '.planning/milestones/v2.7-phases/54-automated-uat-runner/54-03-PLAN.md'`) |

**Outside-exempt-list count:** 0

## Suite Evidence

- `npm test`: PASS, <N> assertions across <M> suites (run after Task 2 completed)

## Acceptance Mapping (DOCREF-01, DOCREF-02 from REQUIREMENTS.md)

| Requirement | Status | Evidence |
|-------------|--------|----------|
| DOCREF-01 (`docs/health-reports/full-audit-2026-04-11.md` references repaired or removed) | Complete | Validator JSON shows zero broken refs to that target both before AND after backfill (target file exists on disk; closure-via-clarification per CONTEXT D-03) |
| DOCREF-02 (`.planning/codebase/STRUCTURE.md` references repaired or removed) | Complete | Validator JSON shows zero broken refs to that target both before AND after backfill (target file exists on disk; closure-via-clarification per CONTEXT D-03) |

## Hand-Off to Wave 2

The validator output now contains broken refs ONLY in the five planned exempt paths. Wave 2 (`57-02-PLAN.md`) will:
1. Add a `--exclude <glob>` flag to `scripts/validate-doc-links.cjs` (multi-value, gitignore-style globs).
2. Add tests for the new flag with a `tests/fixtures/doc-links/exclude/` fixture tree.
3. Confirm coverage thresholds preserved.

After Wave 2, invoking the validator with the planned `--exclude` list against the live repo will return `status: clean`, exit 0, and `broken.length === 0`. Wave 3 then wires CI gates assuming day-1 strict-blocking.
```

Fill in every `<N>`, `<action>`, and `<reason>` placeholder with the actual values from Tasks 1-2. The action and reason cells in the per-line repair table must be specific (e.g., "Removed link, kept visible text 'terminal screencast'", or "Retargeted #how-it-works → #adaptive-workflow (existing heading)").

Commit:

```
chore(57-01): record backfill evidence and DOCREF-01/02 closure

Adds 57-01-SUMMARY.md capturing pre/post validator state, sed-sweep delta,
per-file repair table, and DOCREF-01/02 closure evidence (validator output
shows zero broken refs to docs/health-reports/full-audit-2026-04-11.md or
.planning/codebase/STRUCTURE.md).

Hand-off note for Wave 2: validator broken refs now exist ONLY inside the
five planned exempt paths; Wave 2 adds the --exclude flag.
```
  </action>

  <verify>
    <automated>test -f .planning/phases/57-backfill-and-ci-integration/57-01-SUMMARY.md &amp;&amp; grep -F "DOCREF-01 / DOCREF-02 Closure" .planning/phases/57-backfill-and-ci-integration/57-01-SUMMARY.md &amp;&amp; grep -F "Backfill Applied" .planning/phases/57-backfill-and-ci-integration/57-01-SUMMARY.md &amp;&amp; grep -F "Validator Post-State" .planning/phases/57-backfill-and-ci-integration/57-01-SUMMARY.md &amp;&amp; grep -F "Acceptance Mapping" .planning/phases/57-backfill-and-ci-integration/57-01-SUMMARY.md &amp;&amp; grep -F "Hand-Off to Wave 2" .planning/phases/57-backfill-and-ci-integration/57-01-SUMMARY.md</automated>
  </verify>

  <acceptance_criteria>
- `.planning/phases/57-backfill-and-ci-integration/57-01-SUMMARY.md` exists
- `grep -F "DOCREF-01 / DOCREF-02 Closure" .planning/phases/57-backfill-and-ci-integration/57-01-SUMMARY.md` returns a match
- `grep -F "Backfill Applied" .planning/phases/57-backfill-and-ci-integration/57-01-SUMMARY.md` returns a match
- `grep -F "Validator Post-State" .planning/phases/57-backfill-and-ci-integration/57-01-SUMMARY.md` returns a match
- `grep -F "Acceptance Mapping" .planning/phases/57-backfill-and-ci-integration/57-01-SUMMARY.md` returns a match
- `grep -F "Hand-Off to Wave 2" .planning/phases/57-backfill-and-ci-integration/57-01-SUMMARY.md` returns a match
- `grep -E "^status: complete" .planning/phases/57-backfill-and-ci-integration/57-01-SUMMARY.md` returns a match
- `grep -E "Pre-backfill broken count: [0-9]+" .planning/phases/57-backfill-and-ci-integration/57-01-SUMMARY.md` returns a match
- `grep -E "Post-backfill broken count: [0-9]+" .planning/phases/57-backfill-and-ci-integration/57-01-SUMMARY.md` returns a match
- `grep -E "Outside-exempt-list count: 0" .planning/phases/57-backfill-and-ci-integration/57-01-SUMMARY.md` returns a match
- The SUMMARY contains a row referencing each of the 16 real broken refs by file:line (verify via `grep -F` for at least 5 distinct file paths from the list: `docs/README-technical.md`, `docs/README.md`, `README.md`, `docs/AGENTS.md`, `docs/ARCHITECTURE.md`)
- `git log --oneline -1` subject line begins with `chore(57-01):` and mentions backfill evidence
- `git diff --name-only HEAD~1 HEAD` lists ONLY `.planning/phases/57-backfill-and-ci-integration/57-01-SUMMARY.md`
  </acceptance_criteria>

  <done>SUMMARY.md authored with backfill pre/post evidence, DOCREF-01/02 closure rationale and validator-output evidence, per-line repair table for all 16 real refs, post-state broken-ref breakdown grouped by Wave 2 exempt path, and explicit hand-off section for Wave 2. Status frontmatter is `complete`. Single commit covering only the SUMMARY file.</done>
</task>

</tasks>

<verification>
End-to-end Wave 1 verification:

```bash
# 1. Sed sweep complete — no archived ROADMAP contains milestones/v prefix
git grep -nE '\(milestones/v[0-9]' .planning/milestones/v*-ROADMAP.md && echo FAIL || echo OK
git grep -nE '\]:\s+milestones/v[0-9]' .planning/milestones/v*-ROADMAP.md && echo FAIL || echo OK

# 2. Validator clean OUTSIDE exempt list (DOCREF-01/02 acceptance gate)
node scripts/validate-doc-links.cjs --json | node -e "let d=''; process.stdin.on('data',c=>d+=c); process.stdin.on('end',()=>{const o=JSON.parse(d); const exempt = ['tests/fixtures/doc-links/','.claude/skills/SKILL.md','.claude/skills/dream-memory-consolidation/SKILL.md','.planning/REQUIREMENTS.md','.planning/milestones/v2.7-phases/54-automated-uat-runner/54-03-PLAN.md']; const remaining = o.broken.filter(r => !exempt.some(e => r.file===e || r.file.startsWith(e))); console.log('Remaining outside exempt:', remaining.length); if (remaining.length !== 0) { console.log(JSON.stringify(remaining,null,2)); process.exit(1); }})"

# 3. DOCREF-01 / DOCREF-02 evidence — zero broken refs to relocated docs
node scripts/validate-doc-links.cjs --json | node -e "let d=''; process.stdin.on('data',c=>d+=c); process.stdin.on('end',()=>{const o=JSON.parse(d); const docref = o.broken.filter(r => r.ref.includes('full-audit-2026-04-11.md') || r.ref.includes('codebase/STRUCTURE.md')); console.log('DOCREF refs:', docref.length); if (docref.length !== 0) process.exit(1);})"

# 4. Test suite green
npm test

# 5. SUMMARY exists with closure evidence
test -f .planning/phases/57-backfill-and-ci-integration/57-01-SUMMARY.md
grep -F "DOCREF-01 / DOCREF-02 Closure" .planning/phases/57-backfill-and-ci-integration/57-01-SUMMARY.md
```
</verification>

<success_criteria>
- 83 archived-roadmap broken refs swept via single sed invocation (Task 1 commit)
- 16 real broken refs repaired or removed case-by-case (Task 2 commit)
- All remaining validator-flagged broken refs (10 or fewer) live ONLY inside the five paths planned for Wave 2 `--exclude`
- DOCREF-01 closure: validator JSON shows zero broken refs containing `full-audit-2026-04-11.md`
- DOCREF-02 closure: validator JSON shows zero broken refs containing `codebase/STRUCTURE.md`
- 57-01-SUMMARY.md authored with pre/post state, per-line repair table, DOCREF closure evidence, and Wave 2 hand-off (Task 3 commit)
- `npm test` exits 0 throughout
- Three commits on the branch: sed-sweep, real-ref repairs, SUMMARY
- Wave 2 prerequisite met: validator output is clean against the planned exempt list, allowing Wave 2 to add `--exclude` and produce a fully-clean validator run against the real repo
</success_criteria>

<output>
After Task 3 completes, `.planning/phases/57-backfill-and-ci-integration/57-01-SUMMARY.md` is the deliverable. It MUST embed validator JSON evidence proving DOCREF-01 / DOCREF-02 closure (zero broken refs to the two named relocated docs) and document every per-line repair. Wave 2 reads this SUMMARY to confirm the planned exempt list (5 paths) matches the actual remaining broken-ref locations before implementing the `--exclude` flag.
</output>

<review_disposition>
## Review Disposition (Phase 57 REVIEWS.md cross-AI feedback)

| Review item | Severity | Disposition | How addressed |
|---|---|---|---|
| `sed -i ''` BSD-only portability | Gemini LOW + Codex MEDIUM (consensus) | **Applied** | Task 1 Step 3 now provides a Darwin-vs-Linux-aware bash construct (`SED_INPLACE` array) PLUS explicit GNU `sed -i` fallback command, with an executor note about which platform requires which form |
| Acceptance checks rely on exact paths/line assumptions | Codex MEDIUM | **Already correctly designed** | The plan's acceptance criteria already use file+ref matching in the `must_haves.truths` JSON filters and in the per-edit verification protocol (Step 5 in Task 2). Line numbers are mentioned in the source-of-truth `<groundtruth>` block as inputs to the executor's investigation, not as acceptance gates |
| Per-task commit cadence may conflict with branch hygiene | Codex MEDIUM | **Rejected with reason** | Phase 57 follows v2.8 PR #22 precedent of squash-merge at ship time (CONTEXT confirms `gh pr merge --squash --delete-branch`). Per-subtask commits become squash-history that nobody reads — so the concern is real but its impact is zero in this project's PR workflow |
| `broken count around X` expectations create false alarms | Codex LOW | **Already correctly designed** | The plan's count-based acceptance criterion uses `> 30` upper bound (not equality), giving headroom for unrelated docs to drift; `< 12` upper bound on Task 2 acceptance similarly bounds without exact match |
| Task 0 roadmap reconciliation is process-heavy for a backfill wave | Codex LOW | **Rejected with reason** | The reconciliation IS the closure rationale's audit trail. Skipping Task 0 leaves a ROADMAP success criterion that the plan deliberately doesn't satisfy as written; the surface-level minor cost of one targeted Edit is the right price for end-to-end traceability. Gemini explicitly praised this as "a sign of high maturity" |

</review_disposition>
</content>
</invoke>