---
phase: 55-internal-link-validator
plan: 03
type: execute
wave: 3
depends_on:
  - 01
  - 02
files_modified:
  - CLAUDE.md
  - README.md
  - docs/DEVOPS-HANDOFF.md
autonomous: true
requirements:
  - DOCLINK-01
  - DOCLINK-02
  - DOCLINK-03
  - DOCLINK-04
must_haves:
  truths:
    - "test -f .planning/phases/55-internal-link-validator/55-03-SUMMARY.md && grep -F 'Validator Run Against Real Repo' .planning/phases/55-internal-link-validator/55-03-SUMMARY.md"
    - "npm test exits 0"
    - "npm run test:coverage exits 0"
    - "grep -F 'scripts/validate-doc-links.cjs' CLAUDE.md && grep -F 'scripts/validate-doc-links.cjs' README.md && grep -F 'scripts/validate-doc-links.cjs' docs/DEVOPS-HANDOFF.md (each file references the script's full path)"
    - "git diff --stat HEAD~1 HEAD -- CLAUDE.md README.md docs/DEVOPS-HANDOFF.md shows only insertions (no deletions of pre-existing content) — proves Task 2 added the new validator reference without touching pre-existing broken refs (Pass 2 Codex HIGH fix; backfill is Phase 57's job)"
  artifacts:
    - path: "CLAUDE.md"
      provides: "Updated project instructions referencing the new validator"
      must_contain:
        - "validate-doc-links"
    - path: "README.md"
      provides: "Updated public docs referencing the new validator"
      must_contain:
        - "validate-doc-links"
    - path: "docs/DEVOPS-HANDOFF.md"
      provides: "Updated handoff doc referencing the new validator"
      must_contain:
        - "validate-doc-links"
    - path: ".planning/phases/55-internal-link-validator/55-03-SUMMARY.md"
      provides: "Real-repo run findings + acceptance evidence"
  key_links:
    - from: "CLAUDE.md tests/coverage section"
      to: "scripts/validate-doc-links.cjs"
      via: "documentation reference"
      pattern: "validate-doc-links"
    - from: "README.md command/usage section"
      to: "scripts/validate-doc-links.cjs"
      via: "documentation reference"
      pattern: "validate-doc-links"
    - from: "docs/DEVOPS-HANDOFF.md tooling section"
      to: "scripts/validate-doc-links.cjs"
      via: "documentation reference"
      pattern: "validate-doc-links"
---

<objective>
Acceptance and documentation. Run the validator against the actual repo, record findings (do NOT auto-fix — that is Phase 57's responsibility per ROADMAP), confirm the full test suite is green and coverage thresholds hold, and update the three living docs (CLAUDE.md, README.md, docs/DEVOPS-HANDOFF.md) to mention the new validator script.

Purpose: Close DOCLINK-01..04 at the milestone-acceptance level. Demonstrate the validator works against the real repo, lock in the regression baseline (suite green, coverage thresholds preserved), and surface the new tool to anyone reading the living docs.

Output: Findings document, suite-green evidence, three living-doc updates, and a SUMMARY.md capturing what was observed and decided. Phase 57 will later use the findings document to plan repair work.

This plan does NOT create or fix broken links in real `.md` files. The ROADMAP explicitly assigns DOCREF-01 / DOCREF-02 (cross-reference backfill) to Phase 57. This plan records what is found; Phase 57 fixes it.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/55-internal-link-validator/55-RESEARCH.md
@.planning/phases/55-internal-link-validator/55-VALIDATION.md
@.planning/phases/55-internal-link-validator/55-01-PLAN.md
@.planning/phases/55-internal-link-validator/55-02-PLAN.md
@scripts/validate-doc-links.cjs
@tests/validate-doc-links.test.cjs
@CLAUDE.md
@README.md
@docs/DEVOPS-HANDOFF.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Run validator against real repo, run full suite + coverage, record findings</name>
  <files>.planning/phases/55-internal-link-validator/55-03-SUMMARY.md</files>

  <read_first>
- scripts/validate-doc-links.cjs (final state from plan 55-02 — confirms what is being executed)
- .planning/ROADMAP.md (especially Phase 57 section — confirms broken-ref repair is NOT this phase's job)
- .planning/REQUIREMENTS.md (DOCREF-01 and DOCREF-02 are scoped to Phase 57)
- tests/validate-doc-links.test.cjs (confirms suite contents before running npm test)
  </read_first>

  <action>
Execute these commands in order from the project root and capture output. Do not fix any broken links found — record them.

**Step 1: Run validator (text mode) against real repo. Use unique temp filenames so concurrent runs cannot collide (Pass 2 Codex MEDIUM fix), and keep the exit code in a SEPARATE file from the text output so the artifact is never corrupted:**
```bash
TXT_OUT="$(mktemp -t validate-doc-links-text.XXXXXX)"
TXT_EXIT="${TXT_OUT}.exit"
node scripts/validate-doc-links.cjs > "$TXT_OUT" 2>&1
echo "$?" > "$TXT_EXIT"
echo "Text output: $TXT_OUT"
echo "Exit code:   $(cat $TXT_EXIT)"
```

**Step 2: Run validator (JSON mode) against real repo. Same pattern: unique filename, exit code in a separate file so the JSON file remains parseable (Pass 2 Codex MEDIUM fix — JSON artifact corruption + fixed-/tmp-filename collision):**
```bash
JSON_OUT="$(mktemp -t validate-doc-links-json.XXXXXX)"
JSON_EXIT="${JSON_OUT}.exit"
node scripts/validate-doc-links.cjs --json > "$JSON_OUT" 2>&1
echo "$?" > "$JSON_EXIT"
echo "JSON output: $JSON_OUT"
echo "Exit code:   $(cat $JSON_EXIT)"

# Verify the JSON file parses cleanly:
node -e "JSON.parse(require('fs').readFileSync('$JSON_OUT','utf8'))" || {
  echo "WARN: JSON output is not parseable — capture stderr separately if needed"
}
```

**Step 3: Run full test suite:**
```bash
npm test
```
Record PASS/FAIL and assertion count. Suite must exit 0.

**Step 4: Run coverage:**
```bash
npm run test:coverage 2>&1 | tail -50
```
Record:
- Project-wide line coverage percentage
- Project-wide branch coverage percentage
- `scripts/validate-doc-links.cjs` row from the table (line %, branch %)

**Step 5: Author the SUMMARY.md.** Write `.planning/phases/55-internal-link-validator/55-03-SUMMARY.md` with this structure:

```markdown
---
phase: 55-internal-link-validator
plan: 03
status: complete
created: <today's date>
---

# Phase 55 Plan 03 Summary — Real-Repo Acceptance + Doc Updates

## Validator Run Against Real Repo

**Command:** `node scripts/validate-doc-links.cjs`
**Exit code:** <0 or 1>
**Files scanned:** <N>
**Links checked:** <N>
**Broken links:** <N>

### Broken Link Findings (recorded for Phase 57)

<If exit was 0: write "Real repo is clean — no broken links detected. Phase 57 backfill is still required for DOCREF-01 / DOCREF-02 (relocated docs) but no surprise findings from Phase 55.">

<If exit was 1: paste the FILE/LINE/REF/REASON table from the text run, then add a sub-section listing the unique reasons observed and counts per reason. Example:

| Reason | Count |
|--------|-------|
| file not found | 4 |
| anchor #foo not found in target | 2 |

Then explicitly note: "These broken refs are NOT repaired in Phase 55. Phase 57 (DOCREF-01, DOCREF-02) will repair or remove these references.">

## Suite + Coverage Evidence

- `npm test`: PASS, <N> assertions across <M> suites
- Project-wide line coverage: <X>% (threshold: >=91%)
- Project-wide branch coverage: <Y>% (threshold: >=83%)
- `scripts/validate-doc-links.cjs` per-module coverage: <Z>% lines (threshold: >=80%)

## Decisions

- All four DOCLINK requirement IDs are exercised by the test suite. Acceptance criteria from ROADMAP Phase 55 are satisfied.
- Verify blocks across all three plans use POSIX shell idioms (`grep`, `test`, `mktemp`). Cross-platform support for plan verification on Windows is an explicit non-goal of Phase 55 (Pass 2 consensus MEDIUM finding, deferred). The script itself (`scripts/validate-doc-links.cjs`) is pure Node and works on any platform; only the per-task acceptance shell snippets are POSIX-tied.

## Future Work Recommendations

- `.doclinkignore` (suppression list for intentional broken refs): NOT in scope for Phase 55. Pass 1 + Pass 2 reviews flagged this as scope creep against DOCLINK-01..04 — none of which mention an ignore mechanism. If the real-repo run surfaces cases that ARE intentional and should never trigger the validator, capture them here as candidates for a future phase. Do NOT implement an ignore file inline in Phase 55.
  - Candidates from this run: <list, or "none observed">
- Cross-platform verification for plan acceptance blocks (Windows compatibility): see Decisions above.

## Acceptance Mapping (ROADMAP Phase 55 Success Criteria)

| Success Criterion | Evidence |
|-------------------|----------|
| Running script produces a table on broken refs and exits non-zero | <real-repo or fixture broken-run output proves this> |
| Running script on a clean repo exits zero | <fixture clean run proves this; real-repo run pending Phase 57 backfill> |
| `--json` outputs machine-readable JSON | <fixture JSON run output is valid parseable JSON> |
| Broken anchor refs reported separately from broken file-path refs | <reason strings differ: "file not found" vs "anchor #X not found in target"> |
```

Fill in the placeholders with actual values from the runs in steps 1-4.

**Step 6: Commit SUMMARY.md as its own commit before moving to Task 2.** Keeps Task 2's doc-updates commit atomic so the Task 2 acceptance check `git diff --name-only HEAD~1 HEAD` sees only the three living docs.

Commit message:
```
chore(55-03): record real-repo validator run findings

Adds 55-03-SUMMARY.md capturing validate-doc-links.cjs output against
the real repo, npm test green confirmation, and coverage numbers.
No source files modified.
```
  </action>

  <verify>
    <automated>npm test &amp;&amp; test -f .planning/phases/55-internal-link-validator/55-03-SUMMARY.md &amp;&amp; grep -F "Validator Run Against Real Repo" .planning/phases/55-internal-link-validator/55-03-SUMMARY.md &amp;&amp; grep -F "Suite + Coverage Evidence" .planning/phases/55-internal-link-validator/55-03-SUMMARY.md</automated>
  </verify>

  <acceptance_criteria>
- `.planning/phases/55-internal-link-validator/55-03-SUMMARY.md` exists
- `grep -F "Validator Run Against Real Repo" .planning/phases/55-internal-link-validator/55-03-SUMMARY.md` returns a match
- `grep -F "Suite + Coverage Evidence" .planning/phases/55-internal-link-validator/55-03-SUMMARY.md` returns a match
- `grep -F "Acceptance Mapping" .planning/phases/55-internal-link-validator/55-03-SUMMARY.md` returns a match
- `grep -E "Files scanned: [0-9]+" .planning/phases/55-internal-link-validator/55-03-SUMMARY.md` returns a match (the actual file count was filled in)
- `grep -E "Links checked: [0-9]+" .planning/phases/55-internal-link-validator/55-03-SUMMARY.md` returns a match
- `grep -E "Broken links: [0-9]+" .planning/phases/55-internal-link-validator/55-03-SUMMARY.md` returns a match
- `grep -E "Project-wide line coverage: [0-9]+" .planning/phases/55-internal-link-validator/55-03-SUMMARY.md` returns a match
- `grep -E "Project-wide branch coverage: [0-9]+" .planning/phases/55-internal-link-validator/55-03-SUMMARY.md` returns a match
- `grep -F "Phase 57" .planning/phases/55-internal-link-validator/55-03-SUMMARY.md` returns a match (explicit hand-off note for backfill work)
- `npm test` exits 0
- `npm run test:coverage` exits 0
- No real `.md` files outside `.planning/phases/55-internal-link-validator/` were modified during this task (verify with `git status`: only `55-03-SUMMARY.md` is added/changed by this task)
- `git log --oneline -1 -- .planning/phases/55-internal-link-validator/55-03-SUMMARY.md` shows a commit dedicated to the SUMMARY (not bundled with Task 2's doc updates)
  </acceptance_criteria>

  <done>SUMMARY.md authored with real-repo validator findings (text + JSON in unique mktemp files with separate .exit files), suite green confirmation, coverage numbers, `.doclinkignore` listed under "Future Work Recommendations" (NOT "Decisions"), and Phase 57 hand-off note. No broken links repaired by this task. Test suite green.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Update CLAUDE.md, README.md, docs/DEVOPS-HANDOFF.md to mention the new validator</name>
  <files>CLAUDE.md, README.md, docs/DEVOPS-HANDOFF.md</files>

  <read_first>
- CLAUDE.md (current state — find the right section for documentation tools)
- README.md (current state — find the right section, likely Commands or Tools)
- docs/DEVOPS-HANDOFF.md (current state — find the right section, likely Tooling or Configuration Reference)
- .planning/phases/55-internal-link-validator/55-03-SUMMARY.md (from Task 1 — has accurate file counts and link counts to reference)
- scripts/validate-doc-links.cjs (the file being documented — header comment is the source of truth for usage)
  </read_first>

  <action>
**SCOPE LOCK (Pass 2 Codex HIGH fix — phase-boundary contradiction).** This task ONLY adds NEW lines to the three living docs that mention `scripts/validate-doc-links.cjs`. It MUST NOT modify any pre-existing content in CLAUDE.md, README.md, or docs/DEVOPS-HANDOFF.md, even if the validator (run in Task 1) flagged pre-existing broken refs in those files. Repair of pre-existing broken refs is Phase 57's responsibility (DOCREF-01, DOCREF-02). Concretely:
- If you see a broken ref in CLAUDE.md / README.md / DEVOPS-HANDOFF.md while editing — leave it alone.
- The diff for this commit must show only insertions; verify with `git diff --stat HEAD~1 HEAD -- CLAUDE.md README.md docs/DEVOPS-HANDOFF.md` after committing — only the `+` line counts should be non-zero.
- The new validator-reference lines you add must NOT be broken refs themselves; they describe the script by path, no link syntax that resolves to a file/anchor.

Make targeted, minimal edits to each living doc. Follow these rules:

1. Each doc gets EXACTLY ONE factual mention of `scripts/validate-doc-links.cjs`. Do NOT add multiple paragraphs of marketing prose. One concise sentence or a one-line table row, depending on the doc's existing structure. Pass 2 Codex MEDIUM fix: the original `grep -c "validate-doc-links" outputs 1 or more` was too loose; this task targets exactly one mention per file.
2. Match the existing tone and section structure of each doc. Do not refactor adjacent content.
3. Use the same usage pattern in all three docs for consistency:
   - **What it does:** Validates internal Markdown links (relative file refs and anchor refs) in tracked .md files.
   - **How to run:** `node scripts/validate-doc-links.cjs` (text mode) or `node scripts/validate-doc-links.cjs --json` (machine-readable).
   - **CI status:** Wired into CI as a blocking step in Phase 57 (use the phrase "wired into CI in Phase 57" or equivalent — do NOT claim it is already in CI; that is Phase 57's job).

**For CLAUDE.md:**
Read the file. Find a "Tests and Coverage" or "Documentation Standards" or "Tooling" section. Add one line to the relevant section. Example placement (the executor should pick the most natural section based on actual CLAUDE.md content):

```markdown
- `node scripts/validate-doc-links.cjs` — validate internal Markdown links across tracked `.md` files; exits non-zero on broken refs. Use `--json` for machine-readable output. (Wired into CI in Phase 57.)
```

If a "Documentation Standards" or "Validation" subsection exists, prefer adding it there. If only a "Tests and Coverage" section exists, add a new line below the npm test commands.

**For README.md:**
Read the file. Find the most appropriate section for command/script reference. Add one entry. Example for a markdown-table command list:

```markdown
| `node scripts/validate-doc-links.cjs` | Validates internal Markdown links (relative refs + anchor refs) in tracked `.md` files. Exits non-zero on broken links. Use `--json` for machine output. |
```

Or, if README.md uses a bullet list of scripts, add a matching bullet.

**For docs/DEVOPS-HANDOFF.md:**
Read the file. Find a "Tooling" / "Scripts" / "How to run" / "Configuration Reference" section. Add one entry. The DevOps handoff doc should emphasize the CI integration angle:

```markdown
- `scripts/validate-doc-links.cjs` — internal Markdown link validator. Run via `node scripts/validate-doc-links.cjs` (exits 0 on clean, 1 on broken). Phase 57 wires this as a blocking step in `.github/workflows/test.yml`.
```

After making the three edits, verify each doc still parses as valid Markdown (no broken structure) by running:
```bash
node scripts/validate-doc-links.cjs --json | node -e "let d=''; process.stdin.on('data',c=>d+=c); process.stdin.on('end',()=>{const o=JSON.parse(d); console.log('exit code path validates JSON parses')})"
```

Re-run the validator against the real repo:
```bash
node scripts/validate-doc-links.cjs
```
Note any new findings introduced by the doc edits (the new `validate-doc-links` references should not BE broken — they are descriptive, not actual links). If your doc edits introduced any actual broken links (e.g., a phantom path), fix them before committing.

Re-run the full test suite to confirm nothing regressed:
```bash
npm test
```

COMMIT:
```
docs(55-03): document validate-doc-links.cjs in living docs

Adds a single concise reference to scripts/validate-doc-links.cjs in
CLAUDE.md, README.md, and docs/DEVOPS-HANDOFF.md. CI integration is
deferred to Phase 57 per ROADMAP.

Closes: Phase 55 acceptance criteria — living docs reflect new validator.
```
  </action>

  <verify>
    <automated>grep -F "validate-doc-links" CLAUDE.md &amp;&amp; grep -F "validate-doc-links" README.md &amp;&amp; grep -F "validate-doc-links" docs/DEVOPS-HANDOFF.md &amp;&amp; npm test</automated>
  </verify>

  <acceptance_criteria>
- (Pass 2 Codex MEDIUM fix — exact-count grep, not 1-or-more): `node -e "const fs=require('fs'); for (const f of ['CLAUDE.md','README.md','docs/DEVOPS-HANDOFF.md']) { const n = (fs.readFileSync(f,'utf8').match(/validate-doc-links/g) || []).length; if (n !== 1) { console.error(f, 'has', n, 'matches; expected exactly 1'); process.exit(1); } }"` exits 0
- `grep -F "scripts/validate-doc-links.cjs" CLAUDE.md` returns a match (full path, not just basename)
- `grep -F "scripts/validate-doc-links.cjs" README.md` returns a match
- `grep -F "scripts/validate-doc-links.cjs" docs/DEVOPS-HANDOFF.md` returns a match
- Each doc's mention contains either `--json` OR `exits non-zero` OR `broken` (proves the entry describes behavior, not just names the file)
- `npm test` exits 0 (suite still green after doc edits)
- `node scripts/validate-doc-links.cjs --json` produces parseable JSON (the doc edits did not break the script or test fixtures)
- `git diff --name-only HEAD~1 HEAD 2>/dev/null` (after commit) lists exactly `CLAUDE.md`, `README.md`, `docs/DEVOPS-HANDOFF.md` for this commit
- (Pass 2 Codex HIGH fix — scope lock, no deletions of pre-existing content): run `git diff --numstat HEAD~1 HEAD -- CLAUDE.md README.md docs/DEVOPS-HANDOFF.md` and assert the third whitespace-separated column (deletions count) is `0` for every line. Implementation: `node -e "const {execFileSync} = require('child_process'); const out = execFileSync('git', ['diff','--numstat','HEAD~1','HEAD','--','CLAUDE.md','README.md','docs/DEVOPS-HANDOFF.md'], {encoding:'utf8'}); for (const line of out.trim().split('\\n')) { if (!line) continue; const cols = line.split(/\\s+/); if (parseInt(cols[1],10) > 0) { console.error('Deletion detected in:', line); process.exit(1); } }"` exits 0. Uses `execFileSync` (no shell, no injection).
- No mention of "Phase 57 already done" or "CI already wired" — wording correctly flags CI as deferred to Phase 57
  </acceptance_criteria>

  <done>Three living docs each contain one concise reference to `scripts/validate-doc-links.cjs` with usage and CI-deferral note. Test suite green. Single commit covering all three doc edits.</done>
</task>

</tasks>

<verification>
Phase 55 acceptance, end-to-end:

```bash
# 1. Validator script exists, exports six functions, has main()
node -e "const m = require('./scripts/validate-doc-links.cjs'); const want = ['discoverTrackedFiles','extractHeadingSlugs','extractLinks','formatTable','toGfmSlug','validateLink']; const have = Object.keys(m).sort(); if (JSON.stringify(have) !== JSON.stringify(want)) process.exit(1)"

# 2. Validator unit + integration tests green
node --test tests/validate-doc-links.test.cjs

# 3. Full suite green
npm test

# 4. Coverage thresholds held
npm run test:coverage

# 5. Validator runnable end-to-end
node scripts/validate-doc-links.cjs --root tests/fixtures/doc-links/clean    # exits 0
node scripts/validate-doc-links.cjs --root tests/fixtures/doc-links/broken; test $? -eq 1

# 6. Living docs reference the script
grep -F "validate-doc-links" CLAUDE.md
grep -F "validate-doc-links" README.md
grep -F "validate-doc-links" docs/DEVOPS-HANDOFF.md

# 7. Real-repo run findings recorded
test -f .planning/phases/55-internal-link-validator/55-03-SUMMARY.md
grep -F "Validator Run Against Real Repo" .planning/phases/55-internal-link-validator/55-03-SUMMARY.md
```
</verification>

<success_criteria>
- Validator ran against the real repo and findings are captured in 55-03-SUMMARY.md (text + JSON evidence; output stored in unique temp files via `mktemp`, exit codes stored in separate `.exit` files so JSON artifacts remain parseable)
- Full test suite (`npm test`) exits 0
- Coverage thresholds preserved: project-wide >=91% line / >=83% branch; per-module >=80% on the new script
- CLAUDE.md, README.md, docs/DEVOPS-HANDOFF.md each contain EXACTLY ONE factual mention of `scripts/validate-doc-links.cjs` (verified via Node match-count, not loose `grep -c >= 1`)
- Task 2 commit shows zero deletions of pre-existing content in CLAUDE.md / README.md / docs/DEVOPS-HANDOFF.md (verified via `git diff --numstat`); pre-existing broken refs are left for Phase 57's DOCREF-01 / DOCREF-02
- Two commits on the branch: SUMMARY.md authoring (separate commit), doc updates (separate commit; insertions only)
- All four DOCLINK-01..04 requirements have evidence of acceptance in 55-03-SUMMARY.md
</success_criteria>

<output>
The SUMMARY.md created in Task 1 is the deliverable. After Task 2, append a "Doc Updates Applied" section to 55-03-SUMMARY.md listing:
- The line added to CLAUDE.md (full text)
- The line added to README.md (full text)
- The line added to docs/DEVOPS-HANDOFF.md (full text)
- Commit hash for the doc-updates commit
</output>
