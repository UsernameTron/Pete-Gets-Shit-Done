# Phase 57.1: Bitter Lesson Surgery - Context

**Gathered:** 2026-07-15
**Status:** Ready for execution
**Source:** Operator directive via /gsd:plan-phase args + AskUserQuestion (4 locked decisions), plan-mode approval 2026-07-15

<domain>
## Phase Boundary

Strip judgment scaffolding from the entire GSD harness — routing tables, complexity classifiers, tier-promotion heuristics, keyword-scoring rubrics — while preserving all leverage scaffolding (deterministic gates, verification loops, environment facts, user config). Surface: GSD core (`get-shit-done/workflows/do.md`, `get-shit-done/bin/lib/{classify,model-profiles,history,init,core,config}.cjs`) plus all 47 vendored SKILL.md files under `plugins/` and `.claude/skills/`.

</domain>

<decisions>
## Implementation Decisions

### Locked by operator (2026-07-15)
- Derive the eight steps from the directive text; record at top of BITTER_LESSON_LOG.md (no external plan doc exists — verified).
- Scope: full harness — GSD core AND all 47 vendored skills.
- Phase slot: decimal insert 57.1; roadmap Phase 58 (Finalize Hardening) keeps its slot.
- Cut depth (split cut): DELETE classify.cjs entirely, dynamicSelect() + MODEL_TIERS, detectPatterns() tier-promotion feed. KEEP static per-agent MODEL_PROFILES lookup and history JSONL recording.

### Operator rules (verbatim intent)
- Running BITTER_LESSON_LOG.md: every deletion, line count removed, one-line defense of why judgment not leverage.
- Ambiguity → DEFERRED section, never a guess.
- Branch `bitter-lesson-surgery`, commit but do NOT push.
- Final report: total lines deleted, skills touched, 5 least-confident decisions.
- Subagents work skill-families in parallel. Report only when done or blocked.

### Claude's Discretion
- Registry design: frontmatter-in-place + `gsd-tools do-registry` aggregator at invocation (no generated file on disk — drift liability).
- Commit sequencing: 7 core commits each green; skills packages committed per-package by orchestrator with explicit paths.
- Rubric tiebreak: WHAT exists = leverage (keep); WHICH to pick / HOW MUCH to think = judgment (delete).

</decisions>

<canonical_refs>
## Canonical References

**Downstream agents MUST read these before implementing.**

### Approved plan (full design)
- `~/.claude/plans/58-load-the-entire-eventual-hare.md` — complete surgery design: core commits 1-7, skills rubric, work packages P1-P6, executor contract, verification gates

### Surgery surface
- `get-shit-done/workflows/do.md` — the 30-row routing table being replaced
- `get-shit-done/bin/lib/classify.cjs` — deleted entirely
- `get-shit-done/bin/lib/model-profiles.cjs` — split cut (keep static profiles)
- `get-shit-done/bin/lib/init.cjs` — sole classify consumer, sever lines ~110-155, ~250-295
- `get-shit-done/bin/lib/core.cjs` — dynamicSelect call at ~1310, config defaults/migration

</canonical_refs>

<specifics>
## Specific Ideas

- BITTER_LESSON_LOG.md at repo root is the running memory file; skills executors write BITTER_LESSON_LOG.pkg-N.md fragments merged by orchestrator.
- New do-routing test contract: registry completeness, idempotence, do.md contains no routing table.

</specifics>

<deferred>
## Deferred Ideas

Pre-flagged DEFERRED items live in the approved plan and land in BITTER_LESSON_LOG.md `## DEFERRED` during execution (recordExecution has no writer; detectPatterns recommended_tier field; config cleanup migration; whole-directory skill deletion; registry Trigger lines as browsable index; Phase 59 success-criterion wording references "routing table" — needs re-pointing at the registry when Phase 59 plans).

</deferred>

---

*Phase: 57.1-bitter-lesson-surgery*
*Context gathered: 2026-07-15 via plan-mode discussion + approval*
