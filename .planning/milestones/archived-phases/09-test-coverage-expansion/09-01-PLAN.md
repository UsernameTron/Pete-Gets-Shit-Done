---
phase: 9
plan: 1
title: "Test Coverage Expansion — Branch Coverage 77% → 85%+"
requirements: [SEC-05]
status: pending
---

# Plan 09-01: Test Coverage Expansion

## Objective

Increase branch coverage to 85%+ on security-critical paths and raise overall core.cjs branch coverage from 82.82% to 85%+.

## Current State

- `security.cjs`: 99.5% line / 89.9% branch — only lines 38-40 uncovered (invalid baseDir in validatePath)
- `core.cjs`: 80.02% line / 82.82% branch — many uncovered branches

## Tasks

### Task 1: Security-Critical Path Coverage

Target the four SEC-05 functions: `validatePath`, `validateShellArg`, `output`, `writeStateMd`.

1. **security.cjs lines 38-40**: Test `validatePath` with invalid baseDir (empty string, non-string)
2. **core.cjs output() raw path**: Test `output(result, true, rawValue)` — line 161-162
3. **core.cjs error()**: Test `error()` function — lines 188-191

### Task 2: Core Module Branch Coverage

Target utility functions with uncovered branches to push overall to 85%+:

1. **Config migration** (lines 233-238): Test deprecated "depth" key migration in `loadConfig`
2. **withPlanningLock** (lines 525-545): Test stale lock recovery and timeout paths
3. **planningDir/planningPaths** (lines 556-584): Test workstream-aware path resolution
4. **getActiveWorkstream** (lines 592-603): Test valid/invalid workstream detection
5. **comparePhaseNum** (lines 640-667): Test custom IDs, letter suffixes, decimal phases
6. **extractCurrentMilestone** (lines 820-885): Test version extraction from STATE.md and fallback paths
7. **replaceInCurrentMilestone** (lines 893-902): Test with/without `</details>` blocks
8. **checkAgentsInstalled** (lines 961-991): Test with missing/partial agents dir
9. **resolveModel with resolve_model_ids** (line 1032-1034): Test model ID resolution
10. **extractOneLinerFromBody** (lines 1047-1054): Test one-liner extraction
11. **getMilestoneInfo** (lines 1073-1086): Test in-progress marker detection

## Success Criteria

- security.cjs branch coverage ≥ 95% (currently 89.9%)
- core.cjs branch coverage ≥ 85% (currently 82.82%)
- All existing tests still pass
- No production code changes — test-only additions
