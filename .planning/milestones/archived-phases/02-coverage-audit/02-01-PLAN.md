---
phase: 02-coverage-audit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
autonomous: true
requirements:
  - COV-01

must_haves:
  truths:
    - "Running `npm run test:coverage` produces a per-module line/branch/function report for ALL JavaScript source files in the project"
    - "hooks/*.js files appear in coverage output with 0% (since no tests exercise them)"
    - "gsd-tools.cjs and bin/install.js appear in coverage output"
    - "hooks/dist/** files do NOT appear in coverage output"
  artifacts:
    - path: "package.json"
      provides: "Expanded test:coverage script covering all JS/CJS source files"
      contains: "test:coverage"
    - path: "coverage/coverage-final.json"
      provides: "Machine-readable Istanbul-format coverage data"
  key_links:
    - from: "package.json test:coverage script"
      to: "c8 CLI with expanded --include flags"
      via: "npm run test:coverage"
      pattern: "--include.*hooks.*--include.*gsd-tools"
---

<objective>
Expand c8 coverage instrumentation to measure ALL JavaScript/CJS source files in the project, not just lib modules.

Purpose: COV-01 requires per-module line/branch coverage across ALL modules. The current `test:coverage` script only covers `get-shit-done/bin/lib/*.cjs` (17 modules). This plan expands it to include hooks, gsd-tools.cjs, bin/install.js, and scripts -- then runs the expanded command to produce the raw coverage data that Plan 02 will use for gap analysis and baseline documentation.

Output: Updated package.json with expanded coverage command, coverage-final.json with per-module data for all source files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-coverage-audit/02-CONTEXT.md
@.planning/phases/02-coverage-audit/02-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand c8 coverage command in package.json</name>
  <files>package.json</files>
  <read_first>
    - package.json (current test:coverage script on line 51)
    - .planning/phases/02-coverage-audit/02-RESEARCH.md (Pattern 1: Expanded c8 Include Patterns, lines 78-104; Code Examples section lines 173-181)
    - .planning/phases/02-coverage-audit/02-CONTEXT.md (D-01, D-02, D-03 decisions)
  </read_first>
  <action>
Replace the existing `test:coverage` script in package.json with the expanded version per D-01. Also add a `test:coverage:full` variant that includes lcov output for future CI use.

Current `test:coverage` value (line 51):
```
"test:coverage": "c8 --check-coverage --lines 70 --reporter text --include 'get-shit-done/bin/lib/*.cjs' --exclude 'tests/**' --all node scripts/run-tests.cjs"
```

Replace with these two scripts:
```json
"test:coverage": "c8 --reporter text --reporter json --include 'get-shit-done/bin/lib/*.cjs' --include 'get-shit-done/bin/gsd-tools.cjs' --include 'bin/install.js' --include 'hooks/*.js' --include 'scripts/build-hooks.js' --exclude 'hooks/dist/**' --exclude 'tests/**' --all node scripts/run-tests.cjs",
"test:coverage:full": "c8 --reporter text --reporter lcov --reporter json --include 'get-shit-done/bin/lib/*.cjs' --include 'get-shit-done/bin/gsd-tools.cjs' --include 'bin/install.js' --include 'hooks/*.js' --include 'scripts/build-hooks.js' --exclude 'hooks/dist/**' --exclude 'tests/**' --all node scripts/run-tests.cjs"
```

Key changes from current:
1. Remove `--check-coverage --lines 70` (threshold will be re-established in Phase 3 after tests are written -- running it now would fail since expansion drops overall to ~55-60%)
2. Add `--reporter json` to produce machine-readable `coverage/coverage-final.json`
3. Add `--include 'get-shit-done/bin/gsd-tools.cjs'` (918 lines, currently unmeasured)
4. Add `--include 'bin/install.js'` (5,185 lines, currently unmeasured)
5. Add `--include 'hooks/*.js'` (5 files, 579 lines, currently unmeasured) per D-01
6. Add `--include 'scripts/build-hooks.js'` (82 lines, currently unmeasured)
7. Add `--exclude 'hooks/dist/**'` per D-03 (prevents double-counting compiled hooks)
8. Add `test:coverage:full` variant with lcov reporter for future CI integration (Phase 5)

Do NOT include `scripts/run-tests.cjs` in coverage -- it is test infrastructure, not application code.
Do NOT include any files from `hooks/dist/` per D-03.
Do NOT add `--check-coverage` -- thresholds will be set in Phase 3 after gaps are closed.
  </action>
  <verify>
    <automated>cd /Users/cpconnor/projects/Pete-Gets-Shit-Done && node -e "const p = require('./package.json'); const s = p.scripts['test:coverage']; if (!s.includes('gsd-tools.cjs')) process.exit(1); if (!s.includes('bin/install.js')) process.exit(1); if (!s.includes('hooks/*.js')) process.exit(1); if (!s.includes('hooks/dist')) process.exit(1); if (s.includes('check-coverage')) process.exit(1); if (!s.includes('reporter json')) process.exit(1); console.log('PASS: coverage script expanded correctly');"</automated>
  </verify>
  <acceptance_criteria>
    - package.json `test:coverage` script contains `--include 'get-shit-done/bin/gsd-tools.cjs'`
    - package.json `test:coverage` script contains `--include 'bin/install.js'`
    - package.json `test:coverage` script contains `--include 'hooks/*.js'`
    - package.json `test:coverage` script contains `--exclude 'hooks/dist/**'`
    - package.json `test:coverage` script does NOT contain `--check-coverage`
    - package.json `test:coverage` script contains `--reporter json`
    - package.json `test:coverage:full` script exists and contains `--reporter lcov`
    - `npm run test:coverage` exits 0 and prints per-module coverage table to stdout
    - `coverage/coverage-final.json` exists after running the command
  </acceptance_criteria>
  <done>
    `npm run test:coverage` runs successfully with expanded scope, produces per-module text output AND coverage/coverage-final.json. All JS/CJS source files appear in the report. hooks/dist files are excluded.
  </done>
</task>

<task type="auto">
  <name>Task 2: Run expanded coverage and verify all modules appear</name>
  <files></files>
  <read_first>
    - package.json (to see the updated test:coverage script from Task 1)
    - .planning/phases/02-coverage-audit/02-RESEARCH.md (Pitfall 1: c8 --all shows 0% for unloaded files, lines 149-152)
  </read_first>
  <action>
Run `npm run test:coverage` and verify the output. This is a validation task -- no files are modified.

1. Run `npm run test:coverage` and capture stdout
2. Verify these modules appear in the text output (they are newly added and expected to show 0% or near-0%):
   - `get-shit-done/bin/gsd-tools.cjs`
   - `bin/install.js`
   - `hooks/gsd-prompt-guard.js`
   - `hooks/gsd-workflow-guard.js`
   - `hooks/gsd-statusline.js`
   - `hooks/gsd-check-update.js`
   - `hooks/gsd-context-monitor.js`
   - `scripts/build-hooks.js`
3. Verify these modules STILL appear (existing lib modules):
   - `get-shit-done/bin/lib/security.cjs`
   - `get-shit-done/bin/lib/commands.cjs`
   - `get-shit-done/bin/lib/core.cjs`
4. Verify NO files from `hooks/dist/` appear in the output
5. Verify `coverage/coverage-final.json` was created and contains entries for the newly-included files

If any module is missing from the output, debug by checking that the `--include` pattern matches the actual file path. c8 uses picomatch globs -- `hooks/*.js` matches files directly in hooks/ but not hooks/dist/.

Expected result: Overall line coverage drops from ~91% to ~55-60% due to the 6,764 lines of newly-included 0%-covered code. This is expected and correct.
  </action>
  <verify>
    <automated>cd /Users/cpconnor/projects/Pete-Gets-Shit-Done && npm run test:coverage 2>&1 | grep -c "gsd-tools\|install\.js\|gsd-prompt-guard\|gsd-workflow-guard\|gsd-statusline\|gsd-check-update\|gsd-context-monitor\|build-hooks" | xargs test 8 -le</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test:coverage` exits 0 (all 1,547 tests still pass)
    - stdout contains `gsd-tools.cjs` in the per-module table
    - stdout contains `install.js` in the per-module table
    - stdout contains all 5 hooks/*.js files in the per-module table
    - stdout does NOT contain any `hooks/dist/` files
    - `coverage/coverage-final.json` file exists and is valid JSON
    - coverage-final.json contains a key matching `gsd-tools.cjs`
  </acceptance_criteria>
  <done>
    Expanded coverage command runs cleanly, all expected modules appear in the per-module report, hooks/dist is excluded, and machine-readable JSON output is produced. COV-01 is satisfied.
  </done>
</task>

</tasks>

<verification>
- `npm test` passes (no regressions from package.json change)
- `npm run test:coverage` produces per-module report with ALL JS/CJS source files
- `coverage/coverage-final.json` exists with Istanbul-format data
- No `hooks/dist/` files in coverage output
- All 1,547 existing tests still pass
</verification>

<success_criteria>
COV-01 is met: Running the coverage tool produces a per-module line/branch coverage report for every module in the project. The expanded `test:coverage` script covers lib modules, gsd-tools.cjs, bin/install.js, hooks source files, and scripts/build-hooks.js.
</success_criteria>

<output>
After completion, create `.planning/phases/02-coverage-audit/02-01-SUMMARY.md`
</output>
