---
phase: 03-unit-test-expansion
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/context-monitor-hook.test.cjs
  - tests/statusline-hook.test.cjs
  - tests/check-update-hook.test.cjs
  - tests/build-hooks.test.cjs
autonomous: true
requirements: [UNIT-03]
must_haves:
  truths:
    - "gsd-context-monitor.js has test coverage for warning/critical thresholds, debounce, stale metrics, and config disable"
    - "gsd-statusline.js has test coverage for context bar rendering, bridge file writing, and update notification"
    - "gsd-check-update.js has test coverage for config directory detection and cache directory creation"
    - "build-hooks.js has test coverage for syntax validation, file copy, and error handling"
  artifacts:
    - path: "tests/context-monitor-hook.test.cjs"
      provides: "Unit tests for gsd-context-monitor.js"
      min_lines: 80
    - path: "tests/statusline-hook.test.cjs"
      provides: "Unit tests for gsd-statusline.js"
      min_lines: 60
    - path: "tests/check-update-hook.test.cjs"
      provides: "Unit tests for gsd-check-update.js"
      min_lines: 40
    - path: "tests/build-hooks.test.cjs"
      provides: "Unit tests for build-hooks.js"
      min_lines: 40
  key_links:
    - from: "tests/context-monitor-hook.test.cjs"
      to: "hooks/gsd-context-monitor.js"
      via: "runHook helper from hook-helpers.cjs + writeBridgeFile"
      pattern: "runHook.*context-monitor"
    - from: "tests/build-hooks.test.cjs"
      to: "scripts/build-hooks.js"
      via: "execFileSync running build-hooks.js in temp directory"
      pattern: "execFileSync.*build-hooks"
---

<objective>
Write tests for the remaining 4 modules at 0% coverage: context-monitor, statusline, check-update hooks, and build-hooks build script.

Purpose: Eliminates all remaining 0% coverage modules. Combined with Plan 01, all 6 hook files and build-hooks.js will have test coverage.
Output: 4 new test files covering context-monitor, statusline, check-update, and build-hooks
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-unit-test-expansion/03-RESEARCH.md

@hooks/gsd-context-monitor.js
@hooks/gsd-statusline.js
@hooks/gsd-check-update.js
@scripts/build-hooks.js
@tests/helpers.cjs

Note: This plan depends on tests/hook-helpers.cjs from Plan 01 at runtime (test execution), but both plans can be WRITTEN in parallel since they create independent files. The hook-helpers.cjs must exist before running these tests. If executing in parallel, Plan 01 should complete first or hook-helpers.cjs should be created as a shared prerequisite.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create context-monitor and statusline hook tests</name>
  <files>tests/context-monitor-hook.test.cjs, tests/statusline-hook.test.cjs</files>
  <action>
Create tests/context-monitor-hook.test.cjs testing gsd-context-monitor.js. Import runHook, createTempWithConfig, writeBridgeFile from `./hook-helpers.cjs`. Use cleanup from `./helpers.cjs`.

IMPORTANT pitfall (from Research): The context monitor reads a bridge file from os.tmpdir(). Without it, the hook exits silently with code 0. Use writeBridgeFile in beforeEach to set up known metrics. Also, the debounce state file (`claude-ctx-{sessionId}-warned.json`) must be cleaned between tests to avoid cross-test contamination.

Test cases for context-monitor:

- **No session_id — passthrough**: Send `{}` — exit 0, no output.
- **No bridge file — passthrough**: Send `{ session_id: 'test123' }` without creating bridge file — exit 0.
- **Stale metrics — passthrough**: Write bridge file with timestamp 120 seconds in the past (STALE_SECONDS = 60) — exit 0.
- **High remaining (above 35%) — no warning**: Write bridge with remaining_percentage: 50, used_pct: 50 — exit 0.
- **Warning threshold (remaining <= 35%)**: Write bridge with remaining_percentage: 30, used_pct: 70 — stdout should contain JSON with `CONTEXT WARNING` in additionalContext.
- **Critical threshold (remaining <= 25%)**: Write bridge with remaining_percentage: 20, used_pct: 80 — stdout should contain `CONTEXT CRITICAL`.
- **Debounce suppresses repeated warnings**: After first warning, call hook 3 more times (callsSinceWarn < DEBOUNCE_CALLS=5) — subsequent calls should exit 0 with no output. NOTE: You need to write the warned state file between calls to simulate debounce state.
- **Severity escalation bypasses debounce**: Set warned state with lastLevel='warning' and callsSinceWarn=1, then send critical metrics — should output CRITICAL despite debounce.
- **Config disables warnings**: Create temp with `{ hooks: { context_warnings: false } }`, set cwd in stdin — exit 0.
- **GSD active detection**: Create STATE.md in temp cwd, send warning-level metrics — message should contain GSD-specific text about STATE.md tracking.

Clean up bridge files and warned state files in afterEach.

Create tests/statusline-hook.test.cjs testing gsd-statusline.js:

- **Basic output with model and directory**: Send `{ model: { display_name: 'Claude' }, workspace: { current_dir: '/project/myapp' } }` — stdout should contain 'Claude' and 'myapp'.
- **Context bar rendering — low usage**: Send `{ ..., context_window: { remaining_percentage: 80 } }` — stdout should contain progress bar characters and a percentage.
- **Context bar rendering — high usage**: Send with remaining_percentage: 20 — stdout should contain skull emoji or high-usage indicator.
- **Bridge file creation**: Send with session_id and remaining_percentage — verify bridge file exists in os.tmpdir() at expected path, clean up after.
- **No context_window — no bar**: Send without context_window field — stdout should NOT contain percentage.
- **Update notification**: Create cache file at expected path with `{ update_available: true }` — stdout should contain update indicator.
- **Malformed JSON stdin**: Send invalid JSON — exit 0 with no output.

For statusline tests, set CLAUDE_CONFIG_DIR env to a temp directory to avoid reading real config.
  </action>
  <verify>
    <automated>cd /Users/cpconnor/projects/Pete-Gets-Shit-Done && node --test tests/context-monitor-hook.test.cjs && node --test tests/statusline-hook.test.cjs</automated>
  </verify>
  <done>tests/context-monitor-hook.test.cjs has 10+ passing tests. tests/statusline-hook.test.cjs has 7+ passing tests. Both gsd-context-monitor.js and gsd-statusline.js show > 0% coverage.</done>
</task>

<task type="auto">
  <name>Task 2: Create check-update and build-hooks tests</name>
  <files>tests/check-update-hook.test.cjs, tests/build-hooks.test.cjs</files>
  <action>
Create tests/check-update-hook.test.cjs testing gsd-check-update.js.

IMPORTANT pitfall (from Research): This hook spawns a detached background process. Testing the spawned script directly is impractical. Instead, test the observable side effects: cache directory creation and the detectConfigDir logic.

Test cases for check-update:

- **Cache directory creation**: Run the hook in a temp directory with CLAUDE_CONFIG_DIR set to a temp path that has `get-shit-done/VERSION` file. After running, verify the cache directory was created at `{CLAUDE_CONFIG_DIR}/cache/`. The hook creates this synchronously before spawning the background child.
- **detectConfigDir with CLAUDE_CONFIG_DIR env**: Set CLAUDE_CONFIG_DIR to a temp dir containing `get-shit-done/VERSION`, run hook — cache dir should be under that env path.
- **detectConfigDir fallback to .claude**: Create temp home dir with `.claude/get-shit-done/VERSION`, set HOME to temp — hook should use `.claude` path. NOTE: This test may be fragile; if it is, skip and document why.
- **No VERSION file — uses default path**: Run without any VERSION file — hook should still create cache dir at default `.claude/cache/` path under HOME.

For these tests, since the hook runs synchronously up to the spawn call, the cache directory creation happens before the background child. Use a short sleep (100ms) or just check the synchronous side effects.

Create tests/build-hooks.test.cjs testing scripts/build-hooks.js.

Test approach: Create a temp directory mimicking the hooks/ structure, copy or symlink the build-hooks.js script, then run it. The script uses `__dirname` relative paths, so the simplest approach is to run it from the project root but with modified file paths — OR — create a wrapper that requires and calls the build() function after overriding the paths.

Alternative approach (simpler): Since build-hooks.js uses hardcoded `path.join(__dirname, '..', 'hooks')`, test it by running the actual script from the project root and checking its effects on `hooks/dist/`.

Test cases:

- **Successful build — all hooks copied**: Run `node scripts/build-hooks.js` from project root. Verify all 5 hook files exist in `hooks/dist/`. Verify exit code 0. Verify stdout contains "Build complete".
- **Syntax validation passes for all hooks**: After running build, verify each file in dist/ is valid JavaScript (require it in a try/catch or use vm.Script).
- **Syntax error detection**: Create a temp copy of hooks/ with one file containing `const x = 1; const x = 2;` (duplicate const). Run build-hooks.js with __dirname pointing to temp. Verify exit code 1 and stderr/stdout contains "SyntaxError". NOTE: This may require a modified invocation or a temporary file swap — if too complex, test validateSyntax directly by requiring the script with a module wrapper.
- **Missing hook file — warning**: Temporarily rename one hook file, run build. Verify stdout contains "not found, skipping" warning. Restore the file after test.

If direct execution is too coupled to real filesystem, extract validateSyntax for direct testing:
```javascript
// At top of test file, before requiring build-hooks.js
// build-hooks.js calls build() immediately, so we can't require it directly.
// Instead, test the CLI behavior via execFileSync.
```
  </action>
  <verify>
    <automated>cd /Users/cpconnor/projects/Pete-Gets-Shit-Done && node --test tests/check-update-hook.test.cjs && node --test tests/build-hooks.test.cjs</automated>
  </verify>
  <done>tests/check-update-hook.test.cjs has 3+ passing tests. tests/build-hooks.test.cjs has 3+ passing tests. gsd-check-update.js and build-hooks.js show > 0% coverage.</done>
</task>

</tasks>

<verification>
Run full test suite to confirm no regressions:
```bash
npm test
```

Check coverage on all 4 modules:
```bash
npm run test:coverage 2>&1 | grep -E "context-monitor|statusline|check-update|build-hooks"
```

All 4 modules should show > 0% line coverage.
</verification>

<success_criteria>
- All 4 test files exist and pass
- npm test passes with no regressions
- gsd-context-monitor.js, gsd-statusline.js, gsd-check-update.js, build-hooks.js all show > 0% coverage
- No modules at 0% coverage remain after this plan + Plan 01
</success_criteria>

<output>
After completion, create `.planning/phases/03-unit-test-expansion/03-02-SUMMARY.md`
</output>
