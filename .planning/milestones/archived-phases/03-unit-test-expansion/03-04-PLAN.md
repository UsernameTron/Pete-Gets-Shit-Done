---
phase: 03-unit-test-expansion
plan: 04
type: execute
wave: 2
depends_on: [03-01, 03-02, 03-03]
files_modified:
  - tests/commands.test.cjs
autonomous: true
requirements: [UNIT-04]
must_haves:
  truths:
    - "cmdCommitToSubrepo has test coverage for sub-repo grouping, missing message error, no sub_repos error, and unmatched files warning"
    - "Every command handler identified as uncovered in Phase 2 has at least one test"
    - "Overall npm test suite passes with no regressions"
  artifacts:
    - path: "tests/commands.test.cjs"
      provides: "Extended tests for cmdCommitToSubrepo (appended to existing file)"
      contains: "commit-to-subrepo"
  key_links:
    - from: "tests/commands.test.cjs"
      to: "get-shit-done/bin/lib/commands.cjs"
      via: "runGsdTools CLI invocation"
      pattern: "runGsdTools.*commit-to-subrepo"
---

<objective>
Close the UNIT-04 gap by adding tests for the only untested command handler: cmdCommitToSubrepo. Also run the final coverage sweep to confirm all phase requirements are met.

Purpose: UNIT-04 requires every uncovered command handler to have at least one test. cmdCommitToSubrepo is the only handler with zero tests (cmdProgressRender, cmdTodoMatchPhase, cmdTodoComplete already have tests in commands.test.cjs). This plan also serves as the phase verification gate.
Output: Extended commands.test.cjs with commit-to-subrepo tests, final coverage report
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-unit-test-expansion/03-RESEARCH.md

@tests/commands.test.cjs
@tests/helpers.cjs
@get-shit-done/bin/lib/commands.cjs (lines 321-387 for cmdCommitToSubrepo)

<interfaces>
From get-shit-done/bin/lib/commands.cjs:
```javascript
function cmdCommitToSubrepo(cwd, message, files, raw)
// CLI: gsd-tools.cjs commit-to-subrepo --message "msg" --files file1 file2
// Requires: .planning/config.json with sub_repos array
// Groups files by sub-repo prefix, stages and commits in each sub-repo
// Returns JSON: { committed, repos: { [name]: { committed, hash, files } }, unmatched }
```

From tests/helpers.cjs:
```javascript
function createTempGitProject(prefix) // tmpDir with git init + initial commit
function runGsdTools(args, cwd, env)  // array form for safe arg passing
function cleanup(tmpDir)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add cmdCommitToSubrepo tests and run phase coverage sweep</name>
  <files>tests/commands.test.cjs</files>
  <action>
Append a new `describe('commit-to-subrepo command', ...)` block to the existing tests/commands.test.cjs file. Do NOT modify existing tests — only add new ones at the end.

This command requires git repos, so use `createTempGitProject()` from helpers.cjs. The command needs `.planning/config.json` with a `sub_repos` array.

Test cases:

- **Missing message — error**: Run `runGsdTools(['commit-to-subrepo'], tmpDir)` without --message. Assert success is false and error output mentions "commit message required".

- **No sub_repos configured — error**: Create temp git project with `.planning/config.json` containing `{}` (no sub_repos). Run `runGsdTools(['commit-to-subrepo', '--message', 'test', '--files', 'foo/bar.txt'], tmpDir)`. Assert error mentions "no sub_repos configured".

- **No files provided — error**: Create temp git project with config containing `{ "sub_repos": ["subrepo-a"] }`. Run without --files. Assert error mentions "--files required".

- **Successful commit to sub-repo**: Set up temp git project. Create a sub-repo directory with its own git init:
  1. Create tmpDir as git project
  2. Write `.planning/config.json` with `{ "sub_repos": ["subrepo-a"] }`
  3. Create `subrepo-a/` as a separate git repo (git init, config user, initial commit)
  4. Create a new file `subrepo-a/newfile.txt`
  5. Run `runGsdTools(['commit-to-subrepo', '--message', 'add file', '--files', 'subrepo-a/newfile.txt'], tmpDir)`
  6. Assert success, parse JSON output, verify repos['subrepo-a'].committed is true and hash is non-null.

- **Unmatched files warning**: Set up config with `{ "sub_repos": ["subrepo-a"] }`. Run with files including one that doesn't match any prefix: `['subrepo-a/file.txt', 'other/file.txt']`. Assert the output includes unmatched array or stderr contains warning about unmatched files.

Use the array form of runGsdTools (passing args as array) to avoid shell escaping issues with the --files flag.

IMPORTANT: Each sub-repo needs its own `git init` + user config + initial commit, separate from the parent tmpDir git repo. Use `execSync` for the git setup steps, similar to how `createTempGitProject` works.

After writing the tests, run `npm run test:coverage` as the final phase gate to capture the coverage report. Document the before/after coverage numbers in the SUMMARY.
  </action>
  <verify>
    <automated>cd /Users/cpconnor/projects/Pete-Gets-Shit-Done && node --test tests/commands.test.cjs 2>&1 | grep -A1 "commit-to-subrepo" && npm test</automated>
  </verify>
  <done>cmdCommitToSubrepo has 5 passing tests covering error paths and successful commit flow. All UNIT-04 command handlers now have at least one test. npm test passes. Coverage run shows no modules remain at 0%.</done>
</task>

</tasks>

<verification>
Run full test suite and coverage:
```bash
npm test
npm run test:coverage
```

Phase-level checks:
1. UNIT-01: `grep -c 'installGovernancePlugins' tests/governance-install.test.cjs` shows tests exist
2. UNIT-02: `tests/plugin-loading.test.cjs` exists and passes
3. UNIT-03: All 6 hook test files + build-hooks test exist and pass
4. UNIT-04: `grep -c 'commit-to-subrepo' tests/commands.test.cjs` shows tests exist

No modules should remain at 0% line coverage.
</verification>

<success_criteria>
- cmdCommitToSubrepo has 5 passing tests (error paths + success path)
- All 4 UNIT requirements have test coverage
- npm test passes with no regressions
- No modules at 0% coverage
- commands.cjs coverage improved above 86.89% baseline
</success_criteria>

<output>
After completion, create `.planning/phases/03-unit-test-expansion/03-04-SUMMARY.md`
</output>
