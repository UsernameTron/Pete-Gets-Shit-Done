---
phase: 03-unit-test-expansion
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/governance-install.test.cjs
  - tests/plugin-loading.test.cjs
autonomous: true
requirements: [UNIT-01, UNIT-02]
must_haves:
  truths:
    - "installGovernancePlugins has dedicated test coverage for plugin directory copy, missing plugins dir, and multiple plugin handling"
    - "Plugin loading from plugin.json is tested at runtime level — not just structure validation"
    - "Command routing through the dispatcher has branch coverage for untested paths"
    - "Skill resolution paths are tested for file lookup and fallback behavior"
  artifacts:
    - path: "tests/governance-install.test.cjs"
      provides: "Extended tests for installGovernancePlugins (appended to existing file)"
      contains: "installGovernancePlugins"
    - path: "tests/plugin-loading.test.cjs"
      provides: "Runtime plugin loading, command routing, and skill resolution tests"
      min_lines: 60
  key_links:
    - from: "tests/governance-install.test.cjs"
      to: "bin/install.js"
      via: "require with GSD_TEST_MODE=1"
      pattern: "installGovernancePlugins"
    - from: "tests/plugin-loading.test.cjs"
      to: "plugins/"
      via: "require plugin.json and validate runtime structure"
      pattern: "plugin\\.json"
---

<objective>
Close the UNIT-01 gap (installGovernancePlugins coverage) and UNIT-02 gap (plugin runtime loading, command routing, skill resolution).

Purpose: UNIT-01 requires governance scripts to have test coverage — installGovernancePlugins is the only untested governance function. UNIT-02 requires plugin integration paths to be tested beyond structure validation.
Output: Extended governance-install.test.cjs with installGovernancePlugins tests, new plugin-loading.test.cjs
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-unit-test-expansion/03-RESEARCH.md

@tests/governance-install.test.cjs
@tests/plugin-integration.test.cjs
@tests/dispatcher.test.cjs
@tests/agent-skills.test.cjs
@bin/install.js (lines 4112-4136 for installGovernancePlugins)
@plugins/

<interfaces>
From bin/install.js (exported with GSD_TEST_MODE=1):
```javascript
function installGovernancePlugins(targetDir, src)
// targetDir: path where plugins should be installed (creates targetDir/plugins/)
// src: path to source package containing plugins/ directory
// Copies each subdirectory from src/plugins/ to targetDir/plugins/
// Logs warning if src/plugins/ does not exist
```

From tests/helpers.cjs:
```javascript
function createTempProject(prefix)  // returns tmpDir with .planning/phases/
function createTempDir(prefix)      // returns bare tmpDir
function cleanup(tmpDir)            // rm -rf
function runGsdTools(args, cwd, env) // runs gsd-tools.cjs CLI
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add installGovernancePlugins tests to governance-install.test.cjs</name>
  <files>tests/governance-install.test.cjs</files>
  <action>
Append a new `describe('installGovernancePlugins', ...)` block to the existing tests/governance-install.test.cjs file. Do NOT modify existing tests — only add new ones at the end.

The file already has `process.env.GSD_TEST_MODE = '1'` at the top and requires install.js exports. Add installGovernancePlugins to the destructured import if not already present.

Test cases:

- **Copies plugin directories to target**: Create temp src dir with `plugins/plugin-a/plugin.json` and `plugins/plugin-b/plugin.json` (minimal valid JSON files). Create temp target dir. Call `installGovernancePlugins(targetDir, srcDir)`. Assert `targetDir/plugins/plugin-a/plugin.json` exists and `targetDir/plugins/plugin-b/plugin.json` exists.

- **Missing plugins directory — logs warning and returns**: Create temp src dir WITHOUT a plugins/ subdirectory. Call `installGovernancePlugins(targetDir, srcDir)`. Assert no plugins/ directory was created in targetDir. (The function logs a warning and returns early.)

- **Nested plugin files copied recursively**: Create src with `plugins/my-plugin/commands/cmd.md` and `plugins/my-plugin/skills/skill.md`. Call installGovernancePlugins. Assert both nested files exist in target.

- **Idempotent — second call overwrites cleanly**: Call installGovernancePlugins twice with same args. Assert no errors and files still exist.

Use createTempDir for both src and target directories. Clean up in afterEach.
  </action>
  <verify>
    <automated>cd /Users/cpconnor/projects/Pete-Gets-Shit-Done && node --test tests/governance-install.test.cjs</automated>
  </verify>
  <done>installGovernancePlugins has 4 passing tests covering plugin copy, missing dir, nested files, and idempotency. install.js line coverage improves above 67.56% baseline.</done>
</task>

<task type="auto">
  <name>Task 2: Create plugin-loading runtime tests</name>
  <files>tests/plugin-loading.test.cjs</files>
  <action>
Create tests/plugin-loading.test.cjs covering UNIT-02: plugin runtime loading, command routing, and skill resolution.

The existing plugin-integration.test.cjs validates plugin structure (JSON validity, no forbidden files). This new file tests RUNTIME behavior.

Test cases for plugin loading:

- **Each plugin.json loads and has required fields**: For each plugin directory in `plugins/`, require its plugin.json. Assert it has `name`, `version`, `description`, and at least one of `commands`, `skills`, or `agents`.

- **Plugin command files exist and are readable**: For each plugin, if it declares `commands` in plugin.json, verify each command file referenced exists on disk and is a non-empty markdown file.

- **Plugin skill files exist**: For each plugin, if it declares `skills`, verify each skill directory exists and contains a SKILL.md file.

Test cases for command routing (via CLI):

- **Known command routes correctly**: Run `runGsdTools('help', tmpDir)` — verify it succeeds and output is non-empty. This exercises the dispatcher's routing logic.

- **Plugin-defined command routes**: Run `runGsdTools('stats', tmpDir)` from a temp project — verify it returns valid JSON. This exercises a command that goes through the full dispatch chain.

Test cases for skill resolution:

- **Skill directories discoverable**: Read the project's `.claude/skills/` directory (if it exists) or `get-shit-done/skills/` — verify each subdirectory contains a SKILL.md file.

- **Agent files have valid frontmatter**: For each `.md` file in `agents/` directory, read it and verify it contains YAML frontmatter with at least `name` and `description` fields.

Import runGsdTools, createTempProject, cleanup from `./helpers.cjs`. Use path.join(__dirname, '..') to reference project root for reading plugin directories.
  </action>
  <verify>
    <automated>cd /Users/cpconnor/projects/Pete-Gets-Shit-Done && node --test tests/plugin-loading.test.cjs</automated>
  </verify>
  <done>tests/plugin-loading.test.cjs has 6+ passing tests covering plugin.json loading, command file existence, command routing via CLI, and skill/agent resolution. UNIT-02 requirements are satisfied.</done>
</task>

</tasks>

<verification>
Run full test suite:
```bash
npm test
```

Verify governance and plugin test coverage:
```bash
npm run test:coverage 2>&1 | grep -E "install\.js|commands\.cjs"
```

install.js should show improved coverage above 67.56% baseline.
</verification>

<success_criteria>
- installGovernancePlugins has dedicated tests (4+ passing)
- Plugin runtime loading tested (plugin.json fields, command files, skill files)
- Command routing tested via CLI (dispatcher exercises)
- npm test passes with no regressions
- install.js line coverage > 70%
</success_criteria>

<output>
After completion, create `.planning/phases/03-unit-test-expansion/03-03-SUMMARY.md`
</output>
