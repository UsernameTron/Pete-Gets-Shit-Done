---
phase: 06-crew-assessment-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ~/.claude/agents/architect.md
  - ~/.claude/agents/scaffolder.md
  - ~/.claude/agents/auditor.md
  - ~/.claude/agents/validator.md
  - ~/.claude/agents/memory-seeder.md
  - ~/.claude/agents/extension-validator.md
  - ~/.claude/agents/hook-engineer.md
  - ~/.claude/agents/plugin-builder.md
autonomous: true
requirements: [CREW-01]

must_haves:
  truths:
    - "All 8 agents have single-line description strings in YAML frontmatter"
    - "Every agent file parses as valid YAML frontmatter"
    - "Description meaning is preserved after conversion"
  artifacts:
    - path: "~/.claude/agents/architect.md"
      provides: "Fixed YAML frontmatter"
      contains: 'description: "'
    - path: "~/.claude/agents/scaffolder.md"
      provides: "Fixed YAML frontmatter"
      contains: 'description: "'
    - path: "~/.claude/agents/auditor.md"
      provides: "Fixed YAML frontmatter"
      contains: 'description: "'
    - path: "~/.claude/agents/validator.md"
      provides: "Fixed YAML frontmatter"
      contains: 'description: "'
    - path: "~/.claude/agents/memory-seeder.md"
      provides: "Fixed YAML frontmatter"
      contains: 'description: "'
    - path: "~/.claude/agents/extension-validator.md"
      provides: "Fixed YAML frontmatter"
      contains: 'description: "'
    - path: "~/.claude/agents/hook-engineer.md"
      provides: "Fixed YAML frontmatter"
      contains: 'description: "'
    - path: "~/.claude/agents/plugin-builder.md"
      provides: "Fixed YAML frontmatter"
      contains: 'description: "'
  key_links: []
---

<objective>
Fix YAML parsing errors in 8 agent files by converting multi-line `description: >` blocks to single-line `description: "..."` strings.

Purpose: Priority 1 of crew assessment — eliminate YAML parsing failures that prevent agent metadata from loading correctly.
Output: 8 agent files with valid single-line YAML description fields.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-crew-assessment-fixes/06-CONTEXT.md

Note: These 8 agents exist ONLY in ~/.claude/agents/ (they are not GSD-prefixed agents from the source repo). The CONTEXT.md says "edit BOTH source and global copies" but these files have no source copy in ~/projects/Pete-Gets-Shit-Done/agents/. Edit only the global copies where they exist.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix YAML descriptions in all 8 agents</name>
  <files>
    ~/.claude/agents/architect.md
    ~/.claude/agents/scaffolder.md
    ~/.claude/agents/auditor.md
    ~/.claude/agents/validator.md
    ~/.claude/agents/memory-seeder.md
    ~/.claude/agents/extension-validator.md
    ~/.claude/agents/hook-engineer.md
    ~/.claude/agents/plugin-builder.md
  </files>
  <action>
For each of the 8 agent files listed above:

1. Read the file
2. Find the YAML frontmatter block (between `---` delimiters)
3. If the description uses multi-line syntax (`description: >`, `description: |`, or `description:` followed by indented continuation lines), convert it to a single-line quoted string: `description: "the full description text here"`
4. Preserve the full meaning of the description — collapse whitespace, remove line breaks, keep all content
5. Escape any double quotes within the description text with backslash
6. Write the updated file back
7. If a file already has a single-line description, skip it (no-op)

These files exist ONLY in ~/.claude/agents/. Do not look for source copies in ~/projects/Pete-Gets-Shit-Done/agents/ — they don't exist there.
  </action>
  <verify>
    <automated>for f in architect scaffolder auditor validator memory-seeder extension-validator hook-engineer plugin-builder; do echo "=== $f ===" && head -10 ~/.claude/agents/$f.md | grep -c 'description: "' && echo "OK"; done</automated>
  </verify>
  <done>All 8 agent files have single-line description: "..." strings. No multi-line description blocks remain. Files are otherwise unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Validate YAML parsing for all fixed agents</name>
  <files>~/.claude/agents/*.md</files>
  <action>
Run a YAML parse validation across all 8 fixed agent files. For each file:

1. Extract the YAML frontmatter (content between the first two `---` lines)
2. Parse it and confirm it produces valid key-value pairs
3. Confirm `description` is a string (not null, not an object)
4. Confirm `name` field is present and valid

Use a simple Python or Node script to validate. Example approach:
```bash
python3 -c "
import yaml, sys, glob
errors = []
for name in ['architect','scaffolder','auditor','validator','memory-seeder','extension-validator','hook-engineer','plugin-builder']:
    path = f'{sys.argv[1]}/{name}.md'
    with open(path) as f:
        content = f.read()
    fm = content.split('---')[1]
    try:
        data = yaml.safe_load(fm)
        if not isinstance(data.get('description'), str):
            errors.append(f'{name}: description is not a string')
        if not data.get('name'):
            errors.append(f'{name}: missing name field')
    except yaml.YAMLError as e:
        errors.append(f'{name}: YAML parse error: {e}')
if errors:
    print('FAILURES:'); [print(f'  - {e}') for e in errors]; sys.exit(1)
else:
    print('All 8 agents parse cleanly')
" ~/.claude/agents
```

If any fail, fix the YAML issue before proceeding.
  </action>
  <verify>
    <automated>python3 -c "
import yaml, sys
errors = []
for name in ['architect','scaffolder','auditor','validator','memory-seeder','extension-validator','hook-engineer','plugin-builder']:
    path = f'{sys.argv[1]}/{name}.md'
    with open(path) as f:
        content = f.read()
    fm = content.split('---')[1]
    try:
        data = yaml.safe_load(fm)
        assert isinstance(data.get('description'), str), 'description not string'
        assert data.get('name'), 'missing name'
    except Exception as e:
        errors.append(f'{name}: {e}')
if errors:
    print('FAILURES:'); [print(f'  - {e}') for e in errors]; sys.exit(1)
print(f'All 8 agents parse cleanly')
" ~/.claude/agents</automated>
  </verify>
  <done>Zero YAML parsing errors across all 8 agent files. Every file has a valid name and string description.</done>
</task>

</tasks>

<verification>
- All 8 agents have `description: "..."` single-line format
- YAML frontmatter parses without errors for all 8 files
- No content loss in descriptions (meaning preserved)
- Agent files are otherwise unmodified (body content untouched)
</verification>

<success_criteria>
Priority 1 (YAML Parsing) complete: 8/8 agents fixed, zero parse errors.
</success_criteria>

<output>
After completion, create `.planning/phases/06-crew-assessment-fixes/06-01-SUMMARY.md`
</output>
