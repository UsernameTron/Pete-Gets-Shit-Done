---
phase: 06-crew-assessment-fixes
plan: 04
type: execute
wave: 3
depends_on: [06-02, 06-03]
files_modified:
  - ~/.claude/get-shit-done/workflows/ship.md
  - ~/projects/Pete-Gets-Shit-Done/commands/gsd/finalize.md
  - ~/.claude/agents/*.md (tool-tier updates)
  - ~/projects/Pete-Gets-Shit-Done/agents/*.md (tool-tier updates)
autonomous: true
requirements: [CREW-05, CREW-06]

must_haves:
  truths:
    - "repo-doc-architect is wired into the finalize workflow"
    - "repo-commit-documenter is wired into the ship workflow"
    - "Every active agent has an allowed-tools field matching its assigned tier"
    - "Every active agent has a Tier comment in frontmatter for visibility"
  artifacts:
    - path: "~/.claude/get-shit-done/workflows/ship.md"
      provides: "Ship workflow with repo-commit-documenter integration"
      contains: "repo-commit-documenter"
    - path: "~/projects/Pete-Gets-Shit-Done/commands/gsd/finalize.md"
      provides: "Finalize command with repo-doc-architect integration"
      contains: "repo-doc-architect"
  key_links:
    - from: "~/.claude/get-shit-done/workflows/ship.md"
      to: "repo-commit-documenter"
      via: "subagent spawn"
      pattern: "repo-commit-documenter"
    - from: "~/projects/Pete-Gets-Shit-Done/commands/gsd/finalize.md"
      to: "repo-doc-architect"
      via: "subagent spawn"
      pattern: "repo-doc-architect"
---

<objective>
Wire utility agents into GSD workflows and assign tool-access tiers to every agent.

Purpose: Priorities 5 and 6 of crew assessment — integrate orphan utility agents into the pipeline, and enforce least-privilege tool access across all agents.
Output: Updated ship/finalize workflows, all agents with tier-appropriate allowed-tools.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-crew-assessment-fixes/06-CONTEXT.md

Read these before starting:
@~/.claude/agents/repo-doc-architect.md
@~/.claude/agents/repo-commit-documenter.md
@~/.claude/get-shit-done/workflows/ship.md
@~/projects/Pete-Gets-Shit-Done/commands/gsd/finalize.md

For tool tiers, you will need to read each active agent file to assess its minimum required tier.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire utility agents into GSD workflows (P5)</name>
  <files>
    ~/.claude/get-shit-done/workflows/ship.md
    ~/projects/Pete-Gets-Shit-Done/commands/gsd/finalize.md
  </files>
  <action>
**Wire repo-commit-documenter into ship workflow:**

1. Read ~/.claude/get-shit-done/workflows/ship.md fully
2. Read ~/.claude/agents/repo-commit-documenter.md to understand its capabilities
3. Add a step in the ship workflow (after commit creation, before PR creation) that spawns repo-commit-documenter as a subagent to generate/update commit documentation
4. The spawn should be optional/conditional — only if the workflow detects documentation-worthy changes (not for trivial commits)
5. Add repo-commit-documenter to the agent list at the top of the workflow

**Wire repo-doc-architect into finalize workflow:**

6. Read ~/projects/Pete-Gets-Shit-Done/commands/gsd/finalize.md fully
7. Read ~/.claude/agents/repo-doc-architect.md to understand its capabilities
8. Add a step in the finalize command that spawns repo-doc-architect to auto-generate/update project documentation (README, DEVOPS-HANDOFF, etc.) during finalization
9. The spawn should happen before the final verification step
10. Add repo-doc-architect to any agent list in the command

Note: There is no finalize.md workflow file — the finalize command IS the entry point (~/projects/Pete-Gets-Shit-Done/commands/gsd/finalize.md). If a workflow file needs to be created to match the pattern, create it at ~/.claude/get-shit-done/workflows/finalize.md. Otherwise, wire directly into the command file.

Do NOT modify the agent files themselves — only the workflow/command files.
  </action>
  <verify>
    <automated>echo "=== ship.md has repo-commit-documenter ===" && grep -c "repo-commit-documenter" ~/.claude/get-shit-done/workflows/ship.md && echo "=== finalize has repo-doc-architect ===" && grep -c "repo-doc-architect" ~/projects/Pete-Gets-Shit-Done/commands/gsd/finalize.md && echo "PASS"</automated>
  </verify>
  <done>Both utility agents wired into their respective workflows. repo-commit-documenter spawns during ship. repo-doc-architect spawns during finalize.</done>
</task>

<task type="auto">
  <name>Task 2: Assign tool-access tiers to all active agents (P6)</name>
  <files>
    ~/projects/Pete-Gets-Shit-Done/agents/*.md (all active agents)
    ~/.claude/agents/*.md (all global copies)
  </files>
  <action>
Define 4 tiers:
- **Explore**: Read, Glob, Grep, Bash (read-only commands only)
- **Research**: Read, Glob, Grep, Bash, WebSearch, WebFetch
- **Modify**: Read, Write, Edit, Bash, Glob, Grep
- **Full**: All tools including MCP integrations

For each active agent file (in both source and global locations):

1. Read the agent file
2. Determine the minimum tier needed based on what the agent does:
   - Agents that only analyze/review code → Explore
   - Agents that research external docs/libraries → Research
   - Agents that create/modify files → Modify
   - Agents that need MCP or all tools → Full
3. Update the YAML frontmatter:
   - Set `allowed-tools:` to the tier's tool list (or add it if missing)
   - Add `# Tier: [TierName]` comment on the line above or after allowed-tools
4. Write the updated file to BOTH source and global locations

**Tier assignment guidance** (verify by reading each agent):
- Explore tier candidates: agents that only read/analyze (e.g., gsd-assumptions-analyzer, gsd-ui-auditor, gsd-ui-checker)
- Research tier candidates: agents that need web access (e.g., gsd-research-orchestrator, gsd-advisor-researcher)
- Modify tier candidates: most builder agents (e.g., gsd-executor, gsd-planner, gsd-debugger, scaffolder)
- Full tier candidates: agents needing MCP (rare — flag any currently at Full that could go lower)

Important: Read each agent to confirm — do not guess. Some "analyzer" agents may need Bash write access for running tests.

For agents that exist ONLY in global (~/.claude/agents/) but NOT in source (the non-gsd-prefixed agents like architect, scaffolder, hook-engineer, etc.): update only the global copy.

For agents that exist in BOTH locations: update both.

After all assignments, produce a summary table of agent → tier assignments.
  </action>
  <verify>
    <automated>echo "=== Checking all source agents have tier comments ===" && for f in ~/projects/Pete-Gets-Shit-Done/agents/gsd-*.md; do basename "$f" | tr -d '\n'; echo -n ": "; grep -c "Tier:" "$f" 2>/dev/null || echo "MISSING"; done && echo "=== Checking sample global agents ===" && for f in ~/.claude/agents/gsd-verifier.md ~/.claude/agents/gsd-executor.md ~/.claude/agents/gsd-planner.md; do basename "$f" | tr -d '\n'; echo -n ": "; grep -c "Tier:" "$f" 2>/dev/null || echo "MISSING"; done && echo "DONE"</automated>
  </verify>
  <done>Every active agent has an allowed-tools field matching its assigned tier and a Tier comment for visibility. Summary table of assignments documented.</done>
</task>

</tasks>

<verification>
- repo-commit-documenter spawned in ship workflow
- repo-doc-architect spawned in finalize command/workflow
- Every active agent has Tier comment and matching allowed-tools
- No agent has more access than needed (Full tier only where justified)
</verification>

<success_criteria>
Priorities 5 and 6 complete: Utility agents wired into workflows. All agents have tool-access tier assignments.
</success_criteria>

<output>
After completion, create `.planning/phases/06-crew-assessment-fixes/06-04-SUMMARY.md`
</output>
