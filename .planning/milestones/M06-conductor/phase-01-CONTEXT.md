# CONDUCTOR — Phase 01 Context
**Milestone:** M06-conductor (Extension Factory Phase 6) · **Status:** PHASE_DISCUSSING → resolved, advancing to PHASE_PLANNED

## Source of authority
The accepted proposal (`conductor-phase6-proposal.md`, accepted July 17, 2026) is the architecture of record **as amended by its supersession notice and this file's revision record**. On any conflict, phase-01-PLAN v2 wins. This milestone implements the amended architecture; it does not reopen it. Proposal Phases A/B/C map to GSD work phases 01/02/03.

## Ultrathink transcript (planning pass)

- **Architect:** Sequence inside phase-01 is contracts-first: freeze schemas at v1, then shared lib, then delegates, then the Layer 0 router last (it depends on everything below it). The conductor's judgment lives in rubrics and fragment-based prompts, not the model — encode now, before the Fable 5 seat rotates.
- **Research:** Phase-03's scheduled sweeps reuse the existing off → observe → draft → active rollout doctrine rather than inventing a promotion path. The data-lane check follows the flag-check pattern: a fail-closed precondition at the top of dispatch, independent of model output.
- **Coder:** Machine-specific CLI flags are never written from memory — sub-step 1 probes the installed binaries into `CAPABILITIES.md`, and later steps reference the probe. Skills are generated by the factory's own generators; Phase 6 is the factory extending itself.
- **Tester:** Three hard gates before this phase ships: 100% envelope round-trip on both delegates, the planted-injection fixture surviving the pipeline as inert data, and a measured Grok finding-precision number (which sets phase-02's auto-triage threshold — no invented targets).
- **Synthesis:** Single milestone, three phases matching the accepted A/B/C. Full runbook for phase-01 only; 02/03 stubbed per GSD doctrine — premature detail rots before execution.

## Decisions carried in (all ANSWERED — see PLAN Phase 0)
Placement as factory milestone M06; JSON Schema v1 frozen at gate; delegates are thin bash wrappers over headless CLIs; data classification via per-repo marker file; Grok Build local runbook execution is an entry criterion, not an assumption.

## Explicit non-goals for this phase
No codex-delegate (phase-02). No scheduling or launchd (phase-03). No new memory stores ever — findings destined for retention go through second-brain's existing extract → propose → promote pipeline in phase-03.

## Assumptions flagged
1. Factory `.planning/` exists with phases 0–5 recorded; milestone index M06 is presumed free — renumber the directory on drop-in if taken.
2. Gemini CLI package name (`@google/gemini-cli`) — verify at Phase 0.5 before install.
3. Grok Build deployed per the accepted local runbook (LM Studio backend; the probe-verified local model ID). Verified, not assumed, at Phase 0.4.

## Revision record — v2 (2026-07-17, same day)

Independent review received and integrated. Verdict: accepted on substance. Changes of record:
1. **Air-gap claim corrected → Option A, approved-cloud governance.** The conductor is cloud-backed; content it reads has reached Anthropic before routing occurs. The data lane therefore governs *delegate* providers, with Claude Code as the approved processor of record under Anthropic commercial terms (no training on inputs). "Air-gapped" is reserved for the Grok-local lane with egress technically blocked. Option B (deterministic local pre-router ahead of the conductor) is recorded as a deferred hardening path if client terms ever require it.
2. **Canonicalizer removed from phase-01** — it violated the stated rollback boundary by mutating repos outside the factory. Now phase-04 (repo migration), after router gates.
3. **Read-only enforced structurally**: Gemini runs in plan mode against a write-stripped snapshot; Grok locality is proven by an egress-blocked completion, loopback base_url check, and disabled web/MCP tools — not by a model name in `inspect`.
4. **Provider JSON is transport, not contract.** Wrappers are adapters: extract → normalize → validate against return.v1 → fail closed.
5. **Two-session plan (8–15 h gate-ready)** replaces the 4-hour estimate. First value (first logged delegation) still lands in session one.
Also integrated: location-union evidence contract; provenance block (traceable, not "reproducible"); expanded injection suite; ledger renamed to append-only application log with hash chain, mkdir lock as the portable default; classification taxonomy with missing = confidential and a secret pre-scan on cloud dispatch; ambiguity = no dispatch, never "self."
Retained with clarified framing: 30/90-day success metrics are measurement horizons, not phase-01 automation scope. Benchmark is a smoke test (10 seeded + 5 clean diffs); it sets no automation policy — thresholds are a phase-02 decision on a stratified set.
**Known rubric deviation:** phase exceeds the 4-hour heuristic. Kept as one phase because the gates are indivisible from what they gate; sessions are checkpointed instead.

## Re-homing note (2026-07-17, staging time)
Disk scan showed the standalone factory repo archived (local _archive/, GitHub archived 2026-06-09) and absorbed into Pete-Gets-Shit-Done as plugins/claude-code-factory. M06 planning therefore lives in Pete-Gets-Shit-Done. The "Factory Phase 6" framing stands — the conductor extends the factory plugin. All factory repo paths in these documents were rewritten to this repo at staging.
