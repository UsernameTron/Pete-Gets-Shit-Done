# CONDUCTOR — Phase 03 (stub)
# Autonomy & Integration

**GSD lifecycle state:** stub — flesh out at PHASE_DISCUSSING.

## What you're building
Scheduled overnight Grok sweeps via launchd (read-only sandbox, turn-capped, observe-mode first per rollout doctrine); findings feeding the existing morning brief; accepted lessons feeding second-brain's extract → propose → promote pipeline with receipts — never a new memory store; weekly economics rollup reconciling against the ledger with a per-lane kill switch.

## Gates (summary)
One full week of sweeps with zero unattended writes and zero unexpected egress; brief renders findings; rollup reconciles with ledger to the cent; `/gsd:verify-work` green.

## Rollback
`launchctl unload` the job; nothing else changes.

## Flywheel scope added in v2.1
Adversarial injection-suite growth: the local reasoning model periodically generates injection candidates against the sanitizer in a sandboxed loop; any survivor becomes a permanent fixture — human-reviewed before admission, per untrusted-data doctrine.
