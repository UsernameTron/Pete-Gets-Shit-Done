---
phase: 46-housekeeping
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - CLAUDE.md
  - commands/gsd/crew.md
autonomous: true
requirements:
  - DOC-01
  - REF-01

must_haves:
  truths:
    - "README.md states 63 commands and 18 agents in all count references"
    - "CLAUDE.md states 63 commands and 18 agents in all count references"
    - "README.md command table includes /gsd:audit-agents and /gsd:audit-deps"
    - "README.md inventory table shows 63 GSD commands and 18 Specialized agents"
    - "CLAUDE.md agent roster lists gsd-dependency-auditor and gsd-ecosystem-auditor"
    - "crew.md does not contain the string Pete-Gets-Shit-Done"
  artifacts:
    - path: "README.md"
      provides: "Accurate agent/command counts and complete command table"
      contains: "63 commands, 18 agents"
    - path: "CLAUDE.md"
      provides: "Accurate agent/command counts and complete agent roster"
      contains: "18 built-in agents"
    - path: "commands/gsd/crew.md"
      provides: "Portable path resolution for agents directory"
  key_links:
    - from: "README.md"
      to: "commands/gsd/"
      via: "command count and table entries"
      pattern: "63 commands.*18 agents"
    - from: "CLAUDE.md"
      to: "agents/"
      via: "agent roster listing"
      pattern: "18 built-in agents"
---

<objective>
Update README.md and CLAUDE.md to reflect the actual agent count (18, not 16) and command count (63, not 61), add the two missing commands to the README command table, add the two missing agents to the CLAUDE.md roster, and fix the hardcoded path in crew.md.

Purpose: The foundation audit found documentation drift — counts and tables are stale from v2.3 when gsd-dependency-auditor, gsd-ecosystem-auditor, /gsd:audit-agents, and /gsd:audit-deps were added. The crew.md hardcoded path breaks portability.
Output: Three files updated with accurate counts and portable paths.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/46-housekeeping/46-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update README.md counts and command table</name>
  <files>README.md</files>
  <read_first>
    - README.md (current state of counts and command table)
    - commands/gsd/audit-agents.md (description field for table entry)
    - commands/gsd/audit-deps.md (description field for table entry)
  </read_first>
  <action>
Make these exact changes to README.md:

1. **Line 59** — "What You Get" table, Execution Engine row:
   Change `61 commands, 16 agents` to `63 commands, 18 agents`

2. **Line 69** — Inventory table, GSD commands row:
   Change `| GSD commands | 61 |` to `| GSD commands | 63 |`

3. **Line 70** — Inventory table, Specialized agents row:
   Change `| Specialized agents | 16 |` to `| Specialized agents | 18 |`

4. **Line 1193** — Section heading:
   Change `### GSD Commands (61)` to `### GSD Commands (63)`

5. **Line 218** — Prose paragraph:
   Change `Before we get into the 61 commands` to `Before we get into the 63 commands`

6. **Insert two new rows in the GSD Commands table** (between existing alphabetically sorted entries):
   - After the `/gsd:add-todo` row and before `/gsd:audit-milestone`:
     ```
     | `/gsd:audit-agents` | Audit the GSD agent ecosystem for frontmatter integrity, tool/permission mismatches, and hygiene gaps |
     | `/gsd:audit-deps` | Audit package dependencies for CVEs, staleness, and license issues |
     ```

These are the only changes. Do not modify any other content.
  </action>
  <verify>
    <automated>grep -c "63 commands" README.md && grep -c "18 agents" README.md && grep "audit-agents" README.md && grep "audit-deps" README.md && grep "GSD Commands (63)" README.md</automated>
  </verify>
  <acceptance_criteria>
    - README.md line 59 contains "63 commands, 18 agents"
    - README.md contains "| GSD commands | 63 |"
    - README.md contains "| Specialized agents | 18 |"
    - README.md contains "### GSD Commands (63)"
    - README.md contains "Before we get into the 63 commands"
    - README.md contains "| `/gsd:audit-agents` |"
    - README.md contains "| `/gsd:audit-deps` |"
    - The audit-agents row appears before the audit-milestone row (alphabetical order)
    - No other content is changed
  </acceptance_criteria>
  <done>README.md reflects 18 agents, 63 commands, and includes audit-agents and audit-deps in the command table</done>
</task>

<task type="auto">
  <name>Task 2: Update CLAUDE.md counts and agent roster</name>
  <files>CLAUDE.md</files>
  <read_first>
    - CLAUDE.md (current state of counts and agent roster)
  </read_first>
  <action>
Make these exact changes to CLAUDE.md:

1. **Line 14** — Key capabilities bullet:
   Change `**61 slash commands**` to `**63 slash commands**`

2. **Line 15** — Key capabilities bullet:
   Change `**16 built-in agents**` to `**18 built-in agents**`

3. **Line 76** — Agent roster intro:
   Change `GSD also ships 16 built-in agents activated by the execution engine:` to `GSD also ships 18 built-in agents activated by the execution engine:`

4. **Line 78** — The agent list. The current list is a single line of comma-separated agent names. Add `gsd-dependency-auditor` and `gsd-ecosystem-auditor` in alphabetical position. The updated list should be:
   `gsd-advisor-researcher`, `gsd-assumptions-analyzer`, `gsd-codebase-mapper`, `gsd-debugger`, `gsd-dependency-auditor`, `gsd-ecosystem-auditor`, `gsd-executor`, `gsd-planner`, `gsd-research-orchestrator`, `gsd-research-synthesizer`, `gsd-roadmapper`, `gsd-security-guardian`, `gsd-ui-auditor`, `gsd-ui-checker`, `gsd-ui-researcher`, `gsd-user-profiler`, `gsd-validator-hub`, `gsd-verifier`

These are the only changes. Do not modify any other content.
  </action>
  <verify>
    <automated>grep -c "63 slash commands" CLAUDE.md && grep -c "18 built-in agents" CLAUDE.md && grep "gsd-dependency-auditor" CLAUDE.md && grep "gsd-ecosystem-auditor" CLAUDE.md</automated>
  </verify>
  <acceptance_criteria>
    - CLAUDE.md contains "**63 slash commands**"
    - CLAUDE.md contains "**18 built-in agents**"
    - CLAUDE.md contains "GSD also ships 18 built-in agents"
    - CLAUDE.md contains "gsd-dependency-auditor"
    - CLAUDE.md contains "gsd-ecosystem-auditor"
    - No instances of "16 built-in agents" or "61 slash commands" remain
  </acceptance_criteria>
  <done>CLAUDE.md reflects 18 agents, 63 commands, and lists all 18 agents by name including the two additions</done>
</task>

<task type="auto">
  <name>Task 3: Fix hardcoded path in crew.md</name>
  <files>commands/gsd/crew.md</files>
  <read_first>
    - commands/gsd/crew.md (current content, especially line 33 with hardcoded path)
  </read_first>
  <action>
On line 33 of commands/gsd/crew.md, the current text reads:

```
**Agent directory:** Read all .md files in the project's `agents/` directory. Fall back to `~/.claude/agents/` or `~/projects/Pete-Gets-Shit-Done/agents/` if no local agents/ exists.
```

Replace the entire line with:

```
**Agent directory:** Read all .md files in the project's `agents/` directory. Fall back to `~/.claude/agents/` if no local agents/ exists. For GSD plugin agents, resolve via the get-shit-done plugin install path (the directory containing this skill file).
```

This removes the hardcoded `~/projects/Pete-Gets-Shit-Done/agents/` path and replaces it with a portable reference to the plugin's own install location.
  </action>
  <verify>
    <automated>grep -c "Pete-Gets-Shit-Done" commands/gsd/crew.md | grep "^0$"</automated>
  </verify>
  <acceptance_criteria>
    - commands/gsd/crew.md contains zero occurrences of the string "Pete-Gets-Shit-Done"
    - commands/gsd/crew.md line 33 references "get-shit-done plugin install path" or similar portable resolution
    - The rest of the file is unchanged
  </acceptance_criteria>
  <done>crew.md uses portable path resolution with no hardcoded user-specific paths</done>
</task>

</tasks>

<verification>
- `grep -c "63 commands" README.md` returns at least 3 (table cell, heading, prose)
- `grep -c "18 agents" README.md` returns at least 2 (table row, "What You Get" table)
- `grep "audit-agents\|audit-deps" README.md | wc -l` returns at least 2
- `grep -c "63 slash commands" CLAUDE.md` returns 1
- `grep -c "18 built-in agents" CLAUDE.md` returns 2
- `grep -c "Pete-Gets-Shit-Done" commands/gsd/crew.md` returns 0
- `npm test` passes (no behavior changes, docs only)
</verification>

<success_criteria>
- README.md and CLAUDE.md both report 18 agents and 63 commands
- The two new commands appear in the README command table in correct alphabetical order
- The two new agents appear in the CLAUDE.md roster
- crew.md contains no hardcoded user-specific paths
- All existing tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/46-housekeeping/46-01-SUMMARY.md`
</output>
