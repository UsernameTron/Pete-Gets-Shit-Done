---
phase: 53-daily-dashboard
plan: 02
type: execute
wave: 2
depends_on:
  - 53-01
files_modified:
  - get-shit-done/commands/gsd/daily.md
  - get-shit-done/workflows/daily.md
autonomous: true
requirements:
  - DAILY-01
  - DAILY-03
must_haves:
  truths:
    - "/gsd:daily is a recognized command that routes to the daily workflow"
    - "The daily workflow calls daily.cjs functions and prints formatted output"
    - "The workflow completes in under 2 seconds for a typical project"
  artifacts:
    - path: "get-shit-done/commands/gsd/daily.md"
      provides: "Slash command definition for /gsd:daily"
      contains: "user-invocable: true"
    - path: "get-shit-done/workflows/daily.md"
      provides: "Workflow that gathers state, determines next action, formats and prints dashboard"
      contains: "daily.cjs"
  key_links:
    - from: "get-shit-done/commands/gsd/daily.md"
      to: "get-shit-done/workflows/daily.md"
      via: "Route to: @get-shit-done/workflows/daily.md"
      pattern: "workflows/daily.md"
    - from: "get-shit-done/workflows/daily.md"
      to: "get-shit-done/bin/lib/daily.cjs"
      via: "node -e require().gatherDailyState()"
      pattern: "daily\\.cjs"
---

<objective>
Create the /gsd:daily command and workflow that wire the daily.cjs module into the GSD command surface. The command definition makes `/gsd:daily` user-invocable. The workflow calls gatherDailyState + determineNextAction + formatDashboard and prints the result.

Purpose: This is the user-facing layer. Plan 01 built the engine; this plan connects it to `/gsd:daily`.
Output: Command definition + workflow file. User can run `/gsd:daily` and see their dashboard.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/53-daily-dashboard/53-01-SUMMARY.md

@get-shit-done/commands/gsd/checkpoint.md
@get-shit-done/workflows/checkpoint.md
@get-shit-done/bin/lib/daily.cjs

<interfaces>
<!-- From daily.cjs (created in Plan 01) -->
From get-shit-done/bin/lib/daily.cjs:
```javascript
function gatherDailyState(planningDir) → {
  _source: 'checkpoint'|'state'|'none',
  milestone: string,
  phase: number,
  phase_name: string,
  branch: string,
  commit: string,
  plans: { total, completed[], active, pending[] },
  _stale: boolean,
  _ageHours: number,
  _gitDirty: boolean,
  next_action: string,
  context_note: string
}

function determineNextAction(state) → string  // e.g. '/gsd:execute-phase 52'

function formatDashboard(state) → string  // multiline formatted dashboard
```

<!-- Pattern to follow: see get-shit-done/commands/gsd/checkpoint.md for frontmatter format -->
<!-- Key fields: name (gsd:NAME), description, user-invocable: true -->
<!-- Body: heading, When to Use, Usage, What It Shows/Captures, Instructions with Route to: -->

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create /gsd:daily command definition</name>
  <files>get-shit-done/commands/gsd/daily.md</files>
  <read_first>
    get-shit-done/commands/gsd/checkpoint.md
  </read_first>
  <action>
Create get-shit-done/commands/gsd/daily.md following the exact same frontmatter pattern as checkpoint.md.

The file must have YAML frontmatter (between triple-dash delimiters) with these exact fields:
- name: gsd:daily
- description: Morning briefing dashboard showing milestone progress, phase status, plan completion, git state, warnings, and the exact next GSD command. Run at session start for instant orientation.
- user-invocable: true

The body must contain these sections (use checkpoint.md as the structural template):

1. Heading: `# /gsd:daily`
2. One-line description: "One-command morning briefing. Shows where you are, what happened, and exactly what to do next."
3. `## When to Use` section with bullets: at session start, after context reset, returning to project after time away
4. `## Usage` section showing the bare command `/gsd:daily` — no arguments, reads CHECKPOINT.json first, falls back to STATE.md
5. `## What It Shows` section with bullets: Milestone and phase, Plan progress (completed/active/pending), Git state (branch + dirty warning), Checkpoint freshness (stale warning if >24h), Next action (exact /gsd: command)
6. `## Instructions` section: `Route to: @get-shit-done/workflows/daily.md`
  </action>
  <verify>
    <automated>test -f get-shit-done/commands/gsd/daily.md && grep -q "user-invocable: true" get-shit-done/commands/gsd/daily.md && grep -q "gsd:daily" get-shit-done/commands/gsd/daily.md && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - get-shit-done/commands/gsd/daily.md exists
    - File contains frontmatter with `name: gsd:daily`
    - File contains `user-invocable: true`
    - File contains `Route to: @get-shit-done/workflows/daily.md`
    - File contains `description:` with non-empty value
  </acceptance_criteria>
  <done>Command definition created. /gsd:daily is a recognized GSD command.</done>
</task>

<task type="auto">
  <name>Task 2: Create daily workflow</name>
  <files>get-shit-done/workflows/daily.md</files>
  <read_first>
    get-shit-done/workflows/checkpoint.md
    get-shit-done/workflows/progress.md
    get-shit-done/bin/lib/daily.cjs
  </read_first>
  <action>
Create get-shit-done/workflows/daily.md. Follow the same XML structure as checkpoint.md (trigger, purpose, process with named steps, error_handling).

**Content:**

```xml
<trigger>
Use when:
- User runs /gsd:daily
- Session start orientation
- After context reset to re-orient
- Returning to project after time away
</trigger>

<purpose>
Print a formatted dashboard showing milestone progress, phase status, plan completion,
git state, warnings (dirty tree, stale checkpoint), and the exact next GSD command.
Completes in under 2 seconds. No side effects — read-only operation.
</purpose>

<process>

<step name="gather_state">
Call gatherDailyState to collect all dashboard data from CHECKPOINT.json or STATE.md fallback.

```bash
DAILY_STATE=$(node -e "
  const { gatherDailyState } = require('$HOME/.claude/get-shit-done/bin/lib/daily.cjs');
  const state = gatherDailyState('.planning');
  console.log(JSON.stringify(state));
")
```

Parse the JSON result. The `_source` field tells you where data came from:
- `checkpoint` — fresh checkpoint data
- `state` — STATE.md fallback (no checkpoint present)
- `none` — no project state found
</step>

<step name="determine_next">
Call determineNextAction to get the exact next command.

```bash
NEXT_CMD=$(node -e "
  const { determineNextAction } = require('$HOME/.claude/get-shit-done/bin/lib/daily.cjs');
  const state = JSON.parse(process.env.DAILY_STATE);
  console.log(determineNextAction(state));
" DAILY_STATE="$DAILY_STATE")
```
</step>

<step name="format_and_print">
Call formatDashboard to get the formatted output string.

```bash
DASHBOARD=$(node -e "
  const { formatDashboard } = require('$HOME/.claude/get-shit-done/bin/lib/daily.cjs');
  const state = JSON.parse(process.env.DAILY_STATE);
  console.log(formatDashboard(state));
" DAILY_STATE="$DAILY_STATE")
```

Print the dashboard output to the user verbatim. Do not add extra commentary — the dashboard is self-contained.

After the dashboard, add a single line:

```
<sub>Data source: {_source} | Run `/gsd:checkpoint` to refresh</sub>
```
</step>

</process>

<error_handling>
**If .planning/ directory does not exist:**
Report: "No .planning/ directory found. Run /gsd:new-project to initialize."

**If node fails (daily.cjs not found):**
Report: "daily.cjs module not found. Ensure get-shit-done plugin is installed."

**If JSON parsing fails:**
Fall back to a minimal report:
"Daily dashboard could not parse state. Run /gsd:progress for detailed status."
</error_handling>
```

The workflow is intentionally lightweight — all logic is in daily.cjs. The workflow just calls three functions and prints the result.
  </action>
  <verify>
    <automated>test -f get-shit-done/workflows/daily.md && grep -q "daily.cjs" get-shit-done/workflows/daily.md && grep -q "gatherDailyState" get-shit-done/workflows/daily.md && grep -q "formatDashboard" get-shit-done/workflows/daily.md && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - get-shit-done/workflows/daily.md exists
    - File contains `gatherDailyState` call via node -e
    - File contains `determineNextAction` call via node -e
    - File contains `formatDashboard` call via node -e
    - File contains `daily.cjs` reference
    - File contains error_handling section
    - File contains trigger, purpose, and process sections
  </acceptance_criteria>
  <done>Workflow created. /gsd:daily routes through command definition to workflow, calls daily.cjs, prints dashboard. Full test suite green.</done>
</task>

</tasks>

<verification>
- `test -f get-shit-done/commands/gsd/daily.md` — command exists
- `test -f get-shit-done/workflows/daily.md` — workflow exists
- `grep -q "user-invocable: true" get-shit-done/commands/gsd/daily.md` — command is invocable
- `grep -q "daily.cjs" get-shit-done/workflows/daily.md` — workflow calls the module
- `npm test` — full suite green after integration
</verification>

<success_criteria>
- /gsd:daily is a user-invocable command that routes to the daily workflow
- The workflow calls all three daily.cjs functions (gather, determine, format)
- No side effects — purely read-only operation
- Full test suite stays green
</success_criteria>

<output>
After completion, create `.planning/phases/53-daily-dashboard/53-02-SUMMARY.md`
</output>
