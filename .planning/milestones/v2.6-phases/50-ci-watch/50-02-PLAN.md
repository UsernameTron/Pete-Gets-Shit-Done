---
phase: 50-ci-watch
plan: 02
type: execute
wave: 2
depends_on:
  - 50-01
files_modified:
  - commands/gsd/ci-watch.md
  - tests/ci-patterns.test.cjs
autonomous: true
requirements:
  - CIWATCH-01
  - CIWATCH-02
  - CIWATCH-03
  - CIWATCH-04
  - CIWATCH-05
must_haves:
  truths:
    - "User can invoke /gsd:ci-watch and the command dispatches to the ci-watch workflow"
    - "The slash command file follows GSD command conventions (frontmatter, objective, execution_context, process)"
    - "CI pattern matching logic is tested: known patterns match, unknown input falls through"
  artifacts:
    - path: "commands/gsd/ci-watch.md"
      provides: "GSD slash command entry point for ci-watch"
      contains: "gsd:ci-watch"
    - path: "tests/ci-patterns.test.cjs"
      provides: "Tests for CI failure pattern library"
      contains: "ci-patterns"
  key_links:
    - from: "commands/gsd/ci-watch.md"
      to: "get-shit-done/workflows/ci-watch.md"
      via: "execution_context reference"
      pattern: "workflows/ci-watch"
    - from: "tests/ci-patterns.test.cjs"
      to: "lib/ci-patterns.json"
      via: "require('./lib/ci-patterns.json')"
      pattern: "ci-patterns.json"
---

<objective>
Create the GSD slash command file that wires /gsd:ci-watch to the workflow, and write tests validating the CI failure pattern library.

Purpose: The command file makes ci-watch invokable as a slash command. The tests ensure the pattern library's regexes actually match the failure signatures they claim to detect, and that unknown input does not false-positive.

Output: `commands/gsd/ci-watch.md` (slash command) and `tests/ci-patterns.test.cjs` (pattern tests).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@commands/gsd/do.md
@commands/gsd/quick.md
@lib/ci-patterns.json
@get-shit-done/workflows/ci-watch.md
</context>

<interfaces>
<!-- Existing GSD command frontmatter pattern (from commands/gsd/do.md): -->
```yaml
---
name: gsd:do
description: Route freeform text to the right GSD command automatically
argument-hint: "<description of what you want to do>"
allowed-tools:
  - Read
  - Bash
  - AskUserQuestion
---
```

<!-- CI patterns JSON structure (from lib/ci-patterns.json, created by Plan 01): -->
```json
[
  {
    "source": "<regex>",
    "flags": "i",
    "category": "<category>",
    "description": "<human description>",
    "fix": "<fix suggestion>"
  }
]
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create ci-watch slash command</name>
  <files>commands/gsd/ci-watch.md</files>
  <read_first>
    - commands/gsd/do.md (frontmatter pattern — name, description, argument-hint, allowed-tools)
    - commands/gsd/quick.md (example of command with flags and execution_context reference)
    - get-shit-done/workflows/ci-watch.md (the workflow this command dispatches to)
  </read_first>
  <action>
Create `commands/gsd/ci-watch.md` with this exact structure:

**Frontmatter:**
```yaml
---
name: gsd:ci-watch
description: Poll GitHub Actions for the current branch, stream live status, diagnose failures, and suggest fixes
argument-hint: "[--interval <seconds>]"
allowed-tools:
  - Read
  - Bash
  - Grep
  - Glob
---
```

**Body:**

```xml
<objective>
Monitor GitHub Actions CI runs for the current branch in real time. Polls until all runs reach a terminal state (success, failure, cancelled). On failure, fetches logs, diagnoses the root cause using a pattern library, and suggests concrete fixes.

Use when you want to watch CI without leaving Claude Code. Runs continuously until complete or interrupted with Ctrl+C.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/ci-watch.md
</execution_context>

<context>
$ARGUMENTS
</context>

<process>
Execute the ci-watch workflow from @~/.claude/get-shit-done/workflows/ci-watch.md end-to-end.

Parse $ARGUMENTS for:
- `--interval <N>` flag: override default 15-second polling interval with N seconds

Pass any parsed configuration to the workflow steps.
</process>
```

The command file must:
- Use `name: gsd:ci-watch` exactly
- Reference `@~/.claude/get-shit-done/workflows/ci-watch.md` in execution_context
- Include Read, Bash, Grep, Glob in allowed-tools (needs Bash for gh CLI, Read for pattern file, Grep for log analysis)
- NOT include Write or Edit (this command is read-only / diagnostic)
- Have a clear description mentioning polling, diagnosis, and fix suggestions
  </action>
  <verify>
    <automated>grep -q "name: gsd:ci-watch" commands/gsd/ci-watch.md && grep -q "workflows/ci-watch.md" commands/gsd/ci-watch.md && grep -q "allowed-tools" commands/gsd/ci-watch.md && echo "OK: command file valid"</automated>
  </verify>
  <acceptance_criteria>
    - commands/gsd/ci-watch.md exists
    - File contains `name: gsd:ci-watch` in frontmatter
    - File contains `description:` field in frontmatter
    - File contains `argument-hint:` field mentioning --interval
    - File contains `allowed-tools:` with at least Read and Bash
    - File does NOT contain Write or Edit in allowed-tools
    - File contains `<execution_context>` referencing `workflows/ci-watch.md`
    - File contains `<objective>` tag with description of the command
    - File contains `<process>` tag with argument parsing instructions
  </acceptance_criteria>
  <done>Slash command file exists, follows GSD command conventions, dispatches to ci-watch workflow, accepts --interval flag.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create CI pattern library tests</name>
  <files>tests/ci-patterns.test.cjs</files>
  <read_first>
    - lib/ci-patterns.json (the patterns being tested)
    - tests/security.test.cjs (existing test pattern — node:test + node:assert usage in this project)
  </read_first>
  <behavior>
    - Test 1: ci-patterns.json loads as valid JSON array
    - Test 2: Every pattern has required fields (source, flags, category, description, fix)
    - Test 3: Every pattern's source compiles as a valid RegExp with its flags
    - Test 4: EXDEV pattern matches "Error: EXDEV: cross-device link not permitted"
    - Test 5: missing-module pattern matches "Error: Cannot find module './foo'"
    - Test 6: missing-module pattern matches "Error [MODULE_NOT_FOUND]: Cannot find module"
    - Test 7: sha-pin pattern matches "Unable to resolve action `actions/checkout@deadbeef`, uses: actions/checkout@deadbeef"
    - Test 8: node-version pattern matches "SyntaxError: Unexpected token 'export'"
    - Test 9: test-failure pattern matches "not ok 5 - should return correct value"
    - Test 10: test-failure pattern matches "FAIL tests/unit/core.test.cjs"
    - Test 11: exit-code pattern matches "Process exited with code 1"
    - Test 12: No pattern matches "All tests passed successfully" (negative test)
    - Test 13: No pattern matches "Build completed in 45s" (negative test)
    - Test 14: category field is unique across all patterns (no duplicate categories)
  </behavior>
  <action>
Create `tests/ci-patterns.test.cjs` using `node:test` and `node:assert` (the project's established test framework — no mocha, no jest).

Structure:
```javascript
const { describe, it } = require('node:test');
const assert = require('node:assert/strict');
const path = require('node:path');

// Load patterns from repo root
const patterns = require(path.join(__dirname, '../lib/ci-patterns.json'));
```

Helper function to find matching pattern:
```javascript
function findMatch(text) {
  return patterns.find(p => new RegExp(p.source, p.flags).test(text));
}
```

Test groups:
1. "schema validation" — tests 1-3 (structure, fields, regex compilation)
2. "pattern matching — positive" — tests 4-11 (each known failure signature matches)
3. "pattern matching — negative" — tests 12-13 (clean output does not match)
4. "uniqueness" — test 14 (no duplicate categories)

For each positive match test, assert that `findMatch(text)` returns a pattern with the expected category. For negative tests, assert `findMatch(text)` returns `undefined`.
  </action>
  <verify>
    <automated>node --test tests/ci-patterns.test.cjs</automated>
  </verify>
  <acceptance_criteria>
    - tests/ci-patterns.test.cjs exists
    - File uses `require('node:test')` and `require('node:assert/strict')`
    - File loads lib/ci-patterns.json via require
    - File contains at least 14 test cases
    - All tests pass when run with `node --test tests/ci-patterns.test.cjs`
    - Tests cover: schema validation, positive matching for all 6 categories, negative matching for clean output, category uniqueness
  </acceptance_criteria>
  <done>All 14+ tests pass. Pattern library validated: every regex compiles, every seeded pattern matches its target failure signature, clean CI output does not false-positive.</done>
</task>

</tasks>

<verification>
- `commands/gsd/ci-watch.md` exists with correct frontmatter and execution_context
- `tests/ci-patterns.test.cjs` passes all tests
- Full test suite still passes: `npm test`
- Slash command references workflow file correctly
- No files from Plan 01 were modified (lib/ci-patterns.json, get-shit-done/workflows/ci-watch.md)
</verification>

<success_criteria>
/gsd:ci-watch is wirable as a GSD slash command. The CI failure pattern library has comprehensive test coverage proving each seeded pattern detects its target failure. The full test suite passes with the new test file included.
</success_criteria>

<output>
After completion, create `.planning/phases/50-ci-watch/50-02-SUMMARY.md`
</output>
