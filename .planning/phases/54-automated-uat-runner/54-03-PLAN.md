---
phase: 54-automated-uat-runner
plan: 03
type: execute
wave: 3
depends_on:
  - 54-01
  - 54-02
files_modified:
  - get-shit-done/workflows/verify-work.md
  - get-shit-done/bin/gsd-tools.cjs
autonomous: true
requirements:
  - UAT-03
  - UAT-07
  - UAT-10

must_haves:
  truths:
    - "verify-work.md has a Step 0 that calls runAutomatedUAT before any conversational testing"
    - "verify-work.md presents automated results and only routes unmatched must_haves to conversational UAT"
    - "gsd-tools.cjs registers 'uat run-automated' subcommand that routes to uat-runner.cjs"
    - "npm test passes with 0 failures after all integration changes"
    - "All runner commands are read-only — verify-work Step 0 does not write files"
  artifacts:
    - path: "get-shit-done/workflows/verify-work.md"
      provides: "Updated workflow with Step 0 automated UAT before conversational UAT"
      contains: "uat-runner.cjs"
    - path: "get-shit-done/bin/gsd-tools.cjs"
      provides: "CLI dispatcher with uat run-automated subcommand"
      contains: "run-automated"
  key_links:
    - from: "get-shit-done/workflows/verify-work.md"
      to: "get-shit-done/bin/lib/uat-runner.cjs"
      via: "Step 0 calls runAutomatedUAT via gsd-tools CLI"
      pattern: "uat-runner|run-automated"
    - from: "get-shit-done/bin/gsd-tools.cjs"
      to: "get-shit-done/bin/lib/uat-runner.cjs"
      via: "require and dispatch for uat run-automated subcommand"
      pattern: "uat-runner"
---

<objective>
Wire the UAT runner into the verify-work workflow and CLI. Add Step 0 to verify-work.md that runs automated must_have assertions before any conversational UAT. Register the CLI subcommand in gsd-tools.cjs. Confirm full test suite green.

Purpose: This connects the engine (Plans 01-02) to the user-facing workflow. After this plan, running /gsd:verify-work on any phase will automatically execute must_have assertions before asking the user anything.

Output: Updated verify-work.md with Step 0, updated gsd-tools.cjs with uat run-automated command, full test suite green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/54-automated-uat-runner/54-01-SUMMARY.md
@.planning/phases/54-automated-uat-runner/54-02-SUMMARY.md

<interfaces>
From Plan 02 (uat-runner.cjs):

  const { runAutomatedUAT, formatUATResults } = require('./lib/uat-runner.cjs');
  // runAutomatedUAT(planPaths) returns { passed: [...], failed: [...], manual: [...], total: N }
  // formatUATResults(results) returns formatted string

From existing gsd-tools.cjs dispatcher pattern (line ~740):

  case 'uat': {
    const sub = args[1];
    const uat = require('./lib/uat.cjs');
    if (sub === 'render-checkpoint') {
      uat.cmdRenderCheckpoint(cwd, options, raw);
    } else {
      error('Unknown uat subcommand. Available: render-checkpoint');
    }
    break;
  }

From existing verify-work.md step ordering:
  - initialize
  - check_active_session
  - schema_check
  - find_summaries
  - extract_tests
  - create_uat_file
  - present_test (conversational UAT loop)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add uat run-automated subcommand to gsd-tools.cjs</name>
  <files>get-shit-done/bin/gsd-tools.cjs</files>
  <read_first>
    - get-shit-done/bin/gsd-tools.cjs (lines 734-748 — existing uat case block in dispatcher)
    - get-shit-done/bin/lib/uat-runner.cjs (Plan 02 output — runAutomatedUAT, formatUATResults exports)
    - get-shit-done/bin/lib/core.cjs (planningDir function — to resolve phase directory)
  </read_first>
  <action>
In get-shit-done/bin/gsd-tools.cjs, modify the existing 'uat' case block (around line 740) to add a 'run-automated' subcommand alongside the existing 'render-checkpoint'.

The new subcommand:
1. Reads the --phase option (required)
2. Finds the phase directory using the same findPhaseInternal pattern used elsewhere in the dispatcher
3. Lists all *-PLAN.md files in the phase directory
4. Builds absolute paths for each plan file
5. Calls runAutomatedUAT(planPaths) from uat-runner.cjs
6. If --raw flag: outputs JSON result
7. If not --raw: outputs formatUATResults(results)

Concrete changes to the 'uat' case:

  case 'uat': {
    const sub = args[1];
    const uat = require('./lib/uat.cjs');
    if (sub === 'render-checkpoint') {
      uat.cmdRenderCheckpoint(cwd, options, raw);
    } else if (sub === 'run-automated') {
      const runner = require('./lib/uat-runner.cjs');
      const phaseArg = options.phase;
      if (!phaseArg) { error('--phase required for uat run-automated'); }
      const phaseInfo = findPhaseInternal(cwd, phaseArg);
      if (!phaseInfo || !phaseInfo.found) { error('Phase not found: ' + phaseArg); }
      const phaseDir = path.join(cwd, phaseInfo.directory);
      const planFiles = fs.readdirSync(phaseDir)
        .filter(f => f.endsWith('-PLAN.md'))
        .map(f => path.join(phaseDir, f));
      const results = runner.runAutomatedUAT(planFiles);
      if (raw) {
        output(results, raw);
      } else {
        output(null, false, runner.formatUATResults(results));
      }
    } else {
      error('Unknown uat subcommand. Available: render-checkpoint, run-automated');
    }
    break;
  }

Also update the help text comment near line 71 to include:
  uat run-automated --phase <N>   Run automated must_have assertions for a phase

Note: findPhaseInternal is already imported at the top of gsd-tools.cjs. fs and path are also already imported. Verify before adding duplicate imports.

Commit: feat(54-03): add uat run-automated CLI subcommand
  </action>
  <verify>
    <automated>cd /Users/cpconnor/projects/Pete-Gets-Shit-Done/.claude/worktrees/agent-a64292f2 && node get-shit-done/bin/gsd-tools.cjs uat run-automated --phase 53 --raw 2>&1 | head -5</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "run-automated" get-shit-done/bin/gsd-tools.cjs returns at least 2 (case match + help text)
    - node get-shit-done/bin/gsd-tools.cjs uat run-automated --phase 53 --raw exits 0 and outputs JSON with passed/failed/manual keys
    - node get-shit-done/bin/gsd-tools.cjs uat run-automated --raw 2>&1 returns an error about --phase being required
    - The error message for unknown subcommands now lists both render-checkpoint and run-automated
  </acceptance_criteria>
  <done>CLI subcommand registered and functional. Can run uat run-automated --phase N from command line.</done>
</task>

<task type="auto">
  <name>Task 2: Add Step 0 automated UAT to verify-work.md workflow</name>
  <files>get-shit-done/workflows/verify-work.md</files>
  <read_first>
    - get-shit-done/workflows/verify-work.md (full file — understand current step ordering and conventions)
    - get-shit-done/bin/lib/uat-runner.cjs (Plan 02 output — result structure for display logic)
    - .planning/v27-session-continuity-milestone-plan.md (lines 686-699 — verify-work integration spec)
  </read_first>
  <action>
Add a new step to verify-work.md BETWEEN the "schema_check" step and the "find_summaries" step. This new step runs automated must_have assertions before any conversational UAT begins.

Insert this step after the schema_check closing tag and before the find_summaries step:

  <step name="automated_uat" priority="before-conversational">
  **Automated must_have verification (runs for all modes except schema-only):**

  Before presenting any conversational tests, run automated assertions against plan must_haves.

  This step is fully automated — no user interaction required.

  1. Find all PLAN.md files for this phase:

  ```bash
  PLAN_FILES=$(ls "$phase_dir"/*-PLAN.md 2>/dev/null)
  ```

  2. Run automated UAT via CLI:

  ```bash
  UAT_RESULTS=$(node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" uat run-automated --phase "$PHASE_ARG" --raw)
  if [[ "$UAT_RESULTS" == @file:* ]]; then UAT_RESULTS=$(cat "${UAT_RESULTS#@file:}"); fi
  ```

  3. Parse the JSON results. Extract passed, failed, and manual counts.

  4. Display formatted results:

  ```bash
  node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" uat run-automated --phase "$PHASE_ARG"
  ```

  5. Routing logic:

  - **If ALL must_haves passed automatically (failed=0, manual=0):**
    Report: "All automated must_have checks passed. Phase verification complete."
    Skip conversational UAT entirely. Go to complete_session.

  - **If some failed (failed > 0):**
    Present failures with expected/actual/command details.
    Report: "{N} automated checks failed. Fix these before continuing."
    Do NOT proceed to conversational UAT — failures must be resolved first.
    Go to diagnose_issues with the failure details.

  - **If some are manual (manual > 0, failed = 0):**
    Report: "{passed} automated checks passed. {manual} items need manual verification."
    Continue to find_summaries, but ONLY present the manual items for conversational UAT.
    Skip any SUMMARY-derived test that overlaps with an already-passed automated check.

  - **If no must_haves found (total = 0):**
    Report: "No must_have assertions found in plan frontmatter. Falling through to conversational UAT."
    Continue to find_summaries as normal.

  **Read-only guarantee:** This step runs shell commands generated by the pattern registry. All commands are read-only (file existence checks, grep, diff, node -p). No file writes occur during automated verification. The step itself only reads PLAN.md files and executes read-only assertions.
  </step>

Also update the success_criteria section at the bottom of verify-work.md to include:
  - [ ] Automated must_have assertions run before conversational UAT
  - [ ] Only manual (unmatched) must_haves are presented conversationally
  - [ ] Failed automated checks block conversational UAT and trigger diagnosis

Commit: feat(54-03): add automated UAT step to verify-work workflow
  </action>
  <verify>
    <automated>cd /Users/cpconnor/projects/Pete-Gets-Shit-Done/.claude/worktrees/agent-a64292f2 && grep -c "automated_uat" get-shit-done/workflows/verify-work.md</automated>
  </verify>
  <acceptance_criteria>
    - grep "automated_uat" get-shit-done/workflows/verify-work.md returns at least 1 match
    - grep "uat run-automated" get-shit-done/workflows/verify-work.md returns at least 1 match
    - grep "uat-runner" get-shit-done/workflows/verify-work.md returns at least 1 match
    - The automated_uat step appears BEFORE find_summaries in the file
    - The step contains routing logic for all four cases: all-pass, some-fail, some-manual, no-must-haves
    - success_criteria section includes automated must_have checks
  </acceptance_criteria>
  <done>verify-work.md updated with Step 0 automated UAT. The workflow now runs must_have assertions before conversational testing.</done>
</task>

<task type="auto">
  <name>Task 3: Run full test suite and confirm green</name>
  <files></files>
  <read_first>
    - get-shit-done/bin/gsd-tools.cjs (confirm the uat case block is correct after Task 1 edits)
    - get-shit-done/workflows/verify-work.md (confirm the step ordering is correct after Task 2 edits)
  </read_first>
  <action>
Run the full GSD test suite to confirm nothing is broken by the new modules and integrations:

1. Run: npm test
   - ALL existing tests must still pass
   - The new uat-patterns.test.cjs and uat-runner.test.cjs must pass
   - Zero test failures allowed

2. If any test fails:
   - Read the failure output
   - Diagnose root cause
   - Fix the issue in the relevant file (uat-patterns.cjs, uat-runner.cjs, or gsd-tools.cjs)
   - Re-run npm test
   - Repeat until 0 failures

3. Run a smoke test of the CLI integration:
   - node get-shit-done/bin/gsd-tools.cjs uat run-automated --phase 53 --raw
   - Verify it returns JSON with passed/failed/manual arrays
   - Verify it processes must_haves from the phase 53 PLAN.md files

4. Verify read-only guarantee: grep all generated commands in uat-patterns.cjs for write operators:
   - node -e "const p = require('./get-shit-done/bin/lib/uat-patterns.cjs'); const cmds = p.patterns.map(pt => pt.generate(['','test','42']).command); console.log(cmds.some(c => /[>](?!\\s*\\/dev\\/null)|\\btee\\b|\\brm\\b/.test(c)) ? 'FAIL: write ops found' : 'PASS: read-only')"

Commit: test(54-03): full test suite green after UAT runner integration
  </action>
  <verify>
    <automated>cd /Users/cpconnor/projects/Pete-Gets-Shit-Done/.claude/worktrees/agent-a64292f2 && npm test 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - npm test exits with code 0
    - Output contains "# fail 0" (zero test failures)
    - node get-shit-done/bin/gsd-tools.cjs uat run-automated --phase 53 --raw exits 0
    - The smoke test JSON output contains "passed", "failed", "manual" keys
  </acceptance_criteria>
  <done>Full test suite green. All UAT-* requirements satisfied. Phase 54 implementation complete.</done>
</task>

</tasks>

<verification>
- npm test — 0 failures
- node get-shit-done/bin/gsd-tools.cjs uat run-automated --phase 53 --raw — exits 0, returns JSON
- grep "automated_uat" get-shit-done/workflows/verify-work.md — found
- grep "run-automated" get-shit-done/bin/gsd-tools.cjs — found
</verification>

<success_criteria>
- verify-work.md runs automated must_have assertions BEFORE conversational UAT
- Only unmatched must_haves are presented to the user for manual verification
- CLI subcommand uat run-automated works from command line
- Full test suite (npm test) passes with 0 failures
- All commands executed during automated verification are read-only
</success_criteria>

<output>
After completion, create .planning/phases/54-automated-uat-runner/54-03-SUMMARY.md
</output>
