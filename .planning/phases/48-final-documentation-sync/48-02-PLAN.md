---
phase: 48-final-documentation-sync
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - CLAUDE.md
  - Petes-Get-Shit-Done-Coding-Automation/CLAUDE.md
  - Petes-Get-Shit-Done-Coding-Automation/references/agent-design-patterns.md
  - Petes-Get-Shit-Done-Coding-Automation/references/frontmatter-reference.md
autonomous: true
requirements: [D-01, D-04, D-05]

must_haves:
  truths:
    - "Both CLAUDE.md files show 63 commands, 17 built-in agents, 45 skills"
    - "Both CLAUDE.md files show 479 test suites, 2490 assertions, 90.79% coverage"
    - "Both CLAUDE.md files list exactly 17 agent names (not 16 or 18)"
    - "Reference docs are factually current with no stale agent counts"
    - "CLAUDE.md is optimized for Claude Code AI consumption per D-04"
  artifacts:
    - path: "CLAUDE.md"
      provides: "Outer repo CLAUDE.md with correct counts"
      contains: "17 built-in agents"
    - path: "Petes-Get-Shit-Done-Coding-Automation/CLAUDE.md"
      provides: "Inner repo CLAUDE.md with correct counts"
      contains: "17 built-in agents"
    - path: "Petes-Get-Shit-Done-Coding-Automation/references/agent-design-patterns.md"
      provides: "Updated agent patterns reference"
    - path: "Petes-Get-Shit-Done-Coding-Automation/references/frontmatter-reference.md"
      provides: "Updated frontmatter reference"
  key_links:
    - from: "CLAUDE.md (outer)"
      to: "D-05 counts"
      via: "inline references"
      pattern: "17 built-in agents|63 slash commands"
    - from: "CLAUDE.md (inner)"
      to: "D-05 counts"
      via: "inline references"
      pattern: "17 built-in agents|63 slash commands"
---

<objective>
Update both CLAUDE.md files (outer and inner repo) and both reference docs to match D-05 verified counts. CLAUDE.md files are machine-consumed by Claude Code — optimize for AI comprehension per D-04.

Purpose: CLAUDE.md is the primary instruction file Claude Code reads at session start. Stale counts cause Claude to give wrong information to users. Reference docs inform agent design decisions.
Output: 4 files with accurate counts and current information.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/48-final-documentation-sync/48-CONTEXT.md
@CLAUDE.md
@Petes-Get-Shit-Done-Coding-Automation/CLAUDE.md
@Petes-Get-Shit-Done-Coding-Automation/references/agent-design-patterns.md
@Petes-Get-Shit-Done-Coding-Automation/references/frontmatter-reference.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update both CLAUDE.md files with D-05 counts</name>
  <files>CLAUDE.md, Petes-Get-Shit-Done-Coding-Automation/CLAUDE.md</files>
  <read_first>
    - CLAUDE.md (outer repo — current state)
    - Petes-Get-Shit-Done-Coding-Automation/CLAUDE.md (inner repo — current state)
    - .planning/phases/48-final-documentation-sync/48-CONTEXT.md (D-05 verified counts)
  </read_first>
  <action>
Both CLAUDE.md files are nearly identical (256 lines each). Update both with the same changes.

**Outer repo CLAUDE.md** (`/Users/cpconnor/projects/Pete-Gets-Shit-Done/CLAUDE.md`):

1. Project Overview section — Key capabilities bullets:
   - "63 slash commands" — should already be correct, verify
   - "18 built-in agents" -> "17 built-in agents" (D-05 says 17)
   - "47+ Claude Code skills" -> "45 Claude Code skills" (D-05 says 45)
   - Update the agent name list parenthetical if it lists agent names

2. Tests and Coverage section:
   - "~472 test suites, ~2474 assertions" -> "479 test suites, 2,490 assertions" (remove tildes — these are verified counts now)

3. Deployed Agents section:
   - "GSD also ships 18 built-in agents" -> "GSD also ships 17 built-in agents"
   - The agent name list currently has 18 names. The correct 17 agents per D-05 are in `Petes-Get-Shit-Done-Coding-Automation/agents/`. Run `ls Petes-Get-Shit-Done-Coding-Automation/agents/*.md | wc -l` to confirm which 17 are current. Remove the one that was dropped (likely gsd-dependency-auditor or gsd-ecosystem-auditor — verify by listing the directory). Update the inline list to match exactly what is in the agents/ directory.

4. Skill and Plugin Architecture section:
   - "40+ Claude Desktop skills and 47+ Claude Code skills" -> "40+ Claude Desktop skills and 45 Claude Code skills"

5. Installed Plugin Inventory section (outer CLAUDE.md only — inner may not have this):
   - If it exists, update any agent/command/skill counts in the inventory table

**Inner repo CLAUDE.md** (`Petes-Get-Shit-Done-Coding-Automation/CLAUDE.md`):

Apply the exact same count changes as above. The inner file currently says "61 slash commands" and "16 built-in agents" — update those to 63 and 17 respectively. Also update:
   - "16 built-in agents" in the agent list to show 17 names
   - "47+ Claude Code skills" -> "45 Claude Code skills"
   - "~472 test suites, ~2474 assertions" -> "479 test suites, 2,490 assertions"

IMPORTANT: Both files MUST end up with identical counts. Cross-check after editing.
  </action>
  <verify>
    <automated>cd /Users/cpconnor/projects/Pete-Gets-Shit-Done && echo "=== Outer ===" && grep -n "18 built-in\|16 built-in\|47+\|~472\|~2474\|61 slash\|16 agents" CLAUDE.md; echo "=== Inner ===" && grep -n "18 built-in\|16 built-in\|47+\|~472\|~2474\|61 slash\|16 agents" Petes-Get-Shit-Done-Coding-Automation/CLAUDE.md; echo "STALE_CHECK_DONE"</automated>
  </verify>
  <acceptance_criteria>
    - grep "17 built-in agents" CLAUDE.md returns 1 match (outer)
    - grep "17 built-in agents" inner/CLAUDE.md returns 1 match (inner)
    - grep "63 slash commands" CLAUDE.md returns 1 match (outer)
    - grep "63 slash commands" inner/CLAUDE.md returns 1 match (inner)
    - grep "45 Claude Code skills" CLAUDE.md returns 1 match (outer)
    - grep "479 test suites" CLAUDE.md returns 1 match (outer)
    - grep "2,490 assertions" CLAUDE.md returns 1 match (outer)
    - grep "18 built-in\|16 built-in\|47+" CLAUDE.md returns 0 matches (stale removed, outer)
    - grep "18 built-in\|16 built-in\|47+\|61 slash" inner/CLAUDE.md returns 0 matches (stale removed, inner)
    - The agent name list in both files contains exactly 17 names (count backtick-delimited names)
  </acceptance_criteria>
  <done>Both CLAUDE.md files contain identical, D-05-verified counts. No stale counts remain.</done>
</task>

<task type="auto">
  <name>Task 2: Update reference docs per D-01</name>
  <files>Petes-Get-Shit-Done-Coding-Automation/references/agent-design-patterns.md, Petes-Get-Shit-Done-Coding-Automation/references/frontmatter-reference.md</files>
  <read_first>
    - Petes-Get-Shit-Done-Coding-Automation/references/agent-design-patterns.md (current state)
    - Petes-Get-Shit-Done-Coding-Automation/references/frontmatter-reference.md (current state)
    - .planning/phases/48-final-documentation-sync/48-CONTEXT.md (D-01: update existing reference docs, do NOT create new ones)
  </read_first>
  <action>
Per D-01: Update the 2 existing reference docs. Do NOT create new reference docs.

**agent-design-patterns.md** (102 lines):
- Review for any stale agent count references. If the file mentions "16 agents" or "15 agents" anywhere, update to 17.
- The file is pattern-focused (7 patterns), not count-focused, so it may not need count changes.
- If the Tool Profiles appendix references stale tool names or patterns, verify they still apply.
- The GSD Archetype Tagging section references "a GSD agent" generically — this should be fine.
- Do NOT restructure. Only fix factual inaccuracies.

**frontmatter-reference.md** (116 lines):
- Review for any stale references. The file describes YAML frontmatter schema.
- If it references specific agent counts, update to 17.
- If it references stale tool names or capabilities, update.
- The GSD Additions section references defense-in-depth hygiene — verify the maxTurns examples are still reasonable.
- Do NOT restructure. Only fix factual inaccuracies.

Both files are small (102 and 116 lines). If no factual inaccuracies exist, make no changes and note in the summary that they were reviewed and found current.
  </action>
  <verify>
    <automated>cd /Users/cpconnor/projects/Pete-Gets-Shit-Done && grep -n "16 agents\|15 agents\|18 agents\|16 built-in\|15 built-in\|18 built-in" Petes-Get-Shit-Done-Coding-Automation/references/agent-design-patterns.md Petes-Get-Shit-Done-Coding-Automation/references/frontmatter-reference.md; echo "REF_CHECK_DONE"</automated>
  </verify>
  <acceptance_criteria>
    - grep for stale agent counts (15, 16, or 18) in both reference files returns 0 matches
    - No factual inaccuracies remain in either file
    - Files are not restructured (line count delta is less than 10 lines from current)
  </acceptance_criteria>
  <done>Both reference docs reviewed and updated (or confirmed current). No stale facts remain.</done>
</task>

</tasks>

<verification>
Cross-check all 4 files for consistency:
```bash
grep -n "17 built-in\|63 slash\|63 commands\|45 skills\|479 test\|2,490\|2490" CLAUDE.md Petes-Get-Shit-Done-Coding-Automation/CLAUDE.md
```
Both CLAUDE.md files should show matching counts.
</verification>

<success_criteria>
- Both CLAUDE.md files contain identical D-05 counts
- Agent name lists contain exactly 17 names in both files
- Reference docs contain no stale agent counts
- No stale count references remain in any of the 4 files
</success_criteria>

<output>
After completion, create `.planning/phases/48-final-documentation-sync/48-02-SUMMARY.md`
</output>
