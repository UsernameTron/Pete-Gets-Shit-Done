---
phase: 46-housekeeping
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - commands/gsd/add-backlog.md
  - commands/gsd/crew.md
  - commands/gsd/debug.md
  - commands/gsd/finalize.md
  - commands/gsd/join-discord.md
  - commands/gsd/portfolio.md
  - commands/gsd/prime-patterns.md
  - commands/gsd/reapply-patches.md
  - commands/gsd/resume-work.md
  - commands/gsd/review-backlog.md
  - commands/gsd/set-profile.md
  - commands/gsd/thread.md
  - commands/gsd/workstreams.md
  - get-shit-done/workflows/discovery-phase.md
  - get-shit-done/workflows/verify-phase.md
autonomous: true
requirements:
  - LINK-01

must_haves:
  truths:
    - "Every command without a matching workflow has an inline exemption comment"
    - "Every workflow is referenced by at least one command or workflow"
    - "The convention for command/workflow linkage is documented"
    - "Orphaned workflows that are truly unreferenced are annotated or removed"
  artifacts:
    - path: "commands/gsd/add-backlog.md"
      provides: "Inline exemption annotation"
      contains: "<!-- workflow-exemption:"
    - path: "commands/gsd/crew.md"
      provides: "Inline exemption annotation"
      contains: "<!-- workflow-exemption:"
    - path: "commands/gsd/debug.md"
      provides: "Inline exemption annotation"
      contains: "<!-- workflow-exemption:"
    - path: "get-shit-done/workflows/discovery-phase.md"
      provides: "Orphan annotation or removal"
    - path: "get-shit-done/workflows/verify-phase.md"
      provides: "Orphan annotation or removal"
  key_links:
    - from: "commands/gsd/*.md"
      to: "get-shit-done/workflows/*.md"
      via: "name-matching convention or cross-reference annotation"
      pattern: "workflow-exemption:|workflow:"
---

<objective>
Resolve command/workflow linkage drift by adding inline exemption annotations to commands that intentionally operate without a companion workflow, annotating orphaned workflows, and documenting the convention.

Purpose: The foundation audit found 15 commands without matching workflows and 8 workflows without matching commands. Some are intentional (simple commands, internal sub-workflows) but the convention is not documented, making it impossible to distinguish intentional gaps from bugs.
Output: All 15 unlinked commands annotated, 2 truly orphaned workflows annotated, convention documented inline.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/46-housekeeping/46-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add exemption annotations to commands without workflows</name>
  <files>
    commands/gsd/add-backlog.md
    commands/gsd/crew.md
    commands/gsd/debug.md
    commands/gsd/finalize.md
    commands/gsd/join-discord.md
    commands/gsd/portfolio.md
    commands/gsd/prime-patterns.md
    commands/gsd/reapply-patches.md
    commands/gsd/resume-work.md
    commands/gsd/review-backlog.md
    commands/gsd/set-profile.md
    commands/gsd/thread.md
    commands/gsd/workstreams.md
  </files>
  <read_first>
    - commands/gsd/audit-agents.md (reference — existing exemption comment pattern on line 17)
    - commands/gsd/audit-deps.md (reference — existing exemption comment pattern on line 17)
    - commands/gsd/add-backlog.md
    - commands/gsd/crew.md
    - commands/gsd/debug.md
    - commands/gsd/finalize.md
    - commands/gsd/join-discord.md
    - commands/gsd/portfolio.md
    - commands/gsd/prime-patterns.md
    - commands/gsd/reapply-patches.md
    - commands/gsd/resume-work.md
    - commands/gsd/review-backlog.md
    - commands/gsd/set-profile.md
    - commands/gsd/thread.md
    - commands/gsd/workstreams.md
  </read_first>
  <action>
The existing convention (from audit-agents.md and audit-deps.md) uses a **Convention note:** paragraph inside the `<objective>` block. For consistency, use a standardized HTML comment at the top of each file, immediately after the closing `---` of the YAML frontmatter. This is machine-parseable and does not affect the rendered skill description.

**Format:** Insert on the line immediately after the closing `---` of YAML frontmatter:
```
<!-- workflow-exemption: {reason} -->
```

Add this annotation to each of the 13 commands listed below. Use the EXACT reason strings specified:

| Command file | Reason string |
|---|---|
| `add-backlog.md` | `inline — simple file append, no orchestration needed` |
| `crew.md` | `inline — agent scanning and display logic is self-contained` |
| `debug.md` | `inline — persistent debug state managed entirely within command` |
| `finalize.md` | `inline — orchestrates existing commands (complete-milestone, cleanup, stats, session-report) inline` |
| `join-discord.md` | `inline — single URL display, no logic` |
| `portfolio.md` | `inline — cross-project scanning is self-contained` |
| `prime-patterns.md` | `inline — session context injection is self-contained` |
| `reapply-patches.md` | `inline — patch file detection and reapply is self-contained` |
| `resume-work.md` | `delegates to resume-project workflow (name mismatch is intentional: command is user-facing verb, workflow is system action)` |
| `review-backlog.md` | `inline — backlog review and promotion is self-contained` |
| `set-profile.md` | `inline — config file update, no orchestration needed` |
| `thread.md` | `inline — thread CRUD is self-contained` |
| `workstreams.md` | `inline — workstream CRUD delegates to lib/workstream.cjs, no workflow needed` |

Note: `audit-agents.md` and `audit-deps.md` already have convention notes in their `<objective>` blocks. Also add the standardized `<!-- workflow-exemption: -->` comment to these two files for machine parseability:
- `audit-agents.md`: `<!-- workflow-exemption: inline — orchestration is simple enough to live inline (spawns gsd-ecosystem-auditor agent directly) -->`
- `audit-deps.md`: `<!-- workflow-exemption: inline — orchestration is simple enough to live inline (spawns gsd-dependency-auditor agent directly) -->`

That brings the total to 15 commands with exemption annotations (13 new + 2 existing getting the standardized comment).
  </action>
  <verify>
    <automated>cd /Users/cpconnor/projects/Pete-Gets-Shit-Done && count=$(grep -rl "workflow-exemption:" commands/gsd/*.md | wc -l | tr -d ' ') && echo "Commands with exemption: $count" && test "$count" -ge 15</automated>
  </verify>
  <acceptance_criteria>
    - At least 15 files in commands/gsd/ contain the string "<!-- workflow-exemption:"
    - commands/gsd/add-backlog.md contains "<!-- workflow-exemption: inline"
    - commands/gsd/crew.md contains "<!-- workflow-exemption: inline"
    - commands/gsd/resume-work.md contains "<!-- workflow-exemption: delegates to resume-project workflow"
    - commands/gsd/audit-agents.md contains "<!-- workflow-exemption:"
    - commands/gsd/audit-deps.md contains "<!-- workflow-exemption:"
    - The annotation is placed after the closing --- of YAML frontmatter in each file
  </acceptance_criteria>
  <done>All 15 commands without matching workflows have standardized inline exemption annotations</done>
</task>

<task type="auto">
  <name>Task 2: Annotate orphaned workflows</name>
  <files>
    get-shit-done/workflows/discovery-phase.md
    get-shit-done/workflows/verify-phase.md
  </files>
  <read_first>
    - get-shit-done/workflows/discovery-phase.md
    - get-shit-done/workflows/verify-phase.md
    - get-shit-done/workflows/diagnose-issues.md (reference — internal sub-workflow referenced by verify-work.md)
    - get-shit-done/workflows/discuss-phase-assumptions.md (reference — internal sub-workflow referenced by discuss-phase.md)
  </read_first>
  <action>
Two workflows are not referenced by any command or any other workflow:

1. **discovery-phase.md** — No command or workflow references it. It appears to be a remnant from an earlier architecture.
2. **verify-phase.md** — Only referenced in templates (phase-prompt.md, roadmap.md), not by any command or runtime workflow.

For each, add an HTML comment at the top of the file (before any YAML frontmatter) indicating orphan status:

For `discovery-phase.md`:
```
<!-- orphan-workflow: not referenced by any command or workflow as of v2.4. Retained for potential future use. If still orphaned at v2.5, consider removal. -->
```

For `verify-phase.md`:
```
<!-- orphan-workflow: referenced only by templates (phase-prompt.md, roadmap.md), not by any command or runtime workflow as of v2.4. Retained for template compatibility. -->
```

The remaining 6 "unmatched" workflows are NOT orphans — they are internal sub-workflows:
- `diagnose-issues.md` — referenced by `verify-work.md` workflow
- `discuss-phase-assumptions.md` — referenced by `discuss-phase.md` command
- `execute-plan.md` — referenced by `execute-phase.md` workflow and command
- `node-repair.md` — referenced by `execute-plan.md` workflow (and self-references)
- `resume-project.md` — referenced by `resume-work.md` command (name mismatch documented in Task 1)
- `transition.md` — referenced by 5 workflows (add-tests, autonomous, execute-phase, new-milestone, new-project)

No changes needed for these 6.
  </action>
  <verify>
    <automated>cd /Users/cpconnor/projects/Pete-Gets-Shit-Done && grep "orphan-workflow:" get-shit-done/workflows/discovery-phase.md && grep "orphan-workflow:" get-shit-done/workflows/verify-phase.md</automated>
  </verify>
  <acceptance_criteria>
    - get-shit-done/workflows/discovery-phase.md contains "<!-- orphan-workflow:"
    - get-shit-done/workflows/verify-phase.md contains "<!-- orphan-workflow:"
    - discovery-phase.md annotation says "not referenced by any command or workflow"
    - verify-phase.md annotation says "referenced only by templates"
    - No other workflow files are modified
  </acceptance_criteria>
  <done>Both truly orphaned workflows are annotated with orphan status and retention rationale</done>
</task>

</tasks>

<verification>
- `grep -rl "workflow-exemption:" commands/gsd/*.md | wc -l` returns >= 15
- `grep -rl "orphan-workflow:" get-shit-done/workflows/*.md | wc -l` returns 2
- The 6 internal sub-workflows (diagnose-issues, discuss-phase-assumptions, execute-plan, node-repair, resume-project, transition) are NOT modified
- `npm test` passes (no behavior changes, annotation only)
</verification>

<success_criteria>
- Every command without a matching workflow file has a `<!-- workflow-exemption: -->` annotation
- Every truly orphaned workflow has a `<!-- orphan-workflow: -->` annotation
- The convention is self-documenting: any future audit can grep for these annotations to distinguish intentional gaps from drift
- All existing tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/46-housekeeping/46-02-SUMMARY.md`
</output>
