---
phase: 45-critical-fixes
plan: 02
type: execute
wave: 2
depends_on: []
files_modified:
  - lib/injection-patterns.json
  - get-shit-done/bin/lib/security.cjs
  - hooks/gsd-prompt-guard.js
  - scripts/build-hooks.js
  - tests/prompt-injection-scan.test.cjs
autonomous: true
requirements:
  - SECPAT-01
must_haves:
  truths:
    - "A single canonical JSON file defines all injection patterns used by both the hook and the library"
    - "The combined pattern set is a strict superset of both prior independent sets (23 unique patterns)"
    - "gsd-prompt-guard.js remains zero-dependency — patterns are inlined at build time by build-hooks.js"
    - "security.cjs loads patterns from the shared JSON at require time"
    - "All existing tests pass and a new test validates the canonical pattern file"
  artifacts:
    - path: "lib/injection-patterns.json"
      provides: "Canonical injection pattern source of truth"
      contains: "23 pattern objects with source, flags, and category fields"
    - path: "hooks/dist/gsd-prompt-guard.js"
      provides: "Built hook with all 23 patterns inlined from canonical source"
      contains: "INJECTION_PATTERNS"
  key_links:
    - from: "lib/injection-patterns.json"
      to: "get-shit-done/bin/lib/security.cjs"
      via: "require('./injection-patterns.json')"
      pattern: "INJECTION_PATTERNS"
    - from: "lib/injection-patterns.json"
      to: "hooks/dist/gsd-prompt-guard.js"
      via: "build-time inlining by scripts/build-hooks.js"
      pattern: "INJECTION_PATTERNS"
---

<objective>
Create a single canonical injection pattern source consumed by both gsd-prompt-guard.js (hook) and security.cjs (library), merging the bidirectional gap into a 23-pattern superset.

Purpose: The hook has 18 patterns and the library has 18 patterns with only 13 overlapping — leaving 5 patterns each that the other misses. This creates an inconsistent protection surface. A single source of truth eliminates drift.

Approach: JSON file as canonical source. security.cjs requires it directly. build-hooks.js inlines patterns into the hook at build time (maintaining zero-dependency hook independence).

Output: lib/injection-patterns.json, updated security.cjs, updated hook source with delimiters, updated build-hooks.js with pattern inlining.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/milestones/FOUNDATION-AUDIT-20260416.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create canonical injection-patterns.json</name>
  <read_first>
    - hooks/gsd-prompt-guard.js (current 18 hook patterns — lines 17-38)
    - get-shit-done/bin/lib/security.cjs (current 18 lib patterns — lines 117-149)
  </read_first>
  <files>
    lib/injection-patterns.json
  </files>
  <action>
    Create lib/injection-patterns.json containing all 23 unique patterns as an array of objects.
    Each object has three fields: "source" (regex source string), "flags" (regex flags), "category" (classification).

    The 23 patterns by category:

    override (5 shared): standard instruction override attempts
    impersonation (4 — 3 shared + 1 lib-only "act as"): role/identity manipulation
    extraction (3 — 1 shared + 1 lib-only "what are your" + 1 hook-only "write/save/dump"): system prompt extraction
    delimiter (4 shared): hidden XML/markdown instruction markers
    encoded (1 hook-only): base64 fragment detection
    instruction_delimiter (1 hook-only): equals-sign instruction delimiters
    markdown_role (1 hook-only): markdown heading role injection
    multilingual (1 hook-only): non-English override attempts
    exfiltration (2 lib-only): network and encoding exfiltration
    tool_manipulation (1 lib-only): tool invocation manipulation

    Merge patterns from BOTH sources. Copy exact regex source strings from the original files.
    The "act as" pattern from security.cjs has a negative lookahead (?!plan|phase|wave) that must be preserved.

    Validate: node -e "const p = require('./lib/injection-patterns.json'); console.log(p.length)" should output 23.
    Validate: node -e "const p = require('./lib/injection-patterns.json'); p.forEach(x => new RegExp(x.source, x.flags)); console.log('all valid')" should output "all valid".
  </action>
  <verify>
    <automated>node -e "const p = require('./lib/injection-patterns.json'); console.log(p.length); p.forEach(x => new RegExp(x.source, x.flags)); console.log('all valid')"</automated>
  </verify>
  <acceptance_criteria>
    - node -e "console.log(require('./lib/injection-patterns.json').length)" outputs 23
    - node -e "const p = require('./lib/injection-patterns.json'); p.forEach(x => new RegExp(x.source, x.flags)); console.log('ok')" outputs "ok" (no throw)
    - grep -c '"category"' lib/injection-patterns.json returns 23
    - grep -c '"override"' lib/injection-patterns.json returns 5
    - grep -c '"impersonation"' lib/injection-patterns.json returns 4
    - grep -c '"exfiltration"' lib/injection-patterns.json returns 2
  </acceptance_criteria>
  <done>Canonical injection-patterns.json created with all 23 patterns, each compiles to valid RegExp</done>
</task>

<task type="auto">
  <name>Task 2: Update security.cjs to consume shared patterns</name>
  <read_first>
    - get-shit-done/bin/lib/security.cjs (lines 117-149 — current hardcoded INJECTION_PATTERNS array)
    - lib/injection-patterns.json (just created — canonical source)
  </read_first>
  <files>
    get-shit-done/bin/lib/security.cjs
  </files>
  <action>
    Replace the hardcoded INJECTION_PATTERNS array (lines 117-149) in security.cjs with a dynamic load from the canonical JSON.

    Use require with path.join to navigate from get-shit-done/bin/lib/ up to project root then into lib/:
    require(path.join(__dirname, '..', '..', '..', 'lib', 'injection-patterns.json'))
    Then .map(p => new RegExp(p.source, p.flags)) to compile patterns.

    Add a comment noting this is loaded from the canonical shared source (lib/injection-patterns.json).

    Do NOT change scanForInjection() or the module.exports — only the INJECTION_PATTERNS constant declaration.
  </action>
  <verify>
    <automated>node -e "const s = require('./get-shit-done/bin/lib/security.cjs'); console.log(s.INJECTION_PATTERNS.length); console.log(s.scanForInjection('act as a hacker').clean)"</automated>
  </verify>
  <acceptance_criteria>
    - node -e "console.log(require('./get-shit-done/bin/lib/security.cjs').INJECTION_PATTERNS.length)" outputs 23
    - node -e "const s = require('./get-shit-done/bin/lib/security.cjs'); console.log(s.scanForInjection('act as a plan').clean)" outputs true (negative lookahead preserved)
    - grep -c "injection-patterns.json" get-shit-done/bin/lib/security.cjs returns at least 1
    - npm test exits with code 0
  </acceptance_criteria>
  <done>security.cjs loads patterns from canonical JSON; pattern count increased from 18 to 23; all existing behavior preserved</done>
</task>

<task type="auto">
  <name>Task 3: Update build-hooks.js to inline patterns and update hook source</name>
  <read_first>
    - scripts/build-hooks.js (current build mechanism — uses fs.copyFileSync)
    - hooks/gsd-prompt-guard.js (current hook with hardcoded 18 patterns)
    - lib/injection-patterns.json (canonical pattern source)
  </read_first>
  <files>
    scripts/build-hooks.js
    hooks/gsd-prompt-guard.js
  </files>
  <action>
    Step A: Add pattern delimiters to hook source.
    In hooks/gsd-prompt-guard.js, wrap the existing INJECTION_PATTERNS array with delimiter comments:
    // --- BEGIN_INJECTION_PATTERNS ---
    const INJECTION_PATTERNS = [ ...all 23 patterns as RegExp literals... ];
    // --- END_INJECTION_PATTERNS ---

    Replace the existing 18-pattern array with all 23 patterns (merged from both sources).
    Add a comment above noting source of truth is lib/injection-patterns.json and to edit there, not here.

    Step B: Add pattern inlining to build-hooks.js.
    After the existing copy step, add logic for gsd-prompt-guard.js that:
    1. Reads lib/injection-patterns.json
    2. Generates JS regex literal lines from pattern objects
    3. Replaces the block between BEGIN_INJECTION_PATTERNS and END_INJECTION_PATTERNS in the dest file
    4. Writes back to dest

    This ensures dist/gsd-prompt-guard.js always reflects the canonical patterns regardless of source drift.
  </action>
  <verify>
    <automated>node scripts/build-hooks.js && grep -c "BEGIN_INJECTION_PATTERNS" hooks/dist/gsd-prompt-guard.js</automated>
  </verify>
  <acceptance_criteria>
    - grep "BEGIN_INJECTION_PATTERNS" hooks/gsd-prompt-guard.js returns a match
    - grep "END_INJECTION_PATTERNS" hooks/gsd-prompt-guard.js returns a match
    - grep "injection-patterns.json" hooks/gsd-prompt-guard.js returns a match (comment)
    - grep "injection-patterns.json" scripts/build-hooks.js returns a match
    - node scripts/build-hooks.js exits with code 0
    - grep -c "BEGIN_INJECTION_PATTERNS" hooks/dist/gsd-prompt-guard.js returns 1
    - npm test exits with code 0
  </acceptance_criteria>
  <done>build-hooks.js inlines canonical patterns into hook at build time; hook source has delimiter markers; dist hook has all 23 patterns</done>
</task>

<task type="auto">
  <name>Task 4: Add pattern superset validation test</name>
  <read_first>
    - tests/prompt-injection-scan.test.cjs (existing injection pattern tests)
    - lib/injection-patterns.json (canonical pattern source)
    - get-shit-done/bin/lib/security.cjs (now loads from canonical source)
  </read_first>
  <files>
    tests/prompt-injection-scan.test.cjs
  </files>
  <action>
    Add a new describe block to tests/prompt-injection-scan.test.cjs that validates the canonical pattern file:
    - injection-patterns.json exists and contains 23 patterns
    - All patterns compile to valid RegExp
    - Every pattern has required fields (source, flags, category)
    - security.cjs INJECTION_PATTERNS count matches canonical count
    - Canonical set includes all original hook-only patterns (base64, delimiter, markdown, multilingual, prompt-leak)
    - Canonical set includes all original lib-only patterns (act-as, what-are-your, exfiltration, tool-manipulation)
  </action>
  <verify>
    <automated>npm test 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - grep "canonical injection patterns" tests/prompt-injection-scan.test.cjs returns a match
    - grep "injection-patterns.json" tests/prompt-injection-scan.test.cjs returns a match
    - npm test exits with code 0
  </acceptance_criteria>
  <done>Test suite validates canonical pattern file: count, compilation, required fields, superset coverage of both original sources</done>
</task>

</tasks>

<verification>
- lib/injection-patterns.json exists with 23 patterns, all compile to valid RegExp
- security.cjs loads from canonical source, exports 23-pattern INJECTION_PATTERNS array
- hooks/gsd-prompt-guard.js source has BEGIN/END delimiters with 23 patterns
- hooks/dist/gsd-prompt-guard.js has 23 inlined patterns from build-hooks.js
- build-hooks.js reads canonical JSON and inlines into hook during build
- New tests validate pattern count, compilation, field presence, and superset coverage
- npm test passes with 0 failures
</verification>

<success_criteria>
1. A single canonical injection pattern source exists at lib/injection-patterns.json
2. security.cjs draws its INJECTION_PATTERNS from the canonical source
3. gsd-prompt-guard.js dist version draws patterns from canonical source via build-time inlining
4. The combined set (23 patterns) is a strict superset of both prior independent sets
5. All existing tests continue to pass
</success_criteria>

<output>
After completion, create .planning/phases/45-critical-fixes/45-02-SUMMARY.md
</output>
