---
phase: ad-hoc
task: 6
total_tasks: 7
status: in_progress
last_updated: 2026-04-24T17:14:27.039Z
---

<current_state>
Building `/gsd:harden-repo` command on branch `feat/harden-repo`. All 5 implementation files are created and working. Tests passed 2644/2644 (0 fail). Two small fixes were applied to harden-repo.cjs just before pause — need to re-run tests and do live verification.
</current_state>

<completed_work>

- Task 1: `get-shit-done/bin/lib/harden-repo.cjs` — Core CJS module with execGh, preflight, readProtection, buildGapTable, mergePolicy, applyFix, cmdHardenRepo. Uses execFileSync (not execSync) for shell safety.
- Task 2: `tests/harden-repo.test.cjs` — 23 assertions across 4 test groups: STANDARD_POLICY shape, buildGapTable (pure function), mergePolicy (pure function), dispatch integration.
- Task 3: `get-shit-done/bin/gsd-tools.cjs` — Added dispatch case for `harden-repo` with parseNamedArgs for --fix, --dry-run, --branch flags. Added JSDoc entry under Governance section.
- Task 4: `get-shit-done/commands/gsd/harden-repo.md` — Markdown command file with frontmatter routing to workflow.
- Task 5: `get-shit-done/workflows/harden-repo.md` — Step-by-step workflow with preflight, audit, fix_confirmation, apply, verify steps.
</completed_work>

<remaining_work>

- Task 6: Run `npm test` to verify the two in-flight fixes (normalizeRestrictions + undefined-to-null normalization in check())
- Task 7: Live test — `node get-shit-done/bin/gsd-tools.cjs harden-repo` should show restrictions as PASS now. Then `--fix --dry-run` to verify PUT payload includes both required_pull_request_reviews and required_status_checks with preserved contexts.
</remaining_work>

<decisions_made>

- Separate CJS module (harden-repo.cjs) rather than adding to commands.cjs — self-contained domain with 5+ internal functions
- Export buildGapTable and mergePolicy for testability — pure functions tested without gh mocking
- execFileSync over execSync — prevents shell injection, args as array
- normalizeRestrictions treats empty {users:[],teams:[],apps:[]} as null — GitHub returns object for "no restrictions" but PUT uses null
- No audit log file — command outputs to stdout via output(), consistent with stats/health pattern
</decisions_made>

<blockers>
None.
</blockers>

<context>
Two small fixes were applied to harden-repo.cjs right before pause:
1. Added `normalizeRestrictions()` helper — GitHub returns `{users:[],teams:[],apps:[]}` for "no restrictions" but the standard policy expects `null`. Without this, restrictions always showed FAIL on real repos.
2. Added `undefined -> null` normalization in `check()` — `JSON.stringify` drops undefined values, causing the `actual` field to disappear from gap output.

These fixes have NOT been tested yet. The live test against UsernameTron/Pete-Gets-Shit-Done showed 5/11 PASS before the fix (restrictions was FAIL, PR reviews missing). After the fix, restrictions should flip to PASS (6/11). The remaining FAILs are real — the repo needs required_pull_request_reviews and strict status checks.

Plan file: `/Users/cpconnor/.claude/plans/expressive-jumping-lerdorf.md`
</context>

<next_action>
Start with: `npm test` to confirm 2644/2644 pass with the two fixes. Then `node get-shit-done/bin/gsd-tools.cjs harden-repo` for live verification. Then commit all files on feat/harden-repo branch.
</next_action>
