---
branch: feat/lesson-capture-enforcement
work_item: Commit 2 (lesson-capture matcher tightening) — Phase 1 done, Phase 2 pending
status: paused
last_updated: 2026-04-09T14:50:00Z
head: 3d22336
squash_target: 72acb61
ahead_of_main: 12
worktree: clean
---

# Commit 2 Resume — Phase 2 Test Authoring

## Where we stopped

Phase 1 of the 7-phase Option A plan is **done and committed** as `3d22336`
(chore spec reconciliation on `tasks/todo.md` cases #4, #6, #12 → 1). Hook
file `.claude/hooks/lesson-capture-gate.cjs` is at its final state — **do not
re-edit**. Phase 2 (append +73 tests) has **not been started**.

Pause reason: context hit 91% before test authoring could begin. Partial
test-file writes are worse than a clean checkpoint.

## Branch state

- Branch: `feat/lesson-capture-enforcement`
- HEAD: `3d22336` (chore(hooks): reconcile committed spec cases #4/#6/#12 to 1)
- Ahead of main: 12 commits
- Worktree: clean
- Squash target for final commit: `72acb61` (UNCHANGED — do `git reset --soft 72acb61`)

## Spec reconciliation lock (Phase 1 result)

`tasks/todo.md` cases — committed expected counts now read:

| # | Input (user-after-assistant) | Count | Reason |
|---|------------------------------|-------|--------|
| 4 | `"you're wrong. don't touch deriveSlug."` | 1 | Strong `you're wrong` sent#1 fires; Medium `don't` sent#2 blocked by T33 — `hasRebuttalMarker` misses `you're` contraction, `splitSentences` strips `wrong.` marker |
| 6 | `"try again. that's not right."` | 1 | Strong `that's not right` sent#2 fires; Medium `try again` sent#1 blocked by T33 — no rebuttal marker / 2nd-person in same sentence |
| 12 | `"incorrect. try again."` | 1 | Strong `incorrect` sent#1 fires; Medium `try again` sent#2 blocked by T33 — no rebuttal marker in same sentence |

`≥ 1` still preserves the block contract. Section E line 424 note in
tasks/todo.md also updated. This is the committed spec — **do not re-litigate**.

All other positive cases (#1/#2/#3/#5/#7/#8/#9/#10/#11) and all 12 negative
cases match on-disk matcher behavior unchanged.

## Phase 2 test authoring contract

**File**: `tests/lesson-capture-gate.test.cjs`
**Action**: Append +73 tests. Preserve all 25 existing tests verbatim — do not
edit them, do not reorder.
**Target total**: 98 tests (25 existing + 73 new)

### Block structure (exact counts)

| Block | Count | Target |
|-------|-------|--------|
| splitSentences unit | 8 | sentence splitter edge cases |
| hasRebuttalMarker unit | 7 | marker detection incl. `you ` variants, `— ` em-dash, `no,`/`wrong,` |
| isBootTurn unit | 6 | 3 clauses: boot markers, command+local-command-stdout siblings, short residual |
| matchStrongTier via countSignals | 7 | STRONG_USER substrings, STRONG_USER_SENTENCE_START, STRONG_ASSISTANT comma-precedent |
| matchMediumTier + speaker-gate (T33) | 8 | licensing via prev=assistant AND rebuttal marker present |
| fixture integration | 4 | routed through `parseTranscript` (NOT raw JSONL) |
| positive spec | 12 | all 12 reconciled cases from tasks/todo.md |
| negative spec | 12 | all 12 false-positive suppressions |
| Tightening 1 (double-quote strip) | 3 | `stripCode` killing `"…"` ≤200 char spans |
| Tightening 2 (strong-assistant comma-precedent) | 3 | sentence.slice(0, idx).includes(', ') gate |
| Tightening 3 (session-boot early return) | 3 | first-5-user-turn scan → whole-session 0 |
| **TOTAL NEW** | **73** | |

### Helpers already defined in file (reuse, do not redefine)

- `makeSandbox()`, `writeLessons()`, `writeTranscript()`, `runHook()`
- `userMsg(text, ts)` → `{type:'user', message:{role:'user', content:text}, timestamp:ts}`
- `asstMsg(text, ts)` → same shape, role:'assistant'

### CRITICAL: Fixture routing gotcha

Every fixture-driven test **MUST** route fixture input through `parseTranscript()`
first. Calling `countSignals()` directly on raw JSONL will silently return 0 on
any matcher bug (false green). The 4 fixture integration tests specifically
validate real-session false-positive suppression via
`tests/fixtures/layer1-false-positives.jsonl` — load with `fs.readFileSync` →
`parseTranscript(raw)` → `countSignals(messages)`.

### Strong-assistant resolution flag

Comma-precedent is the **primary** resolution: `sentence.slice(0, idx).includes(', ')`.
There is **NO** 500-char fallback. Tests for Tightening 2 must verify this
exact behavior — a strong-assistant phrase fires only when the preceding
content in the same sentence contains a `, ` separator, otherwise it only
fires sentence-initial.

## Phase 3–7 (unchanged from Option A plan)

3. **Local run**: `node --test tests/lesson-capture-gate.test.cjs` → expect 98/98. **HARD STOP** for ack.
4. **Delegate to test-runner subagent**: `npm test` + `npm run test:coverage`. Baseline 2094 → target 2167. Coverage ≥95% on `.claude/hooks/lesson-capture-gate.cjs`.
5. **Pre-commit report**. **HARD STOP** for ack.
6. **Squash**: `git reset --soft 72acb61` (NOT --hard), single `feat(hooks):` commit with message specified in plan.
7. **Final report**. **HARD STOP**. No rollout gate, no /gsd:next, no /clear.

## Hard stops (all 9)

1. Any fixture signal count > 0
2. Any existing test regression (25 baseline must stay green)
3. Hook coverage < 95%
4. npm test baseline ≠ 2094 before patch
5. Discovery of a 5th tightening need
6. Any spec mismatch beyond the 3 reconciled cases
7. Strong-assistant comma-precedent fails any of its 3 Tightening-2 tests
8. Session-boot early return fails any of its 3 Tightening-3 tests
9. Squash target drifts from `72acb61`

## Resume sequence (next session)

1. `/prime`
2. Read this file (`.continue-here.md`) + `tasks/todo.md` § Session Handoff
3. Verify git state: `HEAD=3d22336`, clean, 12 ahead of main
4. Verify hook unchanged: hash check on `.claude/hooks/lesson-capture-gate.cjs`
5. Open `tests/lesson-capture-gate.test.cjs`, confirm 25 tests present
6. Begin Phase 2: append +73 tests in the block order above
7. Run Phase 3 local check → **STOP for ack**

## Known stale (out of scope)

- `.planning/STATE.md` still reflects v2.0 Intelligence Layer milestone shipped 2026-04-05. Do NOT update in this branch — it's tracked as known stale and will be refreshed in a separate chore after lesson-capture lands.

## Prior session artifacts

- Commit 1 (Layer 1 fixture): landed earlier on this branch
- Commit 2 plan: `tasks/todo.md` §§ Commit 2 + `/Users/cpconnor/.claude/plans/partitioned-cooking-allen.md`
- Decision log: `state/decisions.md` (Options A–E analysis for 4th-tightening discovery)
- Fixture: `tests/fixtures/layer1-false-positives.jsonl` (immutable regression contract)

## Commits this session

- `3d22336` chore(hooks): reconcile committed spec cases #4/#6/#12 to 1

## Context at pause

~91% used. Phase 1 complete and committed. Phase 2 not started. Pausing is the
correct call — partial test-file writes leave a worse checkpoint than a clean
Phase 1-done boundary.
