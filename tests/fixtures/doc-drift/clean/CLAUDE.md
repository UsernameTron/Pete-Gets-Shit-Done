# Test Fixture: Clean CLAUDE.md

This is a synthetic fixture for the Doc Drift Detector clean-pass tests.
Numeric claims below are intentionally tiny round numbers and MUST match
the values in coverage/coverage-final.json plus the inventory the script
would compute against this fixture root (zero — there are no agents/
commands/skills/hooks under the fixture root).

- **Scale**: 5 test suites, 100 assertions, 90.00% line coverage

GSD ships **0 built-in agents**, **0 slash commands**, and **0 Claude Code skills**.

GSD also ships 0 built-in agents in this fixture.
