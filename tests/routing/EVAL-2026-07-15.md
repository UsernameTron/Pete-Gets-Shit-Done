# Routing Eval — 2026-07-15 (merge gate for bitter-lesson-surgery)

> **RE-SCORE (operator decision, 2026-07-15): GATE MET — 24/25 strict (96%).**
> Pete ratified the registry-honest targets for ids 6 and 23 (option 1): `gsd:fast` and `gsd:note` are the routed answers — the old table had never exposed them, and the blind judges' picks fit the descriptions as written. golden-intents.jsonl re-labeled accordingly; judge outputs unchanged (labels cannot influence blind judges, so re-scoring needs no re-run).
> Re-scored: 24/25 strict (96%), high-frequency 15/15, workflows 11/12 exact. The only non-exact is id 8, where the judge ASKED with the expected target in its options — sanctioned do.md ambiguity behavior. Root cause is a real description overlap: `gsd:discuss-phase`'s "--auto" sentence claims `workflow:smart-discuss`'s territory. **Accepted for merge; logged in BITTER_LESSON_LOG DEFERRED as description-quality follow-up.**
> Everything below this block is the original pre-decision report, preserved verbatim for the audit trail.

**Method:** 25 golden intents ([golden-intents.jsonl](golden-intents.jsonl)) resolved by 5 blind judges (5 intents each, session model, no tools, no repo access). Each judge received exactly what invocation-time routing sees: the `gsd-tools do-registry` JSON (78 entries) + do.md's route instruction verbatim. Ground-truth labels follow the deleted routing table's canonical targets.

## Result: GATE NOT MET — STOP

| Metric | Score |
|---|---|
| Strict accuracy (exact target) | **22/25 = 88%** |
| Expected-in-options (ambiguous counted if expected listed) | 23/25 = 92% |
| High-frequency intents | 13/15 hit — **2 high-freq misses** |
| Workflows (12 golden) | 11/12 strict (smart-discuss went to ask-user with expected in options) |

Gate condition was "≥~90% AND no high-frequency miss" — both legs fail.

## Misses

### Miss 1 — id 6 (freq: high)
- **Intent:** "fix the typo in the README title, nothing fancy"
- **Expected:** `gsd:quick`
- **Actual:** `gsd:fast`
- **Description that drove the wrong pick:** `gsd:fast` — "Execute a trivial task inline — no subagents, no planning overhead"
- **Losing description:** `gsd:quick` — "Execute a quick task with GSD guarantees (atomic commits, state tracking) but skip optional agents"
- **Judge's reason:** "Single-line README typo is the canonical trivial task — inline execution, no planning or subagent overhead, matching 'nothing fancy'"
- **Analysis:** Not a judgment failure — a registry-membership change. The old table had NO `gsd:fast` row (fast was never do-routable); the registry made every command routable, creating a quick-vs-fast choice the old table never faced. The judge's pick is defensible on the descriptions as written; arguably more faithful to the ask than the golden label.

### Miss 2 — id 23 (freq: high)
- **Intent:** "note for later: add retry logic to the CI watcher"
- **Expected:** `gsd:add-todo`
- **Actual:** `gsd:note`
- **Description that drove the wrong pick:** `gsd:note` — "Zero-friction idea capture. Append, list, or promote notes to todos."
- **Losing description:** `gsd:add-todo` — "Capture idea or task as todo from current conversation context"
- **Judge's reason:** "'Note for later' is zero-friction idea capture, the literal purpose of gsd:note"
- **Analysis:** Two capture commands with overlapping descriptions. The old table sent "a note, idea, or 'remember to…'" to add-todo; `gsd:note` (also not a row in the old table) now competes and its description literally contains "note." Same class as Miss 1: new-registry-membership collision, not description ambiguity a human would resolve differently.

### Soft miss — id 8 (freq: med) — judge ASKED instead of picking
- **Intent:** "discuss phase 58 but just take your recommended defaults, don't quiz me"
- **Expected:** `workflow:smart-discuss`
- **Actual:** AMBIGUOUS → options `[gsd:discuss-phase, workflow:smart-discuss]` (expected present)
- **Description that drove the collision:** `gsd:discuss-phase` — "…Use --auto to skip interactive questions (Claude picks recommended defaults)." — this sentence advertises smart-discuss's exact value proposition inside discuss-phase's description.
- **Analysis:** Asking is sanctioned do.md behavior for genuine ambiguity, and the ambiguity is real: the two descriptions claim the same capability. This is a description-overlap finding, not a judge error.

## Hits (22)

ids 1–5, 7, 9–22, 24, 25 — all exact, including every workflow except the id-8 ask, and 13/15 high-frequency intents. Judges' stated reasons consistently cite description content, not name-string matching (e.g., id 5 correctly distinguished ship-and-merge from gsd:ship: "gsd:ship stops before merge").

## Read-only observations (NO changes made — operator decides)

All three failures share one root cause: **the registry made previously-unroutable near-duplicate commands routable** (`gsd:fast` vs `gsd:quick`; `gsd:note` vs `gsd:add-todo`), and one description (`discuss-phase`'s `--auto` sentence) claims its sibling workflow's territory. Options if you want the gate green, in increasing order of surgery:
1. Re-label the golden set where the judge's pick is defensibly better (id 6, id 23 both arguable) — accuracy becomes 24/25 (96%) with id 8 the only ask. Cheapest, but ratifies registry behavior over old-table behavior.
2. Sharpen 3 descriptions to separate the near-duplicate pairs (quick vs fast, note vs add-todo, discuss-phase --auto vs smart-discuss) — description edits only, re-run eval.
3. Exclude sibling duplicates from the registry (restore old-table membership semantics) — code change to do-registry.

## Judge raw outputs

Judge 1 (ids 3,8,19,24,6): bug-to-branch ✓, AMBIGUOUS[discuss-phase, smart-discuss] ~, progress ✓, add-tests ✓, fast ✗
Judge 2 (ids 13,2,21,11,15): plan-phase ✓, adopt-codebase ✓, wrap-and-sync ✓, frontend-phase ✓, autonomous ✓
Judge 3 (ids 5,23,7,16,25): ship-and-merge ✓, note ✗, discuss-phase ✓, quality-sweep ✓, quick-change ✓
Judge 4 (ids 18,10,4,22,9): daily-startup ✓, add-phase ✓, debug ✓, groom-backlog ✓, idea-to-shipped ✓
Judge 5 (ids 1,17,12,20,14): new-project ✓, verify-work ✓, hardened-plan ✓, resume-work ✓, execute-phase ✓

Re-run procedure (every model upgrade): resolve each golden-intents.jsonl intent against `gsd-tools do-registry` output with the do.md route instruction, blind, then score strict accuracy vs `expected`.
